---
title: Improving Sparse Memory Finetuning
title_zh: 改进稀疏记忆微调
authors: "Satyam Goyal, Anirudh Kanchi, Garv Shah, Prakhar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05248v1"
tags: ["query:q3"]
score: 10.0
evidence: 用于持续适应且无灾难性遗忘的稀疏记忆微调
tldr: 本研究针对大语言模型在持续学习中的灾难性遗忘问题，改进了稀疏记忆微调（SMF）方法。通过在Qwen-2.5等模型中植入稀疏记忆模块，并引入基于KL散度的槽位选择机制来优先更新关键信息，实现了在消费级硬件上的高效知识获取。实验证明该方法能有效学习新知识并极大地减少对旧能力的干扰，验证了稀疏更新在实际场景中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05248v1/fig-001.webp\", \"caption\": \"Figure 2: Recovery phase training loss on the instruction dataset (using TF-IDF).\", \"page\": 4, \"index\": 1, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-002.webp\", \"caption\": \"Figure 3: Sparse update mechanism: only retrieved key/value entries receive gradients.\", \"page\": 4, \"index\": 2, \"width\": 582, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-003.webp\", \"caption\": \"Figure 4: Finetuning performance across datasets. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse memory variants.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-004.webp\", \"caption\": \"Figure 5: Finetuning performance when adapting to SimpleQA. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse finetuning variants.\", \"page\": 6, \"index\": 4, \"width\": 826, \"height\": 229}]"
motivation: 传统微调方法因修改共享密集表示而导致严重的灾难性遗忘和任务干扰。
method: 提出一种开源流水线为预训练模型添加稀疏记忆层，并利用KL散度识别并更新具有“惊奇性”的Token。
result: 改造后的模型在获取新事实知识的同时，仅产生极小的遗忘，验证了稀疏更新的有效性。
conclusion: 该研究证明了通过局部稀疏更新可以在消费级硬件上实现高效且低遗忘的模型持续学习。
---

## 摘要
大语言模型（LLMs）在训练后通常是静态的，但实际应用要求在不降低现有能力的情况下持续适应新知识。标准的模型更新方法，如全量微调或参数高效方法（如 LoRA），面临一个根本性的权衡：灾难性遗忘。它们修改了共享的密集表示，导致任务间的干扰。稀疏记忆微调（SMF）通过将更新定位到显式记忆层中的一小部分参数，提供了一种极具前景的替代方案。在这项工作中，我们提出了一个开源流水线，用于为现有的预训练模型（Qwen-2.5-0.5B）改造稀疏记忆模块，从而在消费级硬件上实现有效的持续学习。我们通过引入一种基于 Kullback-Leibler (KL) 散度的理论基础槽位选择机制来扩展先前的工作，该机制优先为相对于背景分布具有信息“惊奇性”的标记进行记忆更新。我们的实验表明，改造后的模型可以获取新的事实知识，同时对保留能力的遗忘极小，在实际环境中验证了稀疏更新假设。

## Abstract
Large Language Models (LLMs) are typically static after training, yet real-world applications require continual adaptation to new knowledge without degrading existing capabilities. Standard approaches to updating models, like full finetuning or parameter-efficient methods (e.g., LoRA), face a fundamental trade-off: catastrophic forgetting. They modify shared dense representations, causing interference across tasks. Sparse Memory Finetuning (SMF) offers a promising alternative by localizing updates to a small subset of parameters in explicit memory layers. In this work, we present an open-source pipeline to retrofit existing pretrained models (Qwen-2.5-0.5B) with sparse memory modules, enabling effective continual learning on consumer hardware. We extend prior work by introducing a theoretically grounded slot-selection mechanism based on Kullback-Leibler (KL) divergence, which prioritizes memory updates for informationally "surprising" tokens relative to a background distribution. Our experiments demonstrate that our retrofitted models can acquire new factual knowledge with minimal forgetting of held-out capabilities, validating the sparse update hypothesis in a practical setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Sparse Memory Finetuning》的结构化深入总结：

### 1. 核心问题与研究背景
现代大语言模型（LLMs）在部署后通常是静态的，但现实世界需要模型能够持续吸收新知识。传统的微调方法（如全量微调或 LoRA）存在**灾难性遗忘**问题，因为它们修改了全局共享的密集表示，导致新旧知识相互干扰。本文旨在改进**稀疏记忆微调（SMF）**技术，通过将更新局限在模型内部特定的“记忆槽位”中，实现高效的持续学习，同时保护模型原有的通用能力。

### 2. 方法论：核心思想与技术细节
本文提出了一种将标准 Transformer 改造为稀疏记忆增强模型的流水线，核心步骤如下：
*   **架构改造（Retrofitting）**：将预训练模型（如 Qwen-2.5）中部分层的密集前馈网络（FFN）替换为**稀疏记忆层**。该层包含查询投影矩阵、可训练的键（Keys）和值（Values）表。
*   **稀疏查找机制**：输入标记通过查询投影生成查询向量 $q$，通过 Top-k 检索与 $q$ 最匹配的 $k$ 个记忆槽位，输出为这些槽位对应值的加权和。
*   **三阶段流水线**：
    1.  **替换**：插入记忆层，丢弃原 FFN 权重。
    2.  **修复（Healing）**：在通用指令数据集上微调记忆参数，使新层与预训练的残差流对齐。
    3.  **任务特定微调**：仅更新被选中的少数记忆槽位。
*   **信息论槽位选择（核心创新）**：
    *   **KL 散度评分**：不同于传统的 TF-IDF 启发式方法，本文提出基于 KL 散度的评分规则。它计算当前批次槽位使用分布与背景分布之间的差异，优先更新那些表现出“信息惊奇性”（即在当前任务中被频繁使用但在背景数据中不常见的）的槽位。

### 3. 实验设计
*   **基础模型**：Qwen-2.5-0.5B-Instruct。
*   **数据集**：
    *   **修复阶段**：OpenAssistant (oasst1) 抽取 20,000 条样本。
    *   **目标任务**：TriviaQA（事实知识）和 SimpleQA（文档级问答）。
    *   **评估/保留任务**：GSM8K（数学推理能力）和 NaturalQuestions（通用问答）。
*   **对比基准（Baselines）**：
    *   **全量密集微调（Full Finetuning）**：更新所有模型参数。
    *   **TF-IDF 稀疏微调**：使用传统的词频-逆文档频率启发式方法选择更新槽位。

### 4. 资源与算力
*   论文明确提到该方法旨在使持续学习能够在**消费级硬件**上运行。
*   **具体算力说明**：文中未明确给出具体的 GPU 型号、数量或总训练时长。但从模型规模（0.5B）和样本量（1k-20k）来看，计算需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：论文针对两个主要目标任务（TriviaQA, SimpleQA）进行了实验，并跨越了两个保留任务（GSM8K, NQ）来监测遗忘情况。
*   **充分性评价**：实验设计较为合理，包含了“修复阶段”的损失曲线分析以及不同微调策略下的性能对比。然而，实验主要集中在极小规模模型（0.5B）上，且目标任务的数据量较小（1k 样本），对于更大规模模型或更长序列任务的泛化性验证略显不足。

### 6. 主要结论与发现
*   **塑性（Plasticity）**：稀疏记忆微调在目标任务上表现出比全量微调更快的适应速度，能在较少步数内达到高 F1 分数。
*   **稳定性（Stability）**：全量微调会导致 GSM8K 等推理任务性能严重下降（灾难性遗忘），而稀疏更新能显著保留这些能力。
*   **KL 评分的有效性**：KL 散度评分在处理复杂任务（如 SimpleQA）时表现出更好的稳定性，能防止噪声更新导致的模型漂移；但在简单任务（如 TriviaQA）中，其效果与 TF-IDF 接近。

### 7. 优点与亮点
*   **开源贡献**：提供了将现有开源模型改造为稀疏记忆架构的完整、可复现流水线。
*   **理论创新**：引入 KL 散度作为槽位选择的准则，比传统的 TF-IDF 更具信息论基础，能更精准地定位“知识更新区”。
*   **实用性**：证明了在不增加推理延迟（由于 Top-k 稀疏性）的情况下，可以在消费级硬件上实现高效的知识增量更新。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于 0.5B 参数的模型，尚未在大规模模型（如 7B 或 70B）上验证其改造效果。
*   **架构侵入性**：需要替换原有的 FFN 层，这在初期会导致模型性能大幅下降，必须经过“修复”阶段才能恢复，增加了实施复杂度。
*   **背景分布依赖**：KL 散度方法依赖于预先计算的背景分布统计，如果背景数据选择不当，可能会影响槽位选择的准确性。

（完）
