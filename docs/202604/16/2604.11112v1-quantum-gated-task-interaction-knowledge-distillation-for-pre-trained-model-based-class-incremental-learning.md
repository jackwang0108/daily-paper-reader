---
title: Quantum-Gated Task-interaction Knowledge Distillation for Pre-trained Model-based Class-Incremental Learning
title_zh: 基于预训练模型的类增量学习中的量子门控任务交互知识蒸馏
authors: "Linjie Li, Huiyu Xiao, Jiarui Cao, Zhenyu Wu, Yang Ji"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11112v1"
tags: ["query:q5"]
score: 10.0
evidence: 利用量子门控蒸馏解决类增量学习中的挑战
tldr: 针对预训练模型在类增量学习中因多任务子空间纠缠导致的灾难性遗忘问题，本文提出了一种量子门控任务交互知识蒸馏（QKD）框架。该框架利用量子门控机制动态建模任务间的关联性，并通过任务交互知识蒸馏在不同任务适配器间传递知识，有效弥合了表示差距，在多个基准测试中达到了领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11112v1/fig-001.webp\", \"caption\": \"Figure 2. Performance curve of various methods under different settings. The relative improvement of QKD over the second-best method at the final incremental stage is highlighted with numerical annotations.\", \"page\": 7, \"index\": 1, \"width\": 1036, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2604.11112v1/fig-002.webp\", \"caption\": \"Table 1. Average and final accuracy comparison across five datasets. Bold numbers indicate the best performance, while underlined values represent the second-best. All methods are evaluated under the exemplar-free setting.\", \"page\": 6, \"index\": 2, \"width\": 1031, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.11112v1/fig-003.webp\", \"caption\": \"Figure 1. Illustration of QKD. Left: The current task sample is encoded by the frozen PTM and the first adapter, while old adapters generate task representations forming a task pool. These representations are mapped into the quantum space to compute task correlations. Right: The task correlations produce task scores, which weight the feature-level knowledge distillation to guide new adapter training.\", \"page\": 5, \"index\": 3, \"width\": 1036, \"height\": 413}]"
motivation: 预训练模型在增量学习中难以处理多任务子空间的纠缠，导致任务路由校准不准或表示过于僵化，从而引发严重的遗忘问题。
method: 引入量子门控任务调制机制来捕捉样本与任务间的动态相关性，并据此引导旧适配器向新适配器进行任务交互式知识蒸馏。
result: 实验结果表明，QKD 框架在缓解灾难性遗忘方面表现出色，并在多个类增量学习基准测试中取得了当前最先进的性能。
conclusion: 通过量子门控建模任务间依赖关系并结合知识蒸馏，可以有效提升预训练模型在类增量学习中的知识积累与迁移能力。
---

## 摘要
类增量学习 (CIL) 旨在从任务流中持续积累知识，并为所有已见类别构建统一的分类器。尽管预训练模型 (PTM) 在 CIL 中表现出良好的性能，但它们仍难以处理多任务子空间的纠缠问题，当任务路由参数校准不良或任务级表示过于僵化时，会导致灾难性遗忘。为了解决这一问题，我们提出了一种新颖的量子门控任务交互知识蒸馏 (QKD) 框架，利用量子门控来引导任务间的知识迁移。具体而言，我们引入了一种量子门控任务调制门控机制，用于建模任务嵌入之间的关系依赖，动态捕捉流式任务中联合训练和推理的样本与任务相关性。在量子门控输出的指导下，我们利用这些任务嵌入级的相关性权重，在旧适配器和新适配器之间进行任务交互知识蒸馏，使模型能够弥合独立任务子空间之间的表示差距。广泛的实验表明，QKD 有效地缓解了遗忘，并达到了最先进的性能。

## Abstract
Class-incremental learning (CIL) aims to continuously accumulate knowledge from a stream of tasks and construct a unified classifier over all seen classes. Although pretrained models (PTMs) have shown promising performance in CIL, they still struggle with the entanglement of multi-task subspaces, leading to catastrophic forgetting when task routing parameters are poorly calibrated or task-level representations are rigidly fixed. To address this issue, we propose a novel Quantum-Gated Task-interaction Knowledge Distillation (QKD) framework that leverages quantum gating to guide inter-task knowledge transfer. Specifically, we introduce a quantum-gated task modulation gating mechanism to model the relational dependencies among task embedding, dynamically capturing the sample-to-task relevance for both joint training and inference across streaming tasks. Guided by the quantum gating outputs, we perform task-interaction knowledge distillation guided by these task-embedding-level correlation weights from old to new adapters, enabling the model to bridge the representation gaps between independent task subspaces. Extensive experiments demonstrate that QKD effectively mitigates forgetting and achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **QKD (Quantum-Gated Task-interaction Knowledge Distillation)** 的框架，旨在解决基于预训练模型（PTM）的类增量学习（CIL）中存在的任务子空间纠缠和灾难性遗忘问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在类增量学习中，模型需要不断学习新任务而不遗忘旧知识。虽然基于预训练模型（PTM）的方法（如 Prompt-based 或 Adapter-based）表现优异，但它们在处理多个任务时，不同任务的特征子空间容易发生**纠缠（Entanglement）**。
*   **研究动机**：现有的路由和融合机制多依赖简单的相似度度量或手工设计的启发式规则，难以捕捉复杂的任务间非线性相关性。当任务增多、子空间重叠严重时，模型在推理阶段（任务身份未知）容易产生预测偏差，导致严重的灾难性遗忘。

### 2. 方法论：核心思想与关键技术
QKD 框架的核心思想是利用**量子计算的几何表达能力**来建模样本与任务之间的复杂关系，并以此引导知识蒸馏。

*   **量子门控任务调制 (QGTM)**：
    *   **特征提取与降维**：冻结 PTM 主干，为每个任务分配轻量级适配器（Adapter）。利用 SVD（奇异值分解）提取旧任务适配器的主要成分，构建任务嵌入。
    *   **量子编码**：将样本特征和任务嵌入映射到高维希尔伯特空间（Hilbert Space）。通过参数化量子线路（PQC），利用量子叠加和干涉特性捕捉多路依赖关系。
    *   **相关性度量**：通过测量量子态之间的保真度（Fidelity），计算当前样本与各个历史任务之间的相关性得分（即样本-任务相关系数 $\alpha$）。
*   **任务交互知识蒸馏 (TIKD)**：
    *   利用量子门控输出的权重 $\alpha$，引导从旧适配器到新适配器的特征级知识迁移。
    *   **选择性吸收**：鼓励新任务从高度相关的旧任务中吸收知识，同时抑制不相关任务的干扰。
*   **优化目标**：结合分类损失（CE）、量子门控蒸馏损失（KL 散度）和稀疏正则化项（$L_1$ 正则，用于保持任务选择的专注度）。

### 3. 实验设计
*   **数据集**：涵盖了五个主流的 CIL 基准测试：CIFAR-100、CUB-200、ImageNet-A、ImageNet-R 和 VTAB。
*   **实验设置**：采用 **Exemplar-free（无样本保留）** 设置，即不存储任何旧任务的原始数据，这比传统的重放方法更具挑战性。
*   **对比方法**：
    *   传统 CIL 方法：iCaRL, DER, FOSTER, MEMO 等。
    *   基于 PTM 的 SOTA 方法：L2P, DualPrompt, CODA-Prompt, SimpleCIL, EASE, MoTE 等。
*   **Benchmark**：使用平均增量准确率（Average Accuracy）和最终准确率（Final Accuracy）作为核心评价指标。

### 4. 资源与算力
*   **硬件环境**：论文中**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到使用 PyTorch 实现，Batch Size 为 32，训练 20 个 Epoch，使用 SGD 优化器。
*   **效率分析**：论文在表 4 中对比了计算开销，指出量子门控模块虽然在推理延迟上略高于传统 MLP 或余弦相似度方法，但在参数量和内存占用上保持在同一量级（约 1.3M 参数，2.1GB 显存占用）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个不同规模和领域的标准数据集上进行了主实验。
    *   进行了**大基类（Large-base）**实验，验证模型在初始知识较多时的增量能力。
    *   **消融实验**：分别验证了 QGTM、TIKD 和稀疏正则化的有效性。
    *   **超参数敏感性分析**：对量子比特数、SVD 维度、蒸馏系数等进行了网格搜索。
*   **充分性评价**：实验设计较为全面，对比了从传统方法到最新 SOTA 的多种基准，且涵盖了不同难度的增量场景，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **量子优势**：量子门控机制在捕捉任务间复杂几何关系方面优于经典的余弦相似度、MLP 或注意力机制。
*   **性能领先**：QKD 在所有五个基准测试中均取得了最先进（SOTA）或极具竞争力的性能。例如，在 CIFAR-100 上比次优方法提升了约 1.02%，在 ImageNet-R 上提升了 2.37%。
*   **缓解遗忘**：通过动态的任务交互蒸馏，模型能有效弥合独立任务子空间之间的差距，显著降低了任务身份未知时的分类错误。

### 7. 优点（亮点）
*   **跨学科创新**：首次将量子计算（量子门控路由）引入类增量学习领域，利用量子希尔伯特空间的几何特性解决特征纠缠问题。
*   **端到端一致性**：量子门控生成的权重同时用于训练时的知识蒸馏和推理时的自适应路由，保证了训练与测试的一致性。
*   **高效性**：在不依赖旧样本（Exemplar-free）的情况下，性能超越了许多需要存储样本的方法。

### 8. 不足与局限
*   **推理延迟**：由于量子线路的模拟（在经典硬件上）涉及复杂的张量运算，其推理延迟（Latency）比纯经典方法高出约 50%-60%，这可能限制其在实时嵌入式设备上的应用。
*   **硬件依赖**：目前该方法是在经典计算机上模拟量子电路，尚未在真实的量子硬件上验证其抗噪能力。
*   **任务数量限制**：虽然在 20 个阶段的增量中表现良好，但随着任务数量进一步增加（如 100 个任务），量子比特的扩展性和状态坍缩风险（浓度效应）可能成为瓶颈。

（完）
