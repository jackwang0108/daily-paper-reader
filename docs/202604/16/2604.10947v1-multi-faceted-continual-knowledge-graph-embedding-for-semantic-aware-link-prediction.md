---
title: Multi-Faceted Continual Knowledge Graph Embedding for Semantic-Aware Link Prediction
title_zh: 面向语义感知链接预测的多维度持续知识图谱嵌入
authors: "Jing Qi, Yuxiang Wang, Zhiyuan Yu, Xiaoliang Xu, Yuanshi Zheng, Tianxing Wu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10947v1"
tags: ["query:q1"]
score: 9.0
evidence: 持续知识图谱嵌入以保留先前习得的知识
tldr: 针对持续知识图谱嵌入（CKGE）中实体语义随时间演化且易发生灾难性遗忘的问题，本文提出MF-CKGE框架。该框架通过将新旧知识分离到不同嵌入空间并进行语义解耦，有效捕捉实体的多面语义。在推理阶段，通过自适应识别查询相关的语义，提升了链接预测的准确性，在多个数据集上表现优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10947v1/fig-001.webp\", \"caption\": \"Table 1: Dataset statistics: |𝐸𝑖 |, |𝑅𝑖 |, and |Δ𝑇𝑖 | are the number of entities, relations, and new facts in each snapshot 𝑆𝑖\", \"page\": 6, \"index\": 1, \"width\": 1048, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2604.10947v1/fig-002.webp\", \"caption\": \"Figure 2: An evolving KGwith three snapshots: each contains multi-faceted knowledge updates about David Beckham\", \"page\": 2, \"index\": 2, \"width\": 472, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10947v1/fig-003.webp\", \"caption\": \"Table 3: Main results on the last four datasets. The best and second best results are highlighted in bold and underlined\", \"page\": 7, \"index\": 3, \"width\": 962, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10947v1/fig-004.webp\", \"caption\": \"Table 2: Main results on the first four datasets. The best and second best results are highlighted in bold and underlined\", \"page\": 7, \"index\": 4, \"width\": 964, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10947v1/fig-005.webp\", \"caption\": \"Figure 10: Importance for each snapshot embedding whether exhibiting distinct local peaks (e.g., 0.42 in HYBRID’s 𝑄4) or a relatively smoother attention distribution (as in FACT), their diagonal entries consistently remain the highest within their respective columns. Because𝑄𝑖 is inherently intended to evaluate the knowledge specific to snapshot 𝑆𝑖 , these results strongly confirm the model’s semantic-aware capability. It adaptively identifies the relevance of each embedding to the given query, leveraging critical historical knowledge while filtering out noise from irrelevant snapshots to preserve prediction accuracy.\", \"page\": 10, \"index\": 5, \"width\": 470, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.10947v1/fig-006.webp\", \"caption\": \"Figure 3: An overview of the proposed MF-CKGE framework\", \"page\": 4, \"index\": 6, \"width\": 994, \"height\": 422}]"
motivation: 现有CKGE方法将新旧知识混杂在同一嵌入空间，无法有效区分和捕捉实体随时间演化的多面语义特征。
method: 提出MF-CKGE框架，在离线学习时分离新旧知识空间并进行语义解耦，在线推理时根据查询相关性自适应选择实体嵌入。
result: "在八个数据集上的实验表明，MF-CKGE在MRR和Hits@10指标上分别比最优基准平均提升了1.7%和1.4%。"
conclusion: 通过多面语义建模和空间分离策略，MF-CKGE能更有效地处理持续学习中的知识演化，显著提升了链接预测性能。
---

## 摘要
持续知识图谱嵌入（CKGE）旨在持续学习新知识（即实体和关系）的嵌入，同时保留先前获取的知识。大多数现有的 CKGE 方法通过正则化或重放旧知识来缓解灾难性遗忘。它们将实体的旧知识和新知识合并在同一个嵌入空间中，以寻求两者之间的平衡。然而，实体本质上表现出多维度的语义，这些语义随着其关系上下文随时间的变化而动态演变。共享嵌入无法捕捉和区分这些时间语义变化，从而降低了跨快照的终身链接预测准确性。为了解决这一问题，我们提出了一种用于语义感知链接预测的多维度 CKGE 框架（MF-CKGE）。在离线学习阶段，MF-CKGE 将时间上的旧知识和新知识分离到不同的嵌入空间中，以防止知识纠缠，并采用语义解耦来减少语义冗余，从而提高空间效率。在在线推理阶段，MF-CKGE 通过量化语义重要性，自适应地识别与查询语义相关的实体嵌入，从而减少来自查询无关噪声的干扰。在八个数据集上的实验表明，MF-CKGE 在 MRR 和 Hits@10 指标上较最优基准模型分别实现了 1.7%（最大 2.7%）和 1.4%（最大 3.8%）的平均提升。我们的源代码和数据集可在以下地址获取：https://anonymous.4open.science/r/MF-CKGE-04E5。

## Abstract
Continual Knowledge Graph Embedding (CKGE) aims to continually learn embeddings for new knowledge, i.e., entities and relations, while retaining previously acquired knowledge. Most existing CKGE methods mitigate catastrophic forgetting via regularization or replaying old knowledge. They conflate new and old knowledge of an entity within the same embedding space to seek a balance between them. However, entities inherently exhibit multi-faceted semantics that evolve dynamically as their relational contexts change over time. A shared embedding fails to capture and distinguish these temporal semantic variations, degrading lifelong link prediction accuracy across snapshots. To address this, we propose a Multi-Faceted CKGE framework (MF-CKGE) for semantic-aware link prediction. During offline learning, MF-CKGE separates temporal old and new knowledge into distinct embedding spaces to prevent knowledge entanglement and employs semantic decoupling to reduce semantic redundancy, thereby improving space efficiency. During online inference, MF-CKGE adaptively identifies semantically query-relevant entity embeddings by quantifying their semantic importance, reducing interference from query-irrelevant noise. Experiments on eight datasets show that MF-CKGE achieves an average (maximum) improvement of 1.7% (2.7%) and 1.4% (3.8%) in MRR and Hits@10, respectively, over the best baseline. Our source code and datasets are available at: https://anonymous.4open.science/r/MF-CKGE-04E5.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-Faceted Continual Knowledge Graph Embedding for Semantic-Aware Link Prediction》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**持续知识图谱嵌入（CKGE）**任务。在现实世界中，知识图谱（KG）是动态演化的，新实体和关系不断涌现。
*   **研究动机**：现有CKGE方法通常将实体的所有知识（无论新旧）压缩在同一个共享嵌入空间中。然而，实体具有**多面语义（Multi-faceted Semantics）**，例如“大卫·贝克汉姆”在不同时期可能涉及家庭、职业、爱好等不同领域的语义。
*   **核心痛点**：共享嵌入空间会导致新旧知识纠缠，难以捕捉语义的时间演化，且在推理时会引入与查询无关的噪声（例如：查询家庭成员时，职业背景的嵌入维度成了噪声），从而降低链接预测的准确性。

### 2. 方法论：MF-CKGE 框架
论文提出了 **MF-CKGE** 框架，核心思想是通过“空间分离”和“语义感知”来处理多面语义。

*   **实体嵌入解耦（离线学习）**：
    *   **时间解耦**：为每个时间快照（Snapshot）分配独立的嵌入空间。新知识在当前空间学习，旧知识在历史空间冻结，避免知识覆盖。
    *   **语义解耦**：为了节省空间，计算当前嵌入与历史嵌入的余弦相似度。若相似度高于阈值 $\theta$，则认为语义冗余，仅保留指向历史嵌入的指针；若低于阈值，则保留新嵌入以捕捉新语义面。
    *   **损失函数**：结合了知识表达损失（TransE/RotatE等）、跨快照关系对齐损失（保持关系语义稳定性）和邻域语义一致性损失（利用掩码自编码器 MAE）。
*   **语义感知链接预测（在线推理）**：
    *   **重要性计算**：根据查询中的关系与各快照中关联关系的语义相似度，利用 Top-$k$ 策略计算每个快照嵌入对当前查询的“贡献度”（权重）。
    *   **加权聚合**：将不同快照产生的预测得分进行加权求和，从而抑制无关噪声，放大相关语义。

### 3. 实验设计
*   **数据集**：使用了 8 个数据集，包括 ENTITY、RELATION、HYBRID、FB-CKGE、GraphEqual、GraphHigher、GraphLower，以及作者新构建的大规模语义多样化数据集 **DB-CKGE**（基于 DBpedia）。
*   **Benchmark 任务**：终身链接预测（Lifelong Link Prediction），即在学习完当前快照后，在所有已见过的测试集上评估性能。
*   **对比方法**：涵盖了三类主流 CKGE 方法：
    1.  正则化类：IncDE, LKGE, SI, EWC。
    2.  动态架构类：FastKGE, CWR, PNN。
    3.  重放类：DiCGRL, EMR, GEM。
    4.  参考基准：Retrain（从头全量训练，性能上限但开销巨大）。

### 4. 资源与算力
*   **硬件环境**：Linux 服务器，2.4 GHz CPU，128 GB 内存，**NVIDIA RTX A6000 GPU**。
*   **训练时长**：MF-CKGE 表现出极高的效率。例如在 FB-CKGE 数据集上，MF-CKGE 训练仅需 46 秒，比全量重训（Retrain）快 19 倍，比最强基准 IncDE 快 1.4 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个具有不同演化模式（实体增长、关系增长、混合增长等）的数据集上进行了测试，每组实验运行 5 次取平均值。
*   **消融实验**：针对“解耦模块”和“重要性计算模块”分别做了消融，验证了各部分的必要性。
*   **参数敏感性**：详细分析了语义阈值 $\theta$ 和 Top-$k$ 中 $k$ 值的取值影响。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了效果（MRR, Hits@N），还对比了算力开销（时间、显存），并提供了可视化案例分析，证明了模型确实能识别不同语义面。

### 6. 主要结论与发现
*   **性能领先**：MF-CKGE 在所有数据集上均优于现有 CKGE 基准，MRR 平均提升 1.7%，Hits@10 平均提升 1.4%。
*   **接近上限**：在某些数据集上，MF-CKGE 的表现甚至超过了全量重训（Retrain），证明了抑制语义噪声的有效性。
*   **空间效率**：通过语义解耦，可以在减少约 30% 存储空间的情况下，依然保持极高的预测精度。
*   **抗遗忘能力**：模型在保留旧知识的同时，能利用新知识反哺旧知识的预测。

### 7. 优点
*   **视角新颖**：首次明确提出并解决了 CKGE 中实体的“多面语义”纠缠问题。
*   **高效实用**：不需要复杂的重放缓冲区或大规模正则化计算，训练速度极快，显存占用低。
*   **灵活性强**：可以作为插件适配不同的基础 KGE 模型（如 TransE, RotatE, ComplEx）。

### 8. 不足与局限
*   **关系语义假设**：模型假设关系的语义是相对稳定且唯一的，虽然符合大多数 KG 情况，但对于某些语义随时间剧烈变化的关系可能处理不足。
*   **超参数依赖**：语义解耦阈值 $\theta$ 和 Top-$k$ 的 $k$ 值需要根据具体数据集进行微调，以平衡空间压缩率和精度。
*   **应用场景限制**：目前主要针对“增长型”KG（只增不减），对于包含大量“事实删除”或“属性修改”的动态图谱，其有效性尚待进一步验证。

（完）
