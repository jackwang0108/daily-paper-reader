---
title: "LDEPrompt: Layer-importance guided Dual Expandable Prompt Pool for Pre-trained Model-based Class-Incremental Learning"
title_zh: LDEPrompt：面向基于预训练模型的类增量学习的层重要性引导的双可扩展提示池
authors: "Linjie Li, Zhenyu Wu, Huiyu Xiao, Yang Ji"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11091v1"
tags: ["query:q5"]
score: 10.0
evidence: 基于提示词的类增量学习
tldr: 针对基于提示词的类增量学习中提示池固定、嵌入选择依赖人工及过度依赖预训练骨干网络等局限性，本文提出LDEPrompt。该方法引入了层重要性引导的双可扩展提示池，实现了自适应层选择以及提示池的动态冻结与扩展。实验证明，LDEPrompt在多个基准测试中达到了SOTA性能，展现了极佳的有效性和可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11091v1/fig-001.webp\", \"caption\": \"Fig. 1: Illustration of LDEPrompt. The upper part (from left to right) shows: (1) the forward-pass evaluation of layer importance before training; (2) task-specific prompt retrieval from the global prompt pool at layer K using the CLS token; and (3) the construction of a layer-specific prompt pool of size n, where selected prompts are frozen while newly initialized prompts remain trainable;(4) During training, prompts within each layer are merged to form the final task-specific representations.\", \"page\": 2, \"index\": 1, \"width\": 846, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.11091v1/fig-002.webp\", \"caption\": \"Table 1: Average and final accuracy comparison across three datasets. Bold numbers indicate the best performance, while underlined values represent the second-best. All methods are evaluated under the exemplar-free setting.\", \"page\": 3, \"index\": 2, \"width\": 952, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.11091v1/fig-003.webp\", \"caption\": \"Fig. 2: Performance curve of various methods under different settings.\", \"page\": 4, \"index\": 3, \"width\": 1054, \"height\": 277}]"
motivation: 现有方法存在提示池固定、手动选择嵌入以及在提示词选择上过度依赖预训练骨干网络的问题。
method: 提出一种层重要性引导的双可扩展提示池，支持自适应层选择以及提示池的动态冻结与扩展。
result: 在广泛使用的类增量学习基准测试中，LDEPrompt 取得了当前最先进的性能表现。
conclusion: LDEPrompt 通过动态扩展和自适应层管理，有效提升了预训练模型在类增量学习任务中的性能和可扩展性。
---

## 摘要
基于提示（Prompt）的类增量学习方法通常构建一个由多个可训练键-提示（key-prompts）组成的提示池，并进行实例级匹配以选择最合适的提示嵌入，这已展现出良好的效果。然而，现有方法面临若干局限性，包括固定的提示池、提示嵌入的人工选择，以及在提示选择上对预训练骨干网络的强烈依赖。为了解决这些问题，我们提出了一种层重要性引导的双可扩展提示池（LDEPrompt），它能够实现自适应层选择以及提示池的动态冻结与扩展。在广泛使用的类增量学习基准测试上的大量实验表明，LDEPrompt 达到了最先进的性能，验证了其有效性和可扩展性。

## Abstract
Prompt-based class-incremental learning methods typically construct a prompt pool consisting of multiple trainable key-prompts and perform instance-level matching to select the most suitable prompt embeddings, which has shown promising results. However, existing approaches face several limitations, including fixed prompt pools, manual selection of prompt embeddings, and strong reliance on the pretrained backbone for prompt selection. To address these issues, we propose a \textbf{L}ayer-importance guided \textbf{D}ual \textbf{E}xpandable \textbf{P}rompt Pool (\textbf{LDEPrompt}), which enables adaptive layer selection as well as dynamic freezing and expansion of the prompt pool. Extensive experiments on widely used class-incremental learning benchmarks demonstrate that LDEPrompt achieves state-of-the-art performance, validating its effectiveness and scalability.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LDEPrompt** 的新型类增量学习（Class-Incremental Learning, CIL）框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在基于预训练模型（PTM）的类增量学习中，现有的“提示词（Prompt）”方法虽然通过冻结骨干网络并引入少量可训练参数取得了进展，但仍存在三大局限：
*   **提示池固定：** 预设的提示池容量有限，难以应对不断增加的新任务。
*   **覆盖风险：** 跨任务重复选择的提示词容易被新知识覆盖，导致灾难性遗忘。
*   **选择机制僵化：** 提示词的选择过度依赖冻结的骨干网络特征，缺乏任务特定信息，且插入层数通常由人工预设。

### 2. 论文提出的方法论
LDEPrompt 提出了**层重要性引导**和**双可扩展提示池**机制，核心技术细节如下：
*   **层重要性评估（Layer Importance Evaluation）：** 在训练新任务前，通过计算各层的**信息增益（Information Gain, IG）**来评估其重要性。公式定义为：$IG(l) = I(x; h_l) - I(x; h_{l-1})$（即层输入与输出之间的互信息差）。通过 Softmax 归一化后，仅在得分高于平均值的层插入提示池，实现自适应层选择。
*   **双可扩展提示池（Dual Expandable Prompt Pools）：**
    *   **全局提示池（Global Pool, $P_g$）：** 存储所有已学任务的提示词并将其冻结，以保留旧知识。
    *   **训练提示池（Training Pool, $P_c$）：** 具有固定容量，用于当前任务学习。
*   **动态扩展与共享机制：** 训练时，利用 [CLS] 标记从全局池中检索最相似的提示词填入训练池（实现知识共享），剩余位置随机初始化（学习新知识）。训练结束后，新学习的提示词被合并回全局池，实现动态扩展。
*   **优化目标：** 使用标准的交叉熵损失函数对训练池中的提示词和分类器进行联合优化。

### 3. 实验设计
*   **数据集：** 使用了三个主流基准数据集：**CIFAR100**（100类）、**CUB**（200类细粒度分类）和 **VTAB**（50类跨领域任务）。
*   **实验设置：** 采用“B0-IncN”设置（即从0开始，每阶段增加N类），评估指标包括平均准确率（Avg）和最终阶段准确率（Last）。
*   **对比方法：** 对比了包括 L2P、DualPrompt、CODA-Prompt、Aper-VPT、Evoprompt、Cprompt 和 Convprompt 在内的多种最先进（SOTA）方法。

### 4. 资源与算力
*   **硬件：** 使用了两块 **NVIDIA GeForce RTX 3090 GPU**。
*   **软件框架：** 基于 PyTorch 实现，并使用了 PILOT 持续学习工具箱。
*   **训练细节：** 骨干网络为 ViT-B/16-IN21K；每个任务训练 20 个 epoch；Batch Size 为 24；学习率 0.001。
*   **时长：** 文中未明确提及总训练时长，但从 epoch 数和模型规模来看，属于轻量级微调范畴。

### 5. 实验数量与充分性
*   **实验规模：** 论文在三个不同性质的数据集上进行了测试，并使用了三个随机种子（1991, 1993, 1995）取平均值，以确保结果的统计显著性。
*   **消融实验：** 针对双池设计、冻结机制、扩展机制以及层选择策略分别进行了对比实验（共4组变体），验证了各组件的有效性。
*   **客观性：** 实验遵循了学术界通用的 exemplar-free（无样本保留）设定，对比方法均为近年顶会发表的 SOTA 方案，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先：** LDEPrompt 在所有数据集上均超越了现有方法。在 CIFAR100、CUB 和 VTAB 上，最终准确率分别比次优方法高出 **0.50%、2.91% 和 1.37%**。
*   **层选择的重要性：** 实验证明，并非在所有层插入提示词效果最好，通过信息增益选择特定层不仅能减少参数冗余，还能提升适配效果。
*   **知识保留：** 双池结构有效地平衡了新知识的获取（扩展）与旧知识的保护（冻结）。

### 7. 优点
*   **自适应性强：** 摆脱了人工指定提示词插入位置的局限，能够根据任务特性自动选择最优层。
*   **扩展性良好：** 动态扩展的全局池解决了固定容量提示池在长序列任务中的瓶颈问题。
*   **参数高效：** 继承了 Prompt Tuning 的优点，仅需微调极少量参数即可适配大规模预训练模型。

### 8. 不足与局限
*   **计算开销：** 在训练前需要进行多次前向传播来计算互信息（层重要性），这会增加额外的预处理时间。
*   **存储增长：** 随着任务数量增加，全局提示池会不断膨胀。虽然提示词本身很小，但在极端超多任务场景下的存储管理未被深入讨论。
*   **互信息计算：** 互信息的准确估计在深度学习中通常较为复杂且耗时，文中对具体计算实现的细节描述略显简略。

（完）
