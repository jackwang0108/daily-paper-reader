---
title: Improving Sparse Memory Finetuning
title_zh: 改进稀疏记忆微调
authors: "Satyam Goyal, Anirudh Kanchi, Garv Shah, Prakhar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05248v1"
tags: ["query:q3"]
score: 9.0
evidence: 通过稀疏记忆减轻大语言模型中的灾难性遗忘
tldr: 针对大语言模型在持续学习中面临的灾难性遗忘问题，本文提出了一种改进的稀疏记忆微调（SMF）方法。研究者开发了开源流水线，将 Qwen-2.5-0.5B 模型改造为带有稀疏记忆模块的架构，并引入基于 KL 散度的插槽选择机制，优先更新信息量大的“惊奇”标记。实验证明，该方法能在获取新知识的同时有效保留原有能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05248v1/fig-001.webp\", \"caption\": \"Figure 2: Recovery phase training loss on the instruction dataset (using TF-IDF).\", \"page\": 4, \"index\": 1, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-002.webp\", \"caption\": \"Figure 3: Sparse update mechanism: only retrieved key/value entries receive gradients.\", \"page\": 4, \"index\": 2, \"width\": 582, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-003.webp\", \"caption\": \"Figure 4: Finetuning performance across datasets. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse memory variants.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-004.webp\", \"caption\": \"Figure 5: Finetuning performance when adapting to SimpleQA. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse finetuning variants.\", \"page\": 6, \"index\": 4, \"width\": 826, \"height\": 229}]"
motivation: 传统的全量微调或 LoRA 方法因修改共享密集表示而导致严重的灾难性遗忘，难以实现高效的持续知识更新。
method: 通过开源流水线为预训练模型添加稀疏记忆模块，并利用基于 KL 散度的理论机制筛选信息量大的 token 进行针对性记忆更新。
result: 实验表明，改造后的模型在消费级硬件上能有效习得新事实知识，且对已有能力的干扰极小。
conclusion: 验证了稀疏更新假设在实际应用中的有效性，为大模型在保持原有能力的前提下进行动态知识扩展提供了可行方案。
---

## 摘要
大语言模型（LLMs）在训练后通常是静态的，但实际应用要求模型能够持续适应新知识，且不降低现有能力。标准的模型更新方法，如全量微调或参数高效微调方法（如 LoRA），面临着一个根本性的权衡：灾难性遗忘。这些方法修改了共享的密集表示，从而导致不同任务之间的干扰。稀疏记忆微调（SMF）提供了一种极具前景的替代方案，它通过将更新定位到显式记忆层中的一小部分参数子集来实现。在本研究中，我们提出了一个开源流水线，用于为现有的预训练模型（Qwen-2.5-0.5B）改造稀疏记忆模块，从而在消费级硬件上实现有效的持续学习。我们通过引入一种基于库尔贝克-莱布勒（KL）散度的、具有理论依据的插槽选择机制扩展了前人的工作，该机制会优先针对相对于背景分布而言具有信息“惊奇性”的标记进行记忆更新。我们的实验表明，改造后的模型能够获取新的事实性知识，同时对保留能力的遗忘极小，在实际场景中验证了稀疏更新假设。

## Abstract
Large Language Models (LLMs) are typically static after training, yet real-world applications require continual adaptation to new knowledge without degrading existing capabilities. Standard approaches to updating models, like full finetuning or parameter-efficient methods (e.g., LoRA), face a fundamental trade-off: catastrophic forgetting. They modify shared dense representations, causing interference across tasks. Sparse Memory Finetuning (SMF) offers a promising alternative by localizing updates to a small subset of parameters in explicit memory layers. In this work, we present an open-source pipeline to retrofit existing pretrained models (Qwen-2.5-0.5B) with sparse memory modules, enabling effective continual learning on consumer hardware. We extend prior work by introducing a theoretically grounded slot-selection mechanism based on Kullback-Leibler (KL) divergence, which prioritizes memory updates for informationally "surprising" tokens relative to a background distribution. Our experiments demonstrate that our retrofitted models can acquire new factual knowledge with minimal forgetting of held-out capabilities, validating the sparse update hypothesis in a practical setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Sparse Memory Finetuning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在部署后通常是静态的，但现实世界的信息是动态变化的。传统的更新方法（如全量微调或 LoRA）存在**灾难性遗忘**问题，因为它们修改了全局共享的密集表示，导致新旧知识相互干扰。
本文旨在解决如何在不破坏模型原有能力的前提下，高效地为模型注入新知识。研究基于**稀疏记忆微调（SMF）**假设，即通过将更新限制在显式记忆层的极小参数子集中，可以最小化任务间的干扰。

### 2. 方法论：核心思想与关键技术
论文提出了一套将标准 Transformer 改造为稀疏记忆架构的流程，并引入了新的插槽选择机制：
*   **核心思想**：将 Transformer 中的部分前馈网络（FFN）替换为**稀疏键值记忆层（Sparse Memory Layers）**。该层通过 Query 与 Key 的匹配，仅激活并更新前 $k$ 个最相关的 Value 插槽。
*   **三阶段改造流水线**：
    1.  **改造（Retrofitting）**：移除 Qwen-2.5-0.5B 的部分 FFN 层（第 8, 12, 16 层），替换为随机初始化的记忆模块。
    2.  **恢复（Healing）**：在通用指令数据集（OpenAssistant）上微调新模块，使模型恢复基本的语言生成能力。
    3.  **任务特定微调**：在目标任务上进行稀疏更新。
*   **关键技术：KL 散度插槽选择**：
    *   传统方法使用 TF-IDF 筛选要更新的插槽。
    *   本文提出基于 **KL 散度** 的评分规则：计算当前批次的插槽使用分布与背景分布之间的差异，优先更新那些表现出“信息惊奇性”（即在当前任务中被异常频繁使用）的插槽，从而保护通用知识插槽不被修改。

### 3. 实验设计
*   **模型与数据集**：以 **Qwen-2.5-0.5B-Instruct** 为基础模型。
*   **目标任务（新知识获取）**：TriviaQA（事实问答）和 SimpleQA（文档级问答）。
*   **保留任务（遗忘测试）**：GSM8K（数学推理）和 Natural Questions（通用常识）。
*   **对比基准（Baselines）**：
    1.  **全量密集微调（Full Finetuning）**：更新所有骨干参数。
    2.  **TF-IDF 稀疏微调**：基于词频-逆文档频率启发式规则选择更新插槽。
    3.  **KL 散度稀疏微调**：本文提出的改进方法。

### 4. 资源与算力
*   论文提到该方法旨在使持续学习能在**消费级硬件**上运行。
*   **具体细节**：文中未明确给出具体的 GPU 型号、数量或总训练时长。但提到恢复阶段使用了 20,000 条样本，目标任务微调使用了 1,000 条样本，属于轻量级微调范畴。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同的目标任务（TriviaQA, SimpleQA）分别进行了完整的对比实验，并监控了两个不同维度的保留任务指标。
*   **充分性评价**：实验设计较为合理，涵盖了“塑性”（学习新知识的能力）和“稳定性”（保留旧知识的能力）的权衡分析。通过消融实验对比了 TF-IDF 和 KL 散度两种策略。
*   **局限性**：实验主要集中在 0.5B 的小模型上，尚未在大规模模型（如 7B 或 70B）上验证该机制的扩展性。

### 6. 主要结论与发现
*   **稀疏更新的优越性**：相比全量微调，稀疏记忆微调在获取新知识时速度更快，且对 GSM8K 等推理能力的破坏显著更小。
*   **全量微调的弊端**：全量微调在处理分布偏移的数据时，会导致推理能力（GSM8K 损失值上升）的严重退化。
*   **KL 机制的有效性**：KL 散度评分在 SimpleQA 等任务中表现出更好的稳定性，能有效防止噪声更新导致的模型漂移。

### 7. 优点（亮点）
*   **开源贡献**：提供了一套可复现的、针对现有开源模型（Open-weights Transformers）的改造流水线。
*   **理论支撑**：将插槽选择从简单的启发式（TF-IDF）提升到了信息论层面（KL 散度），使“哪些参数该更新”有了更科学的依据。
*   **实用性强**：证明了在极小数据量（1k 样本）和低算力需求下实现知识更新的可行性。

### 8. 不足与局限
*   **模型规模限制**：仅在 0.5B 模型上验证，缺乏对更大参数量模型的泛化证据。
*   **性能损失**：改造初期的“恢复阶段”虽然能找回能力，但相比原始密集模型，改造后的模型在某些基准上可能仍存在微小的性能缺口。
*   **超参数敏感性**：KL 散度中的背景分布计算和插槽选择数量 $T$ 可能需要针对不同任务进行精细调优。

（完）
