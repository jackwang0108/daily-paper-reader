---
title: "LDEPrompt: Layer-importance guided Dual Expandable Prompt Pool for Pre-trained Model-based Class-Incremental Learning"
title_zh: LDEPrompt：面向基于预训练模型的类增量学习的层重要性引导双可扩展提示池
authors: "Linjie Li, Zhenyu Wu, Huiyu Xiao, Yang Ji"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11091v1"
tags: ["query:q5"]
score: 10.0
evidence: 通过双可扩展提示池解决了基于提示的类增量学习中的局限性。
tldr: 针对类增量学习中现有Prompt方法池固定、依赖手动选择及过度依赖预训练骨干网络等局限，本文提出LDEPrompt。该方法引入层重要性引导的双可扩展Prompt池，支持自适应层选择以及Prompt池的动态冻结与扩展。实验证明，LDEPrompt在多个基准测试中达到了SOTA性能，展现了极佳的有效性和可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11091v1/fig-001.webp\", \"caption\": \"Fig. 1: Illustration of LDEPrompt. The upper part (from left to right) shows: (1) the forward-pass evaluation of layer importance before training; (2) task-specific prompt retrieval from the global prompt pool at layer K using the CLS token; and (3) the construction of a layer-specific prompt pool of size n, where selected prompts are frozen while newly initialized prompts remain trainable;(4) During training, prompts within each layer are merged to form the final task-specific representations.\", \"page\": 2, \"index\": 1, \"width\": 846, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.11091v1/fig-002.webp\", \"caption\": \"Table 1: Average and final accuracy comparison across three datasets. Bold numbers indicate the best performance, while underlined values represent the second-best. All methods are evaluated under the exemplar-free setting.\", \"page\": 3, \"index\": 2, \"width\": 952, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.11091v1/fig-003.webp\", \"caption\": \"Fig. 2: Performance curve of various methods under different settings.\", \"page\": 4, \"index\": 3, \"width\": 1054, \"height\": 277}]"
motivation: 现有的基于Prompt的增量学习方法存在Prompt池固定、依赖手动选择以及过度依赖预训练骨干网络进行Prompt匹配等局限性。
method: 提出LDEPrompt，通过层重要性引导实现自适应层选择，并采用双可扩展Prompt池机制进行动态冻结与扩展。
result: 在广泛使用的类增量学习基准测试中，LDEPrompt取得了当前最先进（SOTA）的性能表现。
conclusion: LDEPrompt有效解决了Prompt池灵活性不足的问题，为基于预训练模型的类增量学习提供了一种高效且可扩展的方案。
---

## 摘要
基于提示（Prompt）的类增量学习方法通常构建一个由多个可训练关键提示组成的提示池，并通过实例级匹配来选择最合适的提示嵌入，这类方法已取得了显著成效。然而，现有方法仍存在一些局限性，包括提示池固定、提示嵌入需人工选择，以及在提示选择上对预训练骨干网络的高度依赖。为了解决这些问题，我们提出了一种层重要性引导的双可扩展提示池（LDEPrompt），该方法支持自适应层选择以及提示池的动态冻结与扩展。在广泛使用的类增量学习基准数据集上进行的深入实验表明，LDEPrompt 达到了最先进的性能，验证了其有效性和可扩展性。

## Abstract
Prompt-based class-incremental learning methods typically construct a prompt pool consisting of multiple trainable key-prompts and perform instance-level matching to select the most suitable prompt embeddings, which has shown promising results. However, existing approaches face several limitations, including fixed prompt pools, manual selection of prompt embeddings, and strong reliance on the pretrained backbone for prompt selection. To address these issues, we propose a \textbf{L}ayer-importance guided \textbf{D}ual \textbf{E}xpandable \textbf{P}rompt Pool (\textbf{LDEPrompt}), which enables adaptive layer selection as well as dynamic freezing and expansion of the prompt pool. Extensive experiments on widely used class-incremental learning benchmarks demonstrate that LDEPrompt achieves state-of-the-art performance, validating its effectiveness and scalability.

---

## 论文详细总结（自动生成）

这是一份关于论文《LDEPrompt: Layer-importance guided Dual Expandable Prompt Pool for Pre-trained Model-based Class-Incremental Learning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：在类增量学习（CIL）中，基于预训练模型（PTM）的 Prompt 方法通过冻结骨干网络并引入少量可训练参数，有效缓解了灾难性遗忘。
*   **核心问题**：现有方法存在三大局限：
    1.  **提示池固定**：容量有限，难以应对不断增加的新任务。
    2.  **覆盖风险**：跨任务重复选择的 Prompt 容易被新知识覆盖，导致旧知识丢失。
    3.  **骨干网络依赖**：Prompt 选择仅依赖预训练的静态嵌入，缺乏任务特定信息，且插入层数通常靠人工经验设定。

### 2. 方法论：LDEPrompt 核心架构
LDEPrompt 提出了一种动态、自适应的 Prompt 机制，主要包含以下关键技术：
*   **层重要性评估（Layer Importance Evaluation）**：
    *   在训练新任务前，通过计算各层的**信息增益（Information Gain, IG）**来评估其重要性。
    *   公式：$IG(l) = I(x; h_l) - I(x; h_{l-1})$，其中 $I$ 代表互信息。
    *   通过 Softmax 归一化得分，仅在得分高于平均值的层插入 Prompt 池，实现自适应层选择。
*   **双可扩展提示池（Dual Expandable Prompt Pools）**：
    *   **全局池（Global Pool, $P_g$）**：存储所有已学任务的 Prompt，保持冻结以固化知识。
    *   **训练池（Training Pool, $P_c$）**：具有固定容量 $S$。
    *   **交互机制**：新任务开始时，通过余弦相似度从全局池检索最相关的 $s$ 个 Prompt（冻结），并随机初始化 $S-s$ 个新 Prompt（可训练）。任务结束后，新 Prompt 合并入全局池，实现动态扩展。
*   **优化目标**：使用标准的交叉熵损失函数，在冻结骨干网络的情况下，仅优化训练池中的 Prompt 和分类头。

### 3. 实验设计与 Benchmark
*   **数据集**：CIFAR100（100类）、CUB（200类鸟类细粒度分类）、VTAB（50类通用视觉任务）。
*   **实验设置**：采用“B0-IncN”设置（初始0类，后续每阶段增加 N 类），评估指标为平均准确率（Avg）和最终准确率（Last）。
*   **对比方法**：对比了 8 种 SOTA 方法，包括 L2P、DualPrompt、CODA-Prompt、Aper-VPT、Evoprompt、Cprompt 和 Convprompt。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   骨干网络：ViT-B/16-IN21K。
    *   优化器：SGD，学习率 0.001，权重衰减 0.005。
    *   训练时长：每个任务训练 **20 个 epoch**，batch size 为 24。
*   **软件框架**：基于 PyTorch 和 PILOT 持续学习工具箱实现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流基准数据集上进行了全流程对比。
    *   **随机种子**：为了保证客观性，在主实验中使用了 3 个不同的随机种子（1991, 1993, 1995）取平均值。
    *   **消融实验**：设计了 4 组对比实验，分别验证了双池设计、冻结机制、扩展机制以及层选择策略的有效性。
*   **评价**：实验设计遵循了 CIL 领域的标准协议，对比方法涵盖了近年来的主流 Prompt 方案，实验结果具有较高的说服力和公平性。

### 6. 主要结论与发现
*   **性能领先**：LDEPrompt 在所有数据集上均显著超越 SOTA。在 CUB 数据集上，最终准确率比次优方法高出 **2.91%**；在 VTAB 上高出 **1.37%**。
*   **层选择的重要性**：消融实验证明，在所有层插入 Prompt（冗余）的效果反而不如基于重要性评估的自适应选择。
*   **知识保留**：双池设计有效地平衡了新知识的获取（扩展）与旧知识的保护（冻结）。

### 7. 优点与亮点
*   **自适应性强**：不再依赖人工指定 Prompt 插入的位置，而是根据数据流的信息增益动态决定。
*   **可扩展性好**：通过全局池的动态增长，解决了固定容量 Prompt 池在长序列任务中的瓶颈问题。
*   **参数高效**：仅在必要层添加 Prompt，避免了不必要的参数冗余。

### 8. 不足与局限
*   **计算开销**：在每个新任务训练前需要进行多次前向传播来评估层重要性，这会增加额外的预处理时间。
*   **存储增长**：虽然 Prompt 本身很轻量，但随着任务数量极度增加，全局池的线性增长在极端长寿命学习场景下可能仍面临存储压力。
*   **应用限制**：目前主要针对图像分类任务，尚未在目标检测或多模态任务中验证其通用性。

（完）
