---
title: "BID-LoRA: A Parameter-Efficient Framework for Continual Learning and Unlearning"
title_zh: BID-LoRA：一种用于持续学习与遗忘的高效参数框架
authors: "Jagadeesh Rachapudi, Ritali Vatsi, Praful Hambarde, Amit Shukla"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12686v1"
tags: ["query:q1"]
score: 9.0
evidence: 持续学习与机器取消学习的统一框架。
tldr: "针对深度学习中持续学习（CL）与机器遗忘（MU）结合时出现的知识泄露问题，本文提出了CLU统一范式，并开发了BID-LoRA框架。该框架通过在注意力层引入保留、新增和遗忘三个专用适配器路径，结合逃逸遗忘策略，仅需更新5%的参数即可实现精准知识删除与高效新知识集成，在CIFAR-100和人脸识别任务中表现优异。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12686v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of CLU. The CLU system removes unwanted knowledge (red), retains prior knowledge (green), and integrates new knowledge (blue).\", \"page\": 1, \"index\": 1, \"width\": 540, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2604.12686v1/fig-002.webp\", \"caption\": \"TABLE I PERFORMANCE COMPARISON ACROSS ALL CONTINUAL LEARNING-UNLEARNING TASKS ON CIFAR-100.\", \"page\": 6, \"index\": 2, \"width\": 1076, \"height\": 639}, {\"url\": \"assets/figures/arxiv/2604.12686v1/fig-003.webp\", \"caption\": \"TABLE II PERFORMANCE COMPARISON ACROSS ALL CONTINUAL LEARNING-UNLEARNING TASKS ON FACE RECOGNISTION TASKS ON CASIA-FACE100\", \"page\": 6, \"index\": 3, \"width\": 1076, \"height\": 640}, {\"url\": \"assets/figures/arxiv/2604.12686v1/fig-004.webp\", \"caption\": \"Fig. 2. LoRA placement in BID-LoRA at Attention Modules\", \"page\": 4, \"index\": 4, \"width\": 540, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.12686v1/fig-005.webp\", \"caption\": \"Fig. 6. Geometric verification of unlearning. Top row: t-SNE visualization showing forget classes migrating toward escape point d∗. Bottom row: 3D hypersphere visualization with dashed antipodal axis from c̄r to d∗\", \"page\": 9, \"index\": 5, \"width\": 539, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.12686v1/fig-006.webp\", \"caption\": \"Fig. 3. Pathway separation for BID-LoRA.\", \"page\": 5, \"index\": 6, \"width\": 526, \"height\": 272}]"
motivation: 现有的持续学习与机器遗忘方法简单叠加会导致基础知识随循环逐渐退化的“知识泄露”问题。
method: 提出BID-LoRA框架，利用三向适配器路径配合逃逸遗忘机制，将待遗忘类别的嵌入推离保留知识区域。
result: 在CIFAR-100和CASIA-Face100数据集上，该方法在多个适配周期中均优于基准模型，且仅需更新极少量的参数。
conclusion: BID-LoRA为持续学习与遗忘的统一处理提供了高效且参数节省的解决方案，特别适用于需要频繁更新和删除用户信息的实际场景。
---

## 摘要
深度学习的最新进展强调了对系统的需求，这些系统不仅能够通过持续学习（CL）获取新知识，还能通过机器遗忘（MU）删除过时、敏感或私密的信息。然而，尽管 CL 方法已相对成熟，MU 技术仍处于早期阶段，这导致依赖这两种能力的统一框架存在关键空白。我们发现，简单地结合现有的 CL 和 MU 方法会导致知识泄漏，即基础知识在重复的适应周期中逐渐退化。为了解决这一问题，我们将持续学习遗忘（CLU）形式化为一个统一范式，包含三个关键目标：(i) 精确删除不需要的知识；(ii) 在保留先前信息的同时高效整合新知识；(iii) 最大程度减少跨周期的知识泄漏。我们提出了双向低秩自适应（BID-LoRA），这是一种新颖的框架，在注意力层中采用了三个专用的自适应器路径（保留、新增和遗忘），并结合了“逃逸遗忘”机制，将遗忘类别的嵌入推向与保留知识距离最远的位置，仅需更新 5% 的参数。在 CIFAR-100 上的实验表明，BID-LoRA 在多个适应周期中均优于 CLU 基准方法。我们进一步在精选的人脸识别数据集 CASIA-Face100 上进行了评估，展示了其在现实世界身份管理系统中的实际适用性，即需要注册新用户并移除注销用户。

## Abstract
Recent advances in deep learning underscore the need for systems that can not only acquire new knowledge through Continual Learning (CL) but also remove outdated, sensitive, or private information through Machine Unlearning (MU). However, while CL methods are well-developed, MU techniques remain in early stages, creating a critical gap for unified frameworks that depend on both capabilities. We find that naively combining existing CL and MU approaches results in knowledge leakage a gradual degradation of foundational knowledge across repeated adaptation cycles. To address this, we formalize Continual Learning Unlearning (CLU) as a unified paradigm with three key goals: (i) precise deletion of unwanted knowledge, (ii) efficient integration of new knowledge while preserving prior information, and (iii) minimizing knowledge leakage across cycles. We propose Bi-Directional Low-Rank Adaptation (BID-LoRA), a novel framework featuring three dedicated adapter pathways-retain, new, and unlearn applied to attention layers, combined with escape unlearning that pushes forget-class embeddings to positions maximally distant from retained knowledge, updating only 5% of parameters. Experiments on CIFAR-100 show that BID-LoRA outperforms CLU baselines across multiple adaptation cycles. We further evaluate on CASIA-Face100, a curated face recognition subset, demonstrating practical applicability to real-world identity management systems where new users must be enrolled and withdrawn users removed.

---

## 论文详细总结（自动生成）

这是一份关于论文《BID-LoRA: A Parameter-Efficient Framework for Continual Learning and Unlearning》的结构化深入分析总结：

### 1. 核心问题与研究背景
*   **研究动机**：在实际应用（如身份管理、隐私保护合规）中，AI模型需要具备“动态调整”能力：既要通过**持续学习（CL）**获取新知识，又要通过**机器遗忘（MU）**删除过时或敏感信息。
*   **核心挑战**：
    1.  **知识泄露（Knowledge Leakage）**：简单地将现有的CL和MU方法叠加，会导致模型基础能力在多次“学习-遗忘”循环中逐渐退化。
    2.  **灾难性遗忘**：在学习新任务时丢失旧任务的性能。
    3.  **参数效率**：全参数微调在大模型时代计算成本过高，且容易破坏预训练的特征表示。

### 2. 方法论：BID-LoRA 框架
*   **核心思想**：提出**双向低秩自适应（Bi-Directional LoRA）**，通过在Transformer的注意力层引入“路径分离”机制，将保留、新增和遗忘三个目标的梯度更新隔离开。
*   **关键技术细节**：
    *   **三路径分离（Pathway Separation）**：为每个线性层设计三个独立的LoRA适配器：$W_{ret}$（保留）、$W_{new}$（新增）和 $W_{f}$（遗忘）。这种设计避免了不同目标之间的梯度干扰。
    *   **逃逸遗忘（Escape Unlearning）**：这是一种被动遗忘策略。系统首先计算所有保留类别的质心，然后通过极小极大优化（Minimax）找到一个与这些质心距离最远的“逃逸方向” $d^*$。
    *   **损失函数设计**：
        *   **遗忘损失**：使用均方误差（MSE）将遗忘类的嵌入推向缩放后的逃逸点，从而彻底破坏其类别判别性。
        *   **保留损失**：结合交叉熵和嵌入锚定（Embedding Anchor），防止模型表示向逃逸点漂移。
        *   **新增损失**：标准的交叉熵损失。
    *   **参数效率**：仅更新约 **5.08%** 的参数，保持主干网络（Backbone）冻结。

### 3. 实验设计
*   **数据集**：
    1.  **CIFAR-100**：用于通用的图像分类评估。
    2.  **CASIA-Face100**：从CASIA-WebFace中提取的子集，模拟现实世界中人脸识别系统的身份注销与注册场景。
*   **评估协议（Benchmark）**：采用**六阶段滑动窗口协议**。每个阶段涉及：保留20个类、遗忘10个旧类、学习10个新类。这种长序列测试旨在验证模型在长期适应过程中的稳定性。
*   **对比方法**：包括 LSF、CLPU-DER++、UniCLUN、UnCLe 和 UG-CLU 等最新的统一 CLU 框架。
*   **评估指标**：遗忘准确率（Acc f）、保留准确率（Acc r）、新增准确率（Acc n）、整体准确率（Acc o）、KL散度（衡量与Oracle模型的相似度）以及成员推理攻击（MIA）成功率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练总时长。
*   **效率体现**：文中强调了参数效率，指出 BID-LoRA 仅需更新约 5% 的参数，相比于基准方法（通常需要 100% 更新）在资源受限的部署场景中具有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同领域的数据集上进行了完整的 6 阶段循环实验。
    *   **消融实验**非常丰富：涵盖了 LoRA 秩（Rank）的影响、路径分离的有效性验证、保留数据 Buffer 比例的影响、逃逸缩放因子 $\lambda_{esc}$ 的调节，以及针对遗忘效果的几何验证（t-SNE 和 3D 球面可视化）。
*   **客观性与公平性**：引入了 **Oracle 模型**（在目标类上直接训练的最优模型）作为性能上限，并使用了 MIA 攻击来验证隐私保护的真实效果，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能领先**：BID-LoRA 在几乎所有任务阶段的整体准确率（Acc o）上均达到第一或第二，且 KL 散度最低，说明其最接近 Oracle 模型的表现。
*   **有效抑制泄露**：相比于基准方法随周期增加而出现的性能大幅下滑，BID-LoRA 的性能曲线非常平稳，成功解决了知识泄露问题。
*   **隐私保护**：MIA 成功率接近 0.5（理想值），证明攻击者无法分辨数据是否被模型“见过”，达到了可验证的隐私保护效果。

### 7. 优点与亮点
*   **架构创新**：三路径分离机制直击 CLU 任务中梯度冲突的痛点，设计简洁且高效。
*   **逃逸机制**：不同于传统的“增加噪声”或“负梯度”遗忘，逃逸遗忘通过几何空间优化，提供了更稳定的遗忘效果。
*   **实用性强**：极低的参数更新比例使其非常适合在边缘设备或需要频繁更新的身份管理系统中部署。

### 8. 不足与局限
*   **Buffer 依赖**：虽然比例较低，但该方法仍依赖于 10% 的保留数据缓冲区（Replay Buffer），在极端隐私敏感或完全不允许存储旧数据的场景下受限。
*   **架构局限**：实验主要基于 Vision Transformer（DeiT 和 FaceTransformer），尚未在更大规模的语言模型（LLM）或其他架构（如纯 CNN）上进行广泛验证。
*   **算力细节缺失**：缺乏具体的训练时间对比，难以直观评估其在实际工程中的收敛速度优势。

（完）
