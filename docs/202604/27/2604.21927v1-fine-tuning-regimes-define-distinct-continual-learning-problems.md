---
title: Fine-Tuning Regimes Define Distinct Continual Learning Problems
title_zh: 微调范式定义了不同的持续学习问题
authors: "Paul-Tiberiu Iordache, Elena Burceanu"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21927v1"
tags: ["query:q4"]
score: 9.0
evidence: 在任务增量持续学习中形式化了自适应机制
tldr: 本研究探讨了持续学习中微调方案（由可训练参数子空间定义）对算法表现的影响。作者将微调方案形式化为固定子空间上的投影优化，并发现改变可训练深度会显著影响任务拟合与知识保留。通过在多个数据集上测试四种主流算法，研究证明了算法的优劣排名并非跨方案一致，且深度增加会加剧遗忘。该研究强调了在评估持续学习方法时，应将微调深度作为关键的实验变量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21927v1/fig-001.webp\", \"caption\": \"Figure 2: Ranking sensitivity persists across other benchmarks. Each panel shows the mean Kendall’s τ between regime specific method rankings, averaged over 11 randomly sampled task orders. Lower τ values indicate lower agreement between regimes in the ranking of the considered continual learning algorithms: online EWC, LwF, SI, and GEM. This disagreement is consistently observed across all considered benchmarks, namely Fashion MNIST, CIFAR-100, MNIST, QMNIST, and KMNIST, indicating that the relative ordering of these algorithms depends strongly on the fine-tuning regime used throughout the continual learning run.\", \"page\": 6, \"index\": 1, \"width\": 979, \"height\": 871}, {\"url\": \"assets/figures/arxiv/2604.21927v1/fig-002.webp\", \"caption\": \"Table 1: Notation used in the theoretical analysis\", \"page\": 13, \"index\": 2, \"width\": 951, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2604.21927v1/fig-003.webp\", \"caption\": \"Figure 1: fine-tuning regime changes comparative conclusions (Fashion MNIST). (a) Standard continual learning methods exhibit different accuracy trends as the fine-tuning regime changes, with very large stds. (b) Pairwise Kendall’s τ between rankings of CL algorithms under different fine-tuning regimes shows frequent changes in their relative ordering. (c,d) Two representative task orders illustrate that ranking stability can vary substantially, from low agreement across regimes to relatively high agreement. Together, these results show that trainable depth can alter the relative ranking of CL algorithms, even within the same benchmark. Reported values are mean and std for the final accuracy metrics, for the considered CL methods (online EWC, LwF, SI, and GEM) over 11 sampled task orders.\", \"page\": 5, \"index\": 3, \"width\": 979, \"height\": 888}]"
motivation: 现有持续学习评估通常固定微调方案，忽略了可训练参数子空间（如深度）对模型性能和遗忘的影响。
method: 将微调方案形式化为固定子空间上的投影优化，并在不同可训练深度下对比了四种主流持续学习算法。
result: 实验表明算法的相对排名随微调深度变化而改变，且更深的微调层会导致更大的更新幅度和更严重的遗忘。
conclusion: 持续学习的评估应将微调方案（特别是可训练深度）视为显式实验变量，以实现更全面的算法比较。
---

## 摘要
持续学习（CL）研究模型如何在按顺序获取任务的同时保留先前学到的知识。尽管在 CL 方法的基准测试方面取得了实质性进展，但对比评估通常保持微调范式固定。在本文中，我们认为由可训练参数子空间定义的微调范式本身就是一个关键的评估变量。我们将自适应范式形式化为固定可训练子空间上的投影优化，表明改变可训练深度会改变有效更新信号，而当前任务拟合和知识保留都通过该信号运作。这一分析引出了一个假设，即方法之间的比较在不同范式下不一定是不变的。我们在任务增量持续学习、五种可训练深度范式以及四种标准方法（online EWC、LwF、SI 和 GEM）中测试了这一假设。在五个基准数据集（即 MNIST、Fashion MNIST、KMNIST、QMNIST 和 CIFAR-100）以及每个数据集的 11 种任务顺序中，我们发现方法的相对排名在不同范式之间并未得到一致保留。我们进一步表明，更深的自适应范式与更大的更新幅度、更高的遗忘率以及两者之间更强的关系相关联。这些结果表明，CL 中的对比结论可能强烈依赖于所选的微调范式，从而促使建立一种将可训练深度视为显式实验因素的范式感知评估协议。

## Abstract
Continual learning (CL) studies how models acquire tasks sequentially while retaining previously learned knowledge. Despite substantial progress in benchmarking CL methods, comparative evaluations typically keep the fine-tuning regime fixed. In this paper, we argue that the fine-tuning regime, defined by the trainable parameter subspace, is itself a key evaluation variable. We formalize adaptation regimes as projected optimization over fixed trainable subspaces, showing that changing the trainable depth alters the effective update signal through which both current task fitting and knowledge preservation operate. This analysis motivates the hypothesis that method comparisons need not be invariant across regimes. We test this hypothesis in task incremental CL, five trainable depth regimes, and four standard methods: online EWC, LwF, SI, and GEM. Across five benchmark datasets, namely MNIST, Fashion MNIST, KMNIST, QMNIST, and CIFAR-100, and across 11 task orders per dataset, we find that the relative ranking of methods is not consistently preserved across regimes. We further show that deeper adaptation regimes are associated with larger update magnitudes, higher forgetting, and a stronger relationship between the two. These results show that comparative conclusions in CL can depend strongly on the chosen fine-tuning regime, motivating regime-aware evaluation protocols that treat trainable depth as an explicit experimental factor.

---

## 论文详细总结（自动生成）

这篇论文《Fine-Tuning Regimes Define Distinct Continual Learning Problems》对持续学习（Continual Learning, CL）的评估方法提出了深刻的挑战。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在持续学习的研究中，研究者通常在固定的微调设置（通常是全参数微调）下比较不同算法的优劣。本文探讨了一个被忽视的变量——**微调范式（Fine-tuning Regime）**，即哪些参数层被设为可训练，是否会从根本上改变算法的相对排名。
*   **研究动机**：作者认为，可训练参数子空间的选择不仅改变了模型的容量，还改变了优化的几何结构。如果算法 A 在全量微调下优于算法 B，但在仅微调最后几层时却劣于算法 B，那么现有的许多 CL 评估结论可能是不稳固的。

### 2. 方法论：投影优化视角
*   **核心思想**：将微调范式形式化为在**固定子空间上的投影优化（Projected Optimization）**。
*   **关键技术细节**：
    *   **投影算子 ($P_S$)**：定义一个正交投影矩阵 $P_S$，它将梯度向量中不可训练参数对应的坐标置零。
    *   **更新公式**：模型更新遵循 $\theta^{(m+1)} = \theta^{(m)} - \eta P_S \nabla J_t(\theta^{(m)})$，其中 $J_t$ 是包含当前任务损失和知识保留项的总目标函数。
    *   **信号分解**：作者将投影梯度分解为“当前任务信号”和“知识保留信号”。通过分析这两个信号在不同子空间（不同深度）下的对齐或冲突情况，解释了为什么改变微调深度会改变优化动力学。
    *   **理论推导**：论文证明了局部优化的进展取决于投影梯度的范数，而非全梯度的范数，这为研究“深度”作为变量提供了理论支撑。

### 3. 实验设计
*   **场景**：任务增量持续学习（Task-Incremental Learning），即在训练和测试时均已知任务身份。
*   **数据集（Benchmark）**：
    *   MNIST 系列：MNIST, Fashion MNIST, KMNIST, QMNIST。
    *   自然图像：CIFAR-100。
*   **对比方法**：
    *   **Online EWC**（基于正则化）
    *   **SI (Synaptic Intelligence)**（基于正则化）
    *   **LwF (Learning without Forgetting)**（基于蒸馏）
    *   **GEM (Gradient Episodic Memory)**（基于梯度投影/记忆）
*   **微调范式变量**：基于 ResNet-18 骨干网络，设置了 5 种可训练深度：仅最后 1 个残差块、最后 2 块、3 块、6 块以及全量微调（8 块）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但考虑到实验涵盖了 5 个数据集、4 种算法、5 种深度以及每组实验 11 种任务顺序，其计算量相对较大。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 1100 次完整的持续学习序列训练（5 数据集 × 4 算法 × 5 深度 × 11 任务顺序）。
*   **充分性与公平性**：
    *   使用了 11 种不同的任务顺序来排除顺序偏差。
    *   引入了 **Kendall’s $\tau$ 相关系数**来量化不同微调范式下算法排名的相关性。
    *   实验设计非常系统，通过跨数据集的一致性表现证明了结论的普适性，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **排名不稳定性**：算法的相对排名在不同微调深度下会发生显著变化。例如，在某种深度下表现最优的算法，在另一种深度下可能变为最差。
*   **深度与遗忘的关系**：更深的微调范式通常伴随着更大的梯度更新幅度和更高的遗忘率。
*   **相关性增强**：在更深（更宽松）的微调方案中，梯度幅度与遗忘之间的相关性变得更强，说明深度增加了干扰发生的可能性。
*   **评估建议**：CL 评估不应再被视为单一维度的比较，而应将“微调深度”作为一个显式的实验因子进行报告。

### 7. 优点
*   **视角新颖**：首次系统性地指出“微调深度”是 CL 评估中的一个关键隐藏变量。
*   **理论与实证结合**：不仅通过大量实验展示现象，还通过投影优化的数学框架提供了物理解释。
*   **严谨的统计分析**：使用 Kendall’s $\tau$ 等指标量化排名变化，而非仅仅依赖准确率曲线。

### 8. 不足与局限
*   **超参数敏感性**：实验中所有深度范式共用一套超参数。作者也承认，某些算法在特定深度下表现不佳，可能是因为超参数未针对该深度进行优化。
*   **架构单一**：主要基于 ResNet-18 进行实验，尚未验证在 Transformer 或更深层网络（如 ResNet-101）中的表现。
*   **范式局限**：仅探讨了任务增量学习（Task-IL），未涉及更具挑战性的类增量学习（Class-IL）或任务不可知学习（Task-agnostic）。
*   **子空间定义较窄**：仅以“层深度”定义子空间，未探讨 LoRA、稀疏掩码等其他参数子空间定义方式。

（完）
