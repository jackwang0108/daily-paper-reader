---
title: "ImageHD: Energy-Efficient On-Device Continual Learning of Visual Representations via Hyperdimensional Computing"
title_zh: ImageHD：基于超维计算的高能效设备端视觉表示持续学习
authors: "Jebacyril Arockiaraj, Dhruv Parikh, Viktor Prasanna"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21280v1"
tags: ["query:q1"]
score: 9.0
evidence: 通过超维计算实现能效高的设备端持续学习
tldr: 针对边缘设备上持续学习面临的高计算和内存开销问题，本文提出了ImageHD，一种基于超维计算（HDC）的FPGA加速器。该系统结合了量化CNN特征提取器与硬件感知的HDC算法，通过统一样本内存和聚类合并策略实现高效的在线更新。在ZCU104 FPGA上的实验表明，ImageHD在保持高精度的同时，显著提升了处理速度和能效，为实时边缘AI提供了实用的持续学习方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21280v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF REPRESENTATIVE CONTINUAL (VISUAL) LEARNING PIPELINES FROM THE PERSPECTIVE OF on-device deployability.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.21280v1/fig-002.webp\", \"caption\": \"Fig. 5. Compute Flow of our On-Device Continual Learning Accelerator\", \"page\": 7, \"index\": 2, \"width\": 748, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2604.21280v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of ImageHD accelerator.\", \"page\": 4, \"index\": 3, \"width\": 450, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.21280v1/fig-004.webp\", \"caption\": \"Fig. 3. Hyperdimensional Encoding Unit (HEU)\", \"page\": 5, \"index\": 4, \"width\": 520, \"height\": 264}]"
motivation: 现有的持续学习方法依赖反向传播或大量样本，导致边缘设备在处理非平稳数据流时面临巨大的计算、内存和延迟挑战。
method: 提出一种硬件感知的HDC持续学习方法，集成量化CNN前端、统一样本内存管理及硬件高效的聚类合并策略，并在FPGA上实现流式数据流架构。
result: 在CORe50数据集上，ImageHD相比优化后的CPU和GPU基准，分别实现了高达40.4倍和4.84倍的加速，能效提升分别达到383倍和105.1倍。
conclusion: ImageHD证明了结合超维计算与专用硬件加速器可以在资源受限的边缘设备上实现高效、实时的视觉持续学习。
---

## 摘要
设备端持续学习（CL）对于处理非平稳数据流的边缘人工智能系统至关重要，但大多数现有方法依赖于反向传播或重样本分类器，从而导致大量的计算、内存和延迟开销。超维计算（HDC）通过快速、非迭代的在线更新提供了一种轻量级的替代方案。结合紧凑型卷积神经网络（CNN）特征提取器，HDC 能够利用强大的视觉表示实现高效的设备端自适应。然而，先前的基于 HDC 的持续学习系统通常依赖于多级存储层次结构和复杂的聚类管理，限制了其在资源受限硬件上的部署能力。我们提出了 ImageHD，这是一种基于 HDC 的视觉数据设备端持续学习 FPGA 加速器。ImageHD 针对严格延迟和片上内存约束下的流式持续学习，避免了昂贵的迭代优化。在算法层面，我们引入了一种硬件感知的持续学习方法，通过统一的样本存储器和硬件高效的聚类合并策略来限制类别样本数量，同时结合量化 CNN 前端以在不牺牲准确性的情况下降低部署开销。在系统层面，ImageHD 在 AMD Zynq ZCU104 FPGA 上实现为流式数据流架构，集成了 HDC 编码、相似度搜索和受限聚类管理，利用字打包二进制超向量在紧张的片上资源预算内实现大规模并行位运算。在 CORe50 数据集上，ImageHD 相比优化的 CPU（GPU）基准实现了高达 40.4 倍（4.84 倍）的加速和 383 倍（105.1 倍）的能效提升，证明了基于 HDC 的持续学习在实时边缘人工智能中的实用性。

## Abstract
On-device continual learning (CL) is critical for edge AI systems operating on non-stationary data streams, but most existing methods rely on backpropagation or exemplar-heavy classifiers, incurring substantial compute, memory, and latency overheads. Hyperdimensional computing (HDC) offers a lightweight alternative through fast, non-iterative online updates. Combined with a compact convolutional neural network (CNN) feature extractor, HDC enables efficient on-device adaptation with strong visual representations. However, prior HDC-based CL systems often depend on multi-tier memory hierarchies and complex cluster management, limiting deployability on resource-constrained hardware.   We present ImageHD, an FPGA accelerator for on-device continual learning of visual data based on HDC. ImageHD targets streaming CL under strict latency and on-chip memory constraints, avoiding costly iterative optimization. At the algorithmic level, we introduce a hardware-aware CL method that bounds class exemplars through a unified exemplar memory and a hardware-efficient cluster merging strategy, while incorporating a quantized CNN front-end to reduce deployment overhead without sacrificing accuracy. At the system level, ImageHD is implemented as a streaming dataflow architecture on the AMD Zynq ZCU104 FPGA, integrating HDC encoding, similarity search, and bounded cluster management using word-packed binary hypervectors for massively parallel bitwise computation within tight on-chip resource budgets. On CORe50, ImageHD achieves up to 40.4x (4.84x) speedup and 383x (105.1x) energy efficiency over optimized CPU (GPU) baselines, demonstrating the practicality of HDC-enabled continual learning for real-time edge AI.

---

## 论文详细总结（自动生成）

这是一份关于论文《ImageHD: Energy-Efficient On-Device Continual Learning of Visual Representations via Hyperdimensional Computing》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：边缘设备（如无人机、机器人、IoT传感器）需要具备“持续学习”（Continual Learning, CL）的能力，即在不依赖云端 retraining 的情况下，从非平稳数据流中增量获取知识。
*   **核心问题**：传统的持续学习方法（如基于反向传播的深度学习）计算开销巨大、内存占用高且存在“灾难性遗忘”问题。现有的超维计算（HDC）方案虽然更新快，但往往依赖复杂的内存分级和高复杂度的聚类算法（如谱聚类），难以在资源极度受限的边缘硬件（如 FPGA）上实时运行。
*   **整体含义**：本文旨在通过算法与硬件的协同设计，实现一个低延迟、高能效、内存受限的设备端视觉持续学习系统。

### 2. 方法论：核心思想与关键技术
ImageHD 采用 **CNN 特征提取 + HDC 持续学习** 的混合架构，核心技术包括：
*   **量化 CNN 前端**：使用经过 INT8 静态量化的 MobileNetV2 作为特征提取器。利用 HDC 对噪声的高容忍度，在大幅降低内存占用的同时保持了学习精度。
*   **硬件优化的聚类合并策略**：
    *   **线性复杂度合并**：放弃了计算复杂度为 $O(N^3)$ 的谱聚类，改用硬件友好的 **kMeans++** 算法，将复杂度降至 $O(N)$。
    *   **统一样本内存**：取消了复杂的短时/长时内存分级，采用单一的、容量受限的片上聚类存储器，简化了控制逻辑。
*   **流式 FPGA 架构**：
    *   **HEU（编码单元）**：将特征向量映射为 D 维二进制超向量（HV），利用位运算（XOR）和多数表决（Bundling）实现快速编码。
    *   **HLU（学习单元）**：执行流式相似度搜索（汉明距离）和在线聚类更新。
    *   **CMU（合并单元）**：周期性触发，通过硬件并行的 PE 阵列对过多的聚类进行合并，确保内存不溢出。

### 3. 实验设计
*   **数据集**：使用了三个主流的持续学习基准数据集：**CIFAR-10**、**CIFAR-100** 和 **CORe50**（专门用于物体识别的持续学习数据集）。
*   **对比基准（Baselines）**：
    *   **硬件平台**：高性能 CPU (AMD EPYC 7763)、工作站 GPU (NVIDIA RTX A4000) 以及边缘 GPU (Jetson AGX Orin)。
    *   **算法对比**：对比了谱聚类（Spectral Clustering）与本文 kMeans++ 的性能差异，并与现有的 FPGA 持续学习加速器（如 AILIS, ILOC 等）进行了对比。

### 4. 资源与算力
*   **FPGA 平台**：AMD Zynq UltraScale+ **ZCU104**（边缘级 FPGA）。
*   **资源占用**：LUT 占用 62%，FF 25%，BRAM 52%，DSP 仅用 7%（主要用于 CNN 卷积），URAM 59%。
*   **运行参数**：工作频率 **300 MHz**。
*   **对比平台算力**：
    *   RTX A4000 GPU (19.2 TFLOPS FP32)
    *   EPYC 7763 CPU (5.0 TFLOPS FP32)
    *   FPGA 峰值性能仅为 0.26 TFLOPS，但通过架构专用化实现了更高效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从算法层面的量化影响分析、聚类算法准确度对比，到系统层面的延迟、功耗、能效比测试。
*   **充分性评价**：实验设计较为充分。作者不仅验证了准确性（ACC、Purity、NMI 等指标），还重点展示了在“Batch-1”（单样本流式输入）这一极端实时场景下的表现。对比对象涵盖了从通用处理器到专用边缘模块，评估维度客观、公平。

### 6. 主要结论与发现
*   **性能飞跃**：在 CORe50 数据集上，ImageHD 比优化后的 CPU 快 **40.4 倍**，比 GPU 快 **4.84 倍**。
*   **能效极高**：能效比（Energy Efficiency）分别是 CPU 的 **383 倍**和 GPU 的 **105.1 倍**。
*   **量化无损**：实验证明 INT8 量化带来的噪声对下游 HDC 编码几乎没有负面影响，甚至在某些指标上略有提升。
*   **算法优化有效**：硬件化的 kMeans++ 相比谱聚类实现了 **8.6 倍**的算法加速，且聚类质量损失微乎其微。

### 7. 优点与亮点
*   **端到端加速**：不仅加速了 HDC 部分，还完整集成了 CNN 特征提取前端，是一个完整的系统级方案。
*   **软硬协同**：针对 FPGA 的位运算优势，重新设计了聚类合并算法，将原本难以硬件化的谱聚类替换为高效的并行 kMeans++。
*   **内存边界清晰**：通过统一样本内存设计，解决了持续学习中内存无限增长的痛点，非常适合嵌入式部署。

### 8. 不足与局限
*   **特征提取器固定**：CNN 前端是预训练且冻结（Frozen）的，这意味着系统只能在预定义的特征空间内进行聚类，无法像端到端反向传播那样学习全新的底层视觉特征。
*   **应用限制**：目前主要针对分类/聚类任务，对于更复杂的视觉任务（如目标检测或分割的持续学习）尚未涉及。
*   **精度差距**：虽然 HDC 在效率上有巨大优势，但在处理极其复杂的视觉分类任务时，其绝对准确率通常仍略低于最先进的深度学习模型。

（完）
