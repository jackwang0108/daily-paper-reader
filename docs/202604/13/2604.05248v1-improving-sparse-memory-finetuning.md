---
title: Improving Sparse Memory Finetuning
title_zh: 改进稀疏记忆微调
authors: "Satyam Goyal, Anirudh Kanchi, Garv Shah, Prakhar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05248v1"
tags: ["query:q3"]
score: 10.0
evidence: 使用稀疏记忆微调解决大语言模型中的灾难性遗忘问题。
tldr: 针对大语言模型在持续学习中面临的灾难性遗忘问题，本文提出了一种改进的稀疏记忆微调（SMF）方法。研究者开发了开源流水线，将 Qwen-2.5-0.5B 模型改造为包含稀疏记忆模块的架构，并引入基于 KL 散度的槽位选择机制，优先更新包含“惊奇”信息的 Token。实验证明，该方法能在消费级硬件上高效获取新知识，同时显著减少对既有能力的干扰。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05248v1/fig-001.webp\", \"caption\": \"Figure 2: Recovery phase training loss on the instruction dataset (using TF-IDF).\", \"page\": 4, \"index\": 1, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-002.webp\", \"caption\": \"Figure 3: Sparse update mechanism: only retrieved key/value entries receive gradients.\", \"page\": 4, \"index\": 2, \"width\": 582, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-003.webp\", \"caption\": \"Figure 4: Finetuning performance across datasets. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse memory variants.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-004.webp\", \"caption\": \"Figure 5: Finetuning performance when adapting to SimpleQA. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse finetuning variants.\", \"page\": 6, \"index\": 4, \"width\": 826, \"height\": 229}]"
motivation: 传统的全量微调或 LoRA 方法因修改共享的密集表示，容易导致严重的灾难性遗忘，难以在保留旧知识的同时吸收新信息。
method: 通过开源流水线为预训练模型植入稀疏记忆模块，并采用基于 KL 散度的理论化机制来选择信息量大的 Token 进行记忆更新。
result: 实验表明，改造后的模型能够有效习得新事实知识，且在保留原有能力方面表现优异，验证了稀疏更新在实际场景中的有效性。
conclusion: 稀疏记忆微调为在消费级硬件上实现高效、低遗忘的持续学习提供了可行方案，证明了局部化更新是解决模型遗忘问题的关键。
---

## 摘要
大语言模型（LLMs）在训练后通常是静态的，但实际应用要求模型能够持续适应新知识，且不降低现有能力。更新模型的标准方法，如全量微调或参数高效方法（如 LoRA），面临着一个根本性的权衡：灾难性遗忘。这些方法修改了共享的密集表示，从而导致任务间的干扰。稀疏记忆微调（SMF）通过将更新定位到显式记忆层中的一小部分参数子集，提供了一种极具前景的替代方案。在本研究中，我们提出了一个开源流水线，用于为现有的预训练模型（Qwen-2.5-0.5B）改造稀疏记忆模块，从而在消费级硬件上实现有效的持续学习。我们通过引入一种基于 Kullback-Leibler (KL) 散度的、具有理论依据的插槽选择机制扩展了前人的工作，该机制优先为相对于背景分布而言具有信息“惊奇性”的标记（tokens）进行记忆更新。我们的实验表明，改造后的模型能够获取新的事实知识，同时对预留能力的遗忘极小，在实际场景中验证了稀疏更新假设。

## Abstract
Large Language Models (LLMs) are typically static after training, yet real-world applications require continual adaptation to new knowledge without degrading existing capabilities. Standard approaches to updating models, like full finetuning or parameter-efficient methods (e.g., LoRA), face a fundamental trade-off: catastrophic forgetting. They modify shared dense representations, causing interference across tasks. Sparse Memory Finetuning (SMF) offers a promising alternative by localizing updates to a small subset of parameters in explicit memory layers. In this work, we present an open-source pipeline to retrofit existing pretrained models (Qwen-2.5-0.5B) with sparse memory modules, enabling effective continual learning on consumer hardware. We extend prior work by introducing a theoretically grounded slot-selection mechanism based on Kullback-Leibler (KL) divergence, which prioritizes memory updates for informationally "surprising" tokens relative to a background distribution. Our experiments demonstrate that our retrofitted models can acquire new factual knowledge with minimal forgetting of held-out capabilities, validating the sparse update hypothesis in a practical setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Sparse Memory Finetuning》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：大语言模型（LLMs）在部署后需要持续学习新知识，但传统的微调方法（如全量微调或 LoRA）会修改共享的密集表示，导致**灾难性遗忘**（Catastrophic Forgetting），即学习新知识时会破坏旧知识。
*   **研究背景**：现有的参数高效微调（PEFT）虽然减少了参数量，但更新仍是全局性的。稀疏记忆微调（SMF）通过将更新局限在显式的记忆层中来减少干扰，但此前的研究多依赖于简单的启发式规则（如 TF-IDF），且缺乏针对开源模型的通用改造流程。

### 2. 方法论：核心思想与技术细节
论文提出了一种将标准 Transformer 改造为支持稀疏记忆更新的架构，核心包含以下三个方面：
*   **三阶段改造流水线**：
    1.  **改造（Retrofitting）**：将预训练模型（如 Qwen-2.5-0.5B）中特定层的密集前馈网络（FFN）替换为稀疏记忆层（包含 Key-Value 查找机制）。
    2.  **恢复（Healing）**：在通用指令数据集上微调新加入的记忆参数，使模型恢复基础的语言生成能力。
    3.  **任务特定微调**：在目标任务上进行稀疏更新。
*   **稀疏记忆层架构**：使用查询投影器将输入映射为查询 $q$，通过 Top-k 机制从记忆库中检索最相关的 $k$ 个插槽（Slots），输出为检索值的加权和。
*   **基于 KL 散度的插槽选择机制**：
    *   这是本文的核心创新。作者认为应该更新那些相对于背景分布而言具有“惊奇性”（Surprising）的 Token。
    *   通过计算当前批次的插槽使用分布与背景分布之间的 **KL 散度贡献度** 来为插槽评分，优先更新评分高的插槽，从而保护存储通用知识的插槽不被覆盖。

### 3. 实验设计
*   **基础模型**：Qwen-2.5-0.5B-Instruct。
*   **数据集**：
    *   **恢复阶段**：OpenAssistant (oasst1)。
    *   **目标任务（新知识学习）**：TriviaQA、SimpleQA。
    *   **保留能力评估（遗忘测试）**：GSM8K（数学推理）、Natural Questions（通用问答）。
*   **对比方法（Baselines）**：
    1.  **全量密集微调（Full Finetuning）**：更新所有权重。
    2.  **基于 TF-IDF 的稀疏微调**：使用传统的词频-逆文档频率启发式规则选择更新插槽。
    3.  **基于 KL 散度的稀疏微调**：本文提出的改进方法。

### 4. 资源与算力
*   **硬件环境**：论文明确提到该流水线旨在使持续学习能在**消费级硬件**上运行。
*   **具体细节**：文中未明确给出具体的 GPU 型号、数量或总训练时长，但提到恢复阶段使用了 20,000 个样本，目标任务微调使用了 1,000 个样本。由于模型规模较小（0.5B）且仅更新稀疏记忆层，其算力需求远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：作者在两个不同的目标任务（TriviaQA 和 SimpleQA）下进行了实验，并同时监控了两个不同维度的保留能力指标。
*   **充分性评价**：实验设计较为合理，涵盖了“学习能力”与“稳定性”的权衡分析。通过对比 TF-IDF 和 KL 散度两种策略，验证了不同选择机制在不同任务下的有效性。不过，实验主要集中在 0.5B 的小模型上，缺乏在更大规模模型（如 7B 或 70B）上的扩展性验证。

### 6. 主要结论与发现
*   **稀疏更新的优越性**：相比全量微调，稀疏记忆微调能以更快的速度适应新任务，同时显著减少在 GSM8K 等推理任务上的性能退化。
*   **KL 机制的有效性**：在处理复杂任务（如 SimpleQA）时，基于 KL 散度的评分机制比 TF-IDF 更稳定，能有效防止噪声更新导致的模型漂移。
*   **可塑性与稳定性的权衡**：实验发现这种权衡是任务相关的，KL 机制在适应信号较弱时对保留旧知识更有利。

### 7. 优点与亮点
*   **开源贡献**：提供了一套可复现的、针对现有开源 Transformer 模型的改造流程，具有很强的实用价值。
*   **理论支撑**：将信息论中的 KL 散度引入插槽选择，相比之前的启发式方法更具科学性。
*   **高效性**：证明了在极少量参数更新（稀疏梯度掩码）的情况下，模型依然能保持强大的学习能力。

### 8. 不足与局限
*   **模型规模局限**：实验仅限于 0.5B 参数的模型，尚不清楚该结论在超大规模模型中是否一致。
*   **超参数敏感性**：稀疏更新中的 Top-T 选择、背景分布的采样数量等超参数可能对结果有较大影响，文中未做深入的消融研究。
*   **任务覆盖面**：虽然测试了问答和数学推理，但对于长文本生成、代码编写等其他能力的遗忘情况未做全面评估。

（完）
