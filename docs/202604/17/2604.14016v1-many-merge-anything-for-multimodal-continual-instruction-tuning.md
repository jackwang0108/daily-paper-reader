---
title: "MAny: Merge Anything for Multimodal Continual Instruction Tuning"
title_zh: MAny：用于多模态持续指令微调的通用合并框架
authors: "Zijian Gao, Wangwang Jia, Xingxing Zhang, Pengfei Qian, Tao Sun, Bo Ding, Yong Dou, Huaimin Wang, Kele Xu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14016v1"
tags: ["query:q3"]
score: 9.0
evidence: 通过参数合并解决多模态持续指令微调中的灾难性遗忘问题
tldr: 本研究针对多模态大模型在持续指令微调中的灾难性遗忘问题，揭示了感知漂移与推理崩溃的双重遗忘现象。为此提出MAny框架，通过跨模态投影合并（CPM）恢复感知对齐，并利用低秩参数合并（LPM）消除任务间干扰。该方法无需额外训练，仅靠高效代数运算即可实现知识融合，在多个基准测试中显著优于现有SOTA方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14016v1/fig-001.webp\", \"caption\": \"Figure 1: Performance diagnosis on UCIT benchmark for LLaVA-1.5-7B and InternVL-Chat7B during sequential LoRA fine-tuning. Tasks follow a clockwise sequence from Image-R.\", \"page\": 2, \"index\": 1, \"width\": 746, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.14016v1/fig-002.webp\", \"caption\": \"Table 1: Results on the UCIT benchmark using LLaVA-1.5-7B and InternVL-Chat-7B models.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.14016v1/fig-003.webp\", \"caption\": \"Table 4: Results on the MLLM-DCL benchmark using LLaVA-1.5-7B and InternVL-Chat-7B models.\", \"page\": 17, \"index\": 3, \"width\": 826, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.14016v1/fig-004.webp\", \"caption\": \"Figure 3: Overview of MAny. The dual-track design explicitly decouples perception from reasoning.\", \"page\": 5, \"index\": 4, \"width\": 826, \"height\": 334}]"
motivation: 旨在解决多模态持续指令微调中跨模态投影空间的感知漂移和低秩参数空间的推理崩溃这一双重遗忘难题。
method: 提出MAny框架，利用视觉原型引导的投影合并（CPM）和基于递归最小二乘法的闭式低秩参数合并（LPM）来实现任务知识的无损融合。
result: "在UCIT基准测试中，MAny在两种主流多模态大模型上分别实现了8.57%和2.85%的平均准确率提升，刷新了现有最先进水平。"
conclusion: MAny作为一种无需训练的参数合并范式，通过高效的代数运算证明了在保持推理稳定性的同时，能有效解决多模态模型的持续学习遗忘问题。
---

## 摘要
多模态持续指令微调（MCIT）对于多模态大语言模型（MLLMs）的序列任务自适应至关重要，但受到灾难性遗忘的严重限制。现有文献主要关注推理语言主干，而本研究揭示了一个关键但被忽视的双重遗忘现象，即跨模态投影空间中的感知漂移和低秩参数空间中的推理崩溃。为解决这一问题，我们提出了 MAny（Merge Anything），这是一个通过跨模态投影合并（CPM）和低秩参数合并（LPM）来融合任务特定知识的框架。具体而言，CPM 通过视觉原型引导自适应地合并跨模态视觉表示，从而恢复感知对齐，确保推理过程中的准确特征恢复。同时，LPM 通过递归合并低秩权重矩阵，消除了任务特定低秩模块之间的相互干扰。利用递归最小二乘法，LPM 提供了一个闭式解，从数学上保证了推理稳定性的最优融合轨迹。值得注意的是，MAny 是一种无需训练的范式，通过高效的基于 CPU 的代数运算实现知识合并，消除了初始微调之外的额外基于梯度的优化。我们广泛的评估证实了 MAny 在多个 MLLM 和基准测试中的卓越性能和鲁棒性。具体而言，在 UCIT 基准测试中，MAny 在两种不同的 MLLM 上，最终平均准确率分别比现有最先进方法领先高达 8.57% 和 2.85%。

## Abstract
Multimodal Continual Instruction Tuning (MCIT) is essential for sequential task adaptation of Multimodal Large Language Models (MLLMs) but is severely restricted by catastrophic forgetting. While existing literature focuses on the reasoning language backbone, in this work, we expose a critical yet neglected dual-forgetting phenomenon across both perception drift in Cross-modal Projection Space and reasoning collapse in Low-rank Parameter Space. To resolve this, we present \textbf{MAny} (\textbf{M}erge \textbf{Any}thing), a framework that merges task-specific knowledge through \textbf{C}ross-modal \textbf{P}rojection \textbf{M}erging (\textbf{CPM}) and \textbf{L}ow-rank \textbf{P}arameter \textbf{M}erging (\textbf{LPM}). Specifically, CPM recovers perceptual alignment by adaptively merging cross-modal visual representations via visual-prototype guidance, ensuring accurate feature recovery during inference. Simultaneously, LPM eliminates mutual interference among task-specific low-rank modules by recursively merging low-rank weight matrices. By leveraging recursive least squares, LPM provides a closed-form solution that mathematically guarantees an optimal fusion trajectory for reasoning stability. Notably, MAny operates as a training-free paradigm that achieves knowledge merging via efficient CPU-based algebraic operations, eliminating additional gradient-based optimization beyond initial tuning. Our extensive evaluations confirm the superior performance and robustness of MAny across multiple MLLMs and benchmarks. Specifically, on the UCIT benchmark, MAny achieves significant leads of up to 8.57\% and 2.85\% in final average accuracy over state-of-the-art methods across two different MLLMs, respectively.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MAny (Merge Anything)** 的框架，旨在解决多模态大语言模型（MLLM）在持续学习过程中的灾难性遗忘问题。以下是对该论文的结构化深入分析：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：多模态持续指令微调（MCIT）要求模型能够按顺序学习新任务，同时不丢失旧任务的能力。
*   **核心问题**：现有的抗遗忘研究大多集中在语言模型主干（LLM backbone）上，而忽视了多模态模型特有的结构。
*   **关键发现**：论文揭示了**“双重遗忘”现象**：
    1.  **感知漂移（Perception Drift）**：跨模态投影层（Projector）在学习新任务时会发生偏移，导致视觉特征与语言空间的对齐失效。
    2.  **推理崩溃（Reasoning Collapse）**：LLM 内部的低秩参数空间（LoRA）在序列更新中产生参数干扰，导致逻辑推理能力下降。

### 2. 方法论：核心思想与关键技术
MAny 采用**解耦感知与推理**的策略，通过两个核心模块实现无需额外训练的知识合并：

*   **跨模态投影合并 (CPM - Cross-modal Projection Merging)**：
    *   **核心思想**：为每个任务保留轻量级的投影层，而不是强制所有任务共享一个不断漂移的投影器。
    *   **技术细节**：在训练阶段，为每个任务提取视觉原型（Visual Prototype，即图像特征的均值）。在推理阶段，计算输入图像与各任务原型的余弦相似度，通过 Softmax 归一化得到权重，自适应地合并多个投影器的输出。
*   **低秩参数合并 (LPM - Low-rank Parameter Merging)**：
    *   **核心思想**：利用数学闭式解将不同任务的 LoRA 权重递归地合并到一个全局任务向量中。
    *   **技术细节**：基于**递归最小二乘法 (RLS)**，通过维护一个累积特征协方差矩阵（Capture Feature Covariance），在不访问历史数据的情况下，找到冲突最小化的参数融合路径。
    *   **优化**：引入了 **MAny\*** 变体，利用奇异值分解 (SVD) 压缩协方差矩阵，大幅降低存储开销。

### 3. 实验设计
*   **数据集与基准**：
    *   **UCIT 基准**：包含 6 个序列任务（Image-R, ArxivQA, Viz-cap, IconQA, CLEVR, Flickr30k），侧重于通用能力的持续学习。
    *   **MLLM-DCL 基准**：包含 5 个专业领域任务（遥感、医学、自动驾驶、科学、金融），侧重于专业知识的获取。
*   **基础模型**：LLaVA-1.5-7B 和 InternVL-Chat-7B。
*   **对比方法**：包括传统的 LoRA 微调、O-LoRA、MoELoRA、ModalPrompt、CL-MoE、HiDE 和 SEFE 等 7 种最先进的持续学习方法。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA RTX A100 GPU 集群**上运行。
*   **训练细节**：UCIT 任务训练 1 个 epoch；MLLM-DCL 任务根据复杂度训练 1-3 个 epoch。
*   **算力优势**：MAny 的核心合并过程是**无需训练（Training-free）**的，仅涉及 CPU 上的代数运算，不需要额外的 GPU 梯度优化或历史数据回放。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流 MLLM 架构和两个大型持续学习基准上进行了测试，涵盖了 11 个不同的数据集。
*   **消融实验**：详细验证了 CPM 和 LPM 各自的贡献，以及不同模态（文本、图像、融合）对原型构建的影响。
*   **敏感性分析**：对缩放因子 $\lambda$ 和 SVD 能量阈值 $\gamma$ 进行了详尽的参数搜索。
*   **评价**：实验设计非常充分且客观，通过 FAA（最终平均准确率）、CAA（累积平均准确率）和 FFM（最终遗忘度）三个维度全面评估了性能，证明了结果的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在 UCIT 基准上，MAny 的最终平均准确率比现有 SOTA 方法分别高出 **8.57%** (LLaVA) 和 **2.85%** (InternVL)。
*   **零遗忘甚至正向迁移**：在 MLLM-DCL 基准上，MAny 实现了负值的 FFM（遗忘度），意味着学习新任务反而增强了旧任务的表现（正向回溯转移）。
*   **感知的重要性**：实验证明，在某些任务（如 CLEVR）中，修复投影层的感知漂移比修复 LLM 的推理权重更为关键。

### 7. 优点与亮点
*   **理论严谨**：LPM 模块基于递归最小二乘法，提供了数学上可证明的最优融合轨迹。
*   **高效轻量**：合并过程无需 GPU 训练，且 MAny\* 变体通过 SVD 极大地压缩了存储需求（仅需原有的 1/27）。
*   **即插即用**：CPM 模块可以轻松集成到现有的其他持续学习方法中，并显著提升其抗遗忘能力。

### 8. 不足与局限
*   **存储开销**：尽管有 SVD 压缩，但随着任务数量增加，仍需存储多个任务特定的投影层和视觉原型。
*   **超参数依赖**：缩放因子 $\lambda$ 对最终性能有一定影响，虽然论文给出了推荐值，但在极端分布变化的任务中可能需要微调。
*   **任务边界假设**：虽然推理是任务不可知的，但训练阶段仍假设了清晰的任务边界，对于完全无监督的流式学习场景可能需要进一步扩展。

（完）
