---
title: Quantum-Gated Task-interaction Knowledge Distillation for Pre-trained Model-based Class-Incremental Learning
title_zh: 基于预训练模型的类增量学习的量子门控任务交互知识蒸馏
authors: "Linjie Li, Huiyu Xiao, Jiarui Cao, Zhenyu Wu, Yang Ji"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11112v1"
tags: ["query:q5"]
score: 10.0
evidence: 提出了一种基于预训练模型的类增量学习量子门控知识蒸馏框架。
tldr: 针对预训练模型在类增量学习中因多任务子空间纠缠导致的灾难性遗忘问题，本文提出了一种量子门控任务交互知识蒸馏（QKD）框架。该框架利用量子门控机制动态建模任务嵌入间的依赖关系，并通过任务交互知识蒸馏弥合独立任务子空间之间的表示差距，在减少遗忘的同时实现了最先进的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11112v1/fig-001.webp\", \"caption\": \"Figure 2. Performance curve of various methods under different settings. The relative improvement of QKD over the second-best method at the final incremental stage is highlighted with numerical annotations.\", \"page\": 7, \"index\": 1, \"width\": 1036, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2604.11112v1/fig-002.webp\", \"caption\": \"Table 1. Average and final accuracy comparison across five datasets. Bold numbers indicate the best performance, while underlined values represent the second-best. All methods are evaluated under the exemplar-free setting.\", \"page\": 6, \"index\": 2, \"width\": 1031, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.11112v1/fig-003.webp\", \"caption\": \"Figure 1. Illustration of QKD. Left: The current task sample is encoded by the frozen PTM and the first adapter, while old adapters generate task representations forming a task pool. These representations are mapped into the quantum space to compute task correlations. Right: The task correlations produce task scores, which weight the feature-level knowledge distillation to guide new adapter training.\", \"page\": 5, \"index\": 3, \"width\": 1036, \"height\": 413}]"
motivation: 预训练模型在增量学习中难以处理多任务子空间的纠缠，导致任务路由参数失准或表示僵化，进而引发灾难性遗忘。
method: 引入量子门控任务调制机制来捕捉样本与任务的相关性，并结合任务交互知识蒸馏，利用任务嵌入级的关联权重引导新旧适配器间的知识迁移。
result: 在多个基准测试上的广泛实验表明，QKD 框架有效缓解了遗忘问题，并达到了当前类增量学习的最优性能水平。
conclusion: 量子门控与知识蒸馏的结合能有效增强任务间的协同作用，是解决预训练模型增量学习中表示差距问题的有效方案。
---

## 摘要
类增量学习（CIL）旨在从任务流中持续积累知识，并为所有已见类别构建统一的分类器。尽管预训练模型（PTMs）在 CIL 中表现出良好的性能，但它们仍难以处理多任务子空间的纠缠问题，当任务路由参数校准不良或任务级表示过于僵化时，会导致灾难性遗忘。为了解决这一问题，我们提出了一种新颖的量子门控任务交互知识蒸馏（QKD）框架，利用量子门控来引导任务间的知识迁移。具体而言，我们引入了一种量子门控任务调制门控机制，用于建模任务嵌入（task embedding）之间的关系依赖，动态捕捉流式任务中联合训练和推理过程中的样本与任务相关性。在量子门控输出的指导下，我们利用这些任务嵌入级的相关性权重，在旧适配器（adapter）与新适配器之间进行任务交互知识蒸馏，使模型能够弥合独立任务子空间之间的表示鸿沟。大量实验表明，QKD 有效地缓解了遗忘问题，并达到了当前最先进的性能。

## Abstract
Class-incremental learning (CIL) aims to continuously accumulate knowledge from a stream of tasks and construct a unified classifier over all seen classes. Although pretrained models (PTMs) have shown promising performance in CIL, they still struggle with the entanglement of multi-task subspaces, leading to catastrophic forgetting when task routing parameters are poorly calibrated or task-level representations are rigidly fixed. To address this issue, we propose a novel Quantum-Gated Task-interaction Knowledge Distillation (QKD) framework that leverages quantum gating to guide inter-task knowledge transfer. Specifically, we introduce a quantum-gated task modulation gating mechanism to model the relational dependencies among task embedding, dynamically capturing the sample-to-task relevance for both joint training and inference across streaming tasks. Guided by the quantum gating outputs, we perform task-interaction knowledge distillation guided by these task-embedding-level correlation weights from old to new adapters, enabling the model to bridge the representation gaps between independent task subspaces. Extensive experiments demonstrate that QKD effectively mitigates forgetting and achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **QKD（Quantum-Gated Task-interaction Knowledge Distillation）** 的框架，旨在解决基于预训练模型（PTM）的类增量学习（CIL）中的任务干扰和遗忘问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在类增量学习中，尽管预训练模型提供了强大的特征表示，但随着新任务不断加入，不同任务的特征子空间会发生**纠缠（Entanglement）**。
*   **研究背景**：现有的方法（如 Prompt-based 或 Adapter-based）通常依赖简单的相似度度量或启发式规则进行任务路由。当任务增多、子空间重叠严重时，这些浅层机制无法准确量化样本与历史任务的相关性，导致在任务不可知的推理阶段出现严重的“灾难性遗忘”和预测偏差。
*   **动机**：探索是否能利用量子计算的高维希尔伯特空间表示能力，更精确地建模复杂的任务间交互关系。

### 2. 核心方法论
QKD 框架主要由两个核心模块组成：
*   **量子门控任务调制（QGTM）**：
    *   **任务嵌入提取**：对每个任务的适配器（Adapter）参数进行奇异值分解（SVD），提取前 $r$ 个主成分作为该任务的量子态表示。
    *   **量子电路编码**：将当前样本特征和任务嵌入映射到参数化量子电路（PQC）中。通过单比特旋转门进行角度编码，并利用 CNOT 门链产生量子纠缠，从而在希尔伯特空间中捕捉高阶非线性依赖。
    *   **相关性度量**：通过测量样本量子态与任务量子态之间的保真度（Fidelity），计算样本与各历史任务的相关性得分 $\alpha_i$。
*   **任务交互知识蒸馏（TIKD）**：
    *   **动态权重蒸馏**：利用量子门控输出的得分 $\alpha_i$ 作为权重，引导知识从旧适配器迁移到新适配器。
    *   **核心思想**：让新任务有选择地吸收高度相关任务的知识，同时抑制无关任务的干扰，从而在保持塑性的同时增强稳定性。
*   **整体优化**：结合了分类交叉熵损失、量子引导的蒸馏损失以及鼓励门控稀疏性的正则项。

### 3. 实验设计
*   **数据集**：涵盖了五个主流基准，包括 CIFAR-100、CUB-200、ImageNet-A、ImageNet-R 和 VTAB。
*   **实验场景**：采用无样本（Exemplar-free）设置，即不存储任何旧任务的原始数据。
*   **对比方法**：对比了包括 L2P、DualPrompt、CODA-Prompt 等 Prompt 类方法，以及 SimpleCIL、EASE、MoTE 等最新的 Adapter/PEFT 类 SOTA 方法。

### 4. 资源与算力
*   **软件环境**：基于 PyTorch 实现。
*   **训练细节**：Batch size 为 32，训练 20 个 epoch，使用 SGD 优化器，学习率 0.05。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量。但文中提供了计算分析表，显示量子门控模块的参数量（约 1.3M）和推理延迟（每张图约 22ms），表明其在经典硬件上通过量子仿真运行，开销在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同规模和领域的基准数据集上进行了测试，涵盖了从通用物体到细粒度分类。
*   **消融实验**：详细验证了 QGTM、蒸馏损失和稀疏正则项的有效性。
*   **鲁棒性分析**：对 SVD 维度、量子比特数、损失函数系数等超参数进行了敏感性分析。
*   **对比维度**：不仅对比了准确率，还对比了参数量、训练时间、内存占用和推理延迟。
*   **评价**：实验设计全面、客观，通过与多种 SOTA 方法在相同 Backbone（ViT-B/16）下的对比，证明了其优越性。

### 6. 主要结论与发现
*   **性能领先**：QKD 在所有五个基准测试中均取得了最先进（SOTA）的性能，尤其在 ImageNet-A 等困难数据集上提升显著。
*   **量子优势**：实验证明，相比于传统的 MLP 或注意力机制，量子门控能更有效地建模任务间的几何相关性，提供更准确的任务路由。
*   **缓解遗忘**：通过相关性引导的蒸馏，模型能更好地分离任务子空间，显著降低了增量过程中的精度下降。

### 7. 优点与亮点
*   **跨学科创新**：首次将量子机器学习（QML）引入类增量学习的任务路由中，利用量子干涉和叠加特性处理子空间纠缠。
*   **参数高效**：基于冻结的 PTM 和轻量化适配器，符合现代大模型微调的趋势。
*   **无需样本**：在不存储旧数据的情况下实现了极高的性能，保护了隐私并节省了存储。

### 8. 不足与局限
*   **推理延迟**：由于是在经典计算机上模拟量子电路，其推理延迟（Latency）高于传统的余弦相似度或简单的 MLP 门控。
*   **量子仿真限制**：目前的量子比特数较少（实验中为 3-15 个），在大规模量子比特下的扩展性及在真实量子硬件上的表现尚待验证。
*   **理论解释性**：虽然实验效果好，但量子优势在视觉特征处理中的深层数学机理仍有待进一步挖掘。

（完）
