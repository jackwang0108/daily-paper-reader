---
title: Gradient-Free Continual Learning in Spiking Neural Networks via Inter-Spike Interval Regularization
title_zh: 脉冲神经网络中基于脉冲间隔正则化的无梯度持续学习
authors: "Samrendra Roy, Kazuma Kobayashi, Souvik Chakraborty, Sajedul Talukder, Syed Bahauddin Alam"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.16496v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于缓解脉冲神经网络遗忘的无梯度突触重要性指标
tldr: 针对神经形态硬件缺乏反向传播支持的问题，本文提出了一种名为ISI-CV的无梯度脉冲神经网络（SNN）持续学习方法。该方法利用脉冲间隔（ISI）的变异系数（CV）来衡量突触重要性，通过保护发放规律的神经元来防止灾难性遗忘。在多个基准测试（包括真实DVS数据）中，ISI-CV实现了极低的遗忘率，且在性能上优于传统的基于梯度的正则化方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16496v1/fig-001.webp\", \"caption\": \"Figure 4: ISI-CV stability–plasticity tradeoff on Split-MNIST (AF scaled ×10 for visibility). Forgetting stays near zero across 𝜆 ∈ [10, 5000]; average accuracy peaks at 𝜆=500 (AA= 0.983). The tradeoff is smooth and controlled by a single hyperparameter with no architectural changes.\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.16496v1/fig-002.webp\", \"caption\": \"Figure 5: Backward Transfer (BWT) across four benchmarks (mean ± std, 3 seeds). ISI-CV achieves BWT≥ 0 on three of four benchmarks. EWC performs worse than No Reg on Split-MNIST and N-MNIST.\", \"page\": 7, \"index\": 2, \"width\": 1038, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.16496v1/fig-003.webp\", \"caption\": \"Figure 1: ISI-CV regularization as a four-step continual learning cycle. (1) The SNN is trained on task 𝑘 via surrogategradient descent. The shared trunk (W1+LIF) feeds 𝐾 pertask heads. (2) After training, a gradient-free inference pass collects hidden spike trains. Neuron 𝑖 (blue) fires regularly (equal ISIs, low CV); Neuron 𝑗 (orange) fires in burst– gap bursts (variable ISIs, high CV). (3) ISI-CV importance scores are assigned: Ω𝑖 = (CV𝑖+𝜀)−1. High-Ω neurons protect W1 (blue bar); low-Ω neurons update freely (orange bar). (4) Task 𝑘+1 trains with the regularized loss (Eq. 5). The new head (teal) adapts freely whileW1 is penalized by Ω (𝑘 ) . The orange loop arrow marks the increment to task 𝑘+2.\", \"page\": 4, \"index\": 3, \"width\": 499, \"height\": 1036}, {\"url\": \"assets/figures/arxiv/2604.16496v1/fig-004.webp\", \"caption\": \"Figure 2: Average Accuracy (top row) and Average Forgetting (bottom row) for all methods across four benchmarks (mean ± std, 3 seeds). ISI-CV achieves the lowest forgetting on every benchmark and the highest accuracy on FashionMNIST and N-MNIST.\", \"page\": 5, \"index\": 4, \"width\": 1036, \"height\": 427}]"
motivation: 现有的持续学习方法依赖梯度计算，无法直接应用于缺乏反向传播支持的神经形态硬件。
method: 提出基于脉冲间隔变异系数（ISI-CV）的突触重要性度量，通过保护发放规律的神经元并允许不规律神经元更新来保留知识。
result: 在Split-MNIST等任务上实现了零遗忘，并在复杂的N-MNIST神经形态数据集上取得了最高的准确率和最低的遗忘率。
conclusion: ISI-CV证明了仅利用脉冲时间信息即可实现高效的持续学习，为在资源受限的神经形态芯片上部署在线学习提供了可行方案。
---

## 摘要
持续学习是指按顺序获取新任务而不遗忘先前知识的能力，这对于在动态现实环境中部署神经网络至关重要，应用场景涵盖从核数字孪生监测到电网边缘故障检测。现有的突触重要性方法，如弹性权重固化（EWC）和突触智能（SI），依赖于梯度计算，这使得它们与缺乏反向传播支持的类脑硬件不兼容。我们提出了 ISI-CV，这是首个用于脉冲神经网络（SNN）持续学习的无梯度突触重要性度量，它源自脉冲间隔（ISI）的变异系数（CV）。定期放电（低 CV）的神经元编码稳定的、与任务相关的特征，并受到保护免于被覆盖；放电不规律的神经元则被允许自由调整。ISI-CV 仅需要脉冲时间计数器和整数运算，这些在所有类脑芯片中都是原生的。我们在四个难度递增的基准测试上进行了评估：Split-MNIST、Permuted-MNIST、Split-FashionMNIST 以及使用真实动态视觉传感器（DVS）事件数据的 Split-N-MNIST。在三个随机种子下，ISI-CV 在 Split-MNIST 和 Split-FashionMNIST 上实现了零遗忘（AF = 0.000 +/- 0.000），在 Permuted-MNIST 上实现了接近零遗忘（AF = 0.001 +/- 0.000），并在真实类脑 DVS 数据上获得了最高的准确率和最低的遗忘率（AA = 0.820 +/- 0.012, AF = 0.221 +/- 0.014）。在 N-MNIST 上，基于梯度的方法会产生不可靠的重要性估计，表现甚至不如不进行正则化；而 ISI-CV 通过设计避免了这一失败。

## Abstract
Continual learning, the ability to acquire new tasks sequentially without forgetting prior knowledge, is essential for deploying neural networks in dynamic real-world environments, from nuclear digital twin monitoring to grid-edge fault detection. Existing synaptic importance methods, such as Elastic Weight Consolidation (EWC) and Synaptic Intelligence (SI), rely on gradient computation, making them incompatible with neuromorphic hardware that lacks backpropagation support. We propose ISI-CV, the first gradient-free synaptic importance metric for SNN continual learning, derived from the Coefficient of Variation (CV) of Inter-Spike Intervals (ISIs). Neurons that fire regularly (low CV) encode stable, task-relevant features and are protected from overwriting; neurons with irregular firing are permitted to adapt freely. ISI-CV requires only spike time counters and integer arithmetic, all of which are native to every neuromorphic chip. We evaluate on four benchmarks of increasing difficulty: Split-MNIST, Permuted-MNIST, Split-FashionMNIST, and Split-N-MNIST using real Dynamic Vision Sensor (DVS) event data. Across three seeds, ISI-CV achieves zero forgetting (AF = 0.000 +/- 0.000) on Split-MNIST and Split-FashionMNIST, near-zero forgetting on Permuted-MNIST (AF = 0.001 +/- 0.000), and the highest accuracy with the lowest forgetting on real neuromorphic DVS data (AA = 0.820 +/- 0.012, AF = 0.221 +/- 0.014). On N-MNIST, gradient-based methods produce unreliable importance estimates and perform worse than no regularization; ISI-CV avoids this failure by design.

---

## 论文详细总结（自动生成）

这是一份关于论文《Gradient-Free Continual Learning in Spiking Neural Networks via Inter-Spike Interval Regularization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在脉冲神经网络（SNN）中实现高效的**持续学习**（Continual Learning），且不依赖于梯度计算。
*   **研究背景**：
    *   **灾难性遗忘**：神经网络在学习新任务时会覆盖旧任务的权重。
    *   **硬件不兼容性**：现有的正则化方法（如 EWC, SI）依赖反向传播产生的梯度来评估突触重要性。然而，主流的神经形态硬件（如 Intel Loihi, IBM TrueNorth）原生不支持反向传播，导致这些算法无法在端侧芯片上直接运行。
    *   **应用需求**：在核电数字孪生监测、电网边缘故障检测等实时、低功耗场景中，需要 SNN 能够在硬件上直接进行在线学习且不遗忘。

### 2. 方法论：ISI-CV 正则化
*   **核心思想**：借鉴神经科学发现，利用**脉冲间隔（ISI）的变异系数（CV）**作为神经元重要性的指标。
    *   **低 CV（规律发放）**：神经元像时钟一样定期放电，表明其已捕获稳定的、任务相关的特征，应予以保护。
    *   **高 CV（不规律发放）**：神经元放电随机或处于未承诺状态，可自由更新以学习新任务。
*   **关键技术细节**：
    1.  **推理阶段统计**：在完成任务 $k$ 训练后，进行一次不带梯度的推理，记录隐藏层神经元的脉冲时间点。
    2.  **计算 ISI-CV**：计算每个神经元脉冲间隔的标准差与均值之比（$CV_i = \sigma / \mu$）。
    3.  **重要性映射**：定义重要性分数 $\Omega_i = 1 / (CV_i + \epsilon)$，并进行 95 分位裁剪和归一化。
    4.  **正则化损失**：在学习任务 $k+1$ 时，在交叉熵损失基础上增加一个惩罚项，限制重要神经元权重的变化。
*   **硬件友好性**：该流程仅涉及脉冲计数、减法和基础算术运算，完全符合神经形态芯片的计算约束。

### 3. 实验设计
*   **数据集/场景**：
    *   **Split-MNIST**：手写数字分类（5个任务）。
    *   **Permuted-MNIST**：像素随机排列的数字分类（5个任务，输入统计特性冲突，遗忘压力大）。
    *   **Split-FashionMNIST**：视觉更复杂的服装分类（5个任务）。
    *   **Split-N-MNIST**：使用真实动态视觉传感器（DVS）捕获的神经形态事件数据，最具现实意义。
*   **Benchmark 与对比方法**：
    *   **No Reg**：无正则化的顺序微调。
    *   **EWC (Elastic Weight Consolidation)**：基于 Fisher 信息矩阵的梯度方法。
    *   **SI (Synaptic Intelligence)**：基于在线路径积分的梯度方法。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3060 (12 GB)** GPU 的服务器上完成。
*   **训练细节**：使用了 **FP16 混合精度** 训练，每个任务训练 10 个 epoch，Batch Size 为 128。
*   **算力说明**：文中未明确给出总训练时长，但强调了 ISI-CV 在重要性估计阶段比 EWC/SI 节省了大量的反向传播计算开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从简单图像到复杂神经形态事件数据的 4 个主流基准。
    *   所有实验均在 **3 个独立随机种子**下运行，并报告了均值和标准差。
    *   进行了详细的**超参数敏感性分析**（$\lambda$ 扫描，跨越 3 个数量级）和 **Pareto 前沿分析**。
*   **客观性与公平性**：实验对比了经典的梯度正则化方法，并揭示了这些方法在 SNN（尤其是处理 N-MNIST 数据时）中由于代理梯度不准导致的失效模式，论证较为客观。

### 6. 主要结论与发现
*   **零遗忘表现**：ISI-CV 在 Split-MNIST 和 Split-FashionMNIST 上实现了 **0.000 的平均遗忘率（AF）**。
*   **超越梯度方法**：在复杂的 N-MNIST 数据集上，ISI-CV 的准确率（82.0%）和遗忘率（0.221）均优于 EWC 和 SI。
*   **梯度失效现象**：研究发现，在 SNN 中，基于梯度的 EWC 和 SI 有时表现甚至不如“不进行正则化”，因为代理梯度产生的 Fisher 信息估计不可靠，而 ISI-CV 避开了这一问题。
*   **稳定性**：ISI-CV 的性能方差显著低于对比方法，表现出极强的鲁棒性。

### 7. 优点（亮点）
*   **首创性**：提出了首个专门针对 SNN 且完全**无梯度**的持续学习重要性指标。
*   **生物启发**：将神经科学中的放电规律性理论成功转化为机器学习的正则化约束。
*   **部署潜力**：算法逻辑极简，仅需整数运算和计数，是目前最接近在神经形态硬件上实现“片上持续学习”的方案。

### 8. 不足与局限
*   **任务设置限制**：目前主要在**任务增量（Task-IL）**设置下评估（即推理时已知任务 ID），尚未深入探讨更难的类增量（Class-IL）设置。
*   **准确率权衡**：在某些任务（如 Permuted-MNIST）上，为了实现极低的遗忘率，其平均准确率略低于 EWC。
*   **规模扩展**：实验数据集仍属于中小规模，尚未在大规模神经形态数据集（如 N-Caltech101 或 DVS-Gesture）上验证其扩展性。

（完）
