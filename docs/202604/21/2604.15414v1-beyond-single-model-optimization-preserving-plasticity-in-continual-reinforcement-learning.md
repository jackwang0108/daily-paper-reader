---
title: "Beyond Single-Model Optimization: Preserving Plasticity in Continual Reinforcement Learning"
title_zh: 超越单模型优化：在持续强化学习中保持可塑性
authors: "Lute Lillo, Nick Cheney"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15414v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于在任务间保持塑性的持续强化学习框架
tldr: 本研究针对持续强化学习中单一模型保存导致的可塑性丧失问题，提出了TeLAPA框架。该方法借鉴质量多样性算法，为每个任务维护一个行为多样的策略存档，并通过共享潜空间对齐不同任务的策略。TeLAPA将持续学习的重点从保留孤立的最优解转向维护技能对齐的策略邻域，从而在面对非平稳环境时提供更灵活的重学起点。实验证明，该方法在任务成功率和迁移效率上显著提升，为构建具备高度可塑性的终身学习智能体提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15414v1/fig-001.webp\", \"caption\": \"Table 7: Alternative curriculum 2: skill-scrambled non-monotone order. We report mean ± 95% CI across 20 runs. We evaluate all single-model preservation baselines and TeLAPA variations following the Alternative Curriculum 2. We use the same hyperparameter configuration for all methods as in the main paper results.\", \"page\": 25, \"index\": 1, \"width\": 992, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-002.webp\", \"caption\": \"Table 6: Alternative curriculum 1: reverse anti-curriculum. We report mean± 95% CI across 20 runs. We evaluate all single-model preservation baselines and TeLAPA variations following the Alternative Curriculum 1. We use the same hyperparameter configuration for all methods as in the main paper results.\", \"page\": 25, \"index\": 2, \"width\": 983, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-003.webp\", \"caption\": \"Table 8: Episodic Intrinsic Reward Ablation. We report mean ± 95% CI across 20 runs. We introduce Episodic Intrinsic Reward in all single-model preservation baselines and TeLAPA variations following Eq. 58. We use the same hyperparameter configuration for all methods as in the main paper results.\", \"page\": 26, \"index\": 3, \"width\": 1008, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-004.webp\", \"caption\": \"Table 4: MAP-Elites hyperparameters used in the illumination stage.\", \"page\": 15, \"index\": 4, \"width\": 977, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-005.webp\", \"caption\": \"Table 2: Curriculum environments and intended skill increments. Each task builds on prior primitives while adding a new interaction requirement (navigation → pickup → unlocking → clutter manipulation → long-horizon subgoal chaining).\", \"page\": 14, \"index\": 5, \"width\": 943, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-006.webp\", \"caption\": \"Table 10: Revisit Mean SR (Continual Learning signal). Per-task mean Success Rate on revisited tasks after the archive has been illuminated across previous tasks. ∗ indicates that multiple methods achieve 100% success rate on that task.\", \"page\": 28, \"index\": 6, \"width\": 493, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-007.webp\", \"caption\": \"Figure 8: Cross-task geometry in the shared latent space. Left: t-SNE of elites from the best run (highest mean SR across tasks). Middle: same visualization for the worst run (lowest mean SR across tasks). Right: mean separation ratio matrix Skℓ (Eq. 61), measuring centroid distance normalized by within-task dispersion.\", \"page\": 28, \"index\": 7, \"width\": 979, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-008.webp\", \"caption\": \"Table 5: Hyperparameter search space explored for TELAPA, together with the final selected configuration used in the main experiments.\", \"page\": 17, \"index\": 8, \"width\": 752, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-009.webp\", \"caption\": \"Figure 6: Stepping-stone lineage structure across task archives. (a) Immediate source-task archive → current target-task archive. Rows denote the most recent source archive used for transfer, and columns denote the current target-task archive. Each cell shows how many evaluated candidates for that target were drawn from that immediate source archive. (b) Recorded archive visits in lineage. Columns denote the current target-task archive, and rows denote archive tags appearing anywhere in recorded lineage history. Each cell shows the total number of recorded lineage visits to archive tag S among candidates evaluated for target archive T . Counts are overlapping rather than exclusive: a single candidate contributes to every archive tag that appears in its lineage.\", \"page\": 9, \"index\": 9, \"width\": 979, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-010.webp\", \"caption\": \"Figure 7: Lineage utility across transferred candidates. Each panel compares the distributions of final transfer performance for two candidate groups defined by a different lineage property, shown separately for each target task. (a) Breadth-rich versus breadth-poor lineage histories. Candidates are grouped by whether their lineage contains the original archive family corresponding to the current target. (b) Revisit-family membership. Candidates are grouped by whether their lineage contains the original family corresponding to the current target. We measure whether including that target-relevant family predicts higher final transfer performance. (c) Adjacent versus non-adjacent ancestry. Candidates are grouped by whether their lineage includes a non-adjacent transition in archive-family order.\", \"page\": 9, \"index\": 10, \"width\": 979, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.15414v1/fig-011.webp\", \"caption\": \"Table 1: TELAPA against single-model baselines. Across 20 runs (mean± 95% CI), TELAPA is strongest on metrics most directly tied to continual recoverability—higher mean SR, faster TTT, broader task coverage, and stronger threshold retention—supporting the claim that preserving policy neighborhoods improves later reuse relative to singlemodel preservation. Mean SR, Coverage, nBWT, and TR are higher-is-better; TTT is reported in millions of environment steps and lower-is-better. For BWT, values closer to 0 are better, negative values indicate stronger forgetting.\", \"page\": 5, \"index\": 11, \"width\": 979, \"height\": 254}]"
motivation: 传统持续强化学习依赖单一模型保存，容易在任务切换中丧失可塑性，导致难以快速适应新任务或恢复旧能力。
method: 提出TeLAPA框架，通过构建按任务划分的行为多样性策略存档，并利用共享潜空间保持策略的可比性与可重用性。
result: 在MiniGrid实验中，TeLAPA在任务成功率、干扰后能力恢复速度以及序列任务性能保持方面均优于现有方法。
conclusion: 持续强化学习应从保留单一最优解转向维护具备竞争力的策略邻域，以通过多样性选择提升智能体的终身学习能力。
---

## 摘要
持续强化学习必须平衡保留与适应，然而许多方法仍依赖于“单模型保留”（single-model preservation），即将一个不断演化的策略作为跨任务的主要可复用解决方案。即使保留了先前成功的策略，在受到干扰后，它可能也不再能为快速适应提供可靠的起点，这反映了一种单策略保留无法解决的“可塑性丧失”（loss of plasticity）。受质量-多样性（quality-diversity）方法的启发，我们引入了 TeLAPA（支持迁移的潜空间对齐策略存档），这是一个持续强化学习框架，它将行为多样化的策略邻域组织到每个任务的存档中，并维护一个共享的潜空间，使存档的策略在非平稳漂移下保持可比性和可复用性。这一视角将持续强化学习从保留孤立的解决方案转变为维护“技能对齐邻域”（skill-aligned neighborhoods），其中包含能够支持未来重新学习的、胜任且行为相关的策略。在我们的 MiniGrid 持续学习设置中，TeLAPA 成功学习了更多任务，在受到干扰后能更快地恢复对重访任务的胜任力，并在任务序列中保持了更高的性能。我们的分析表明，源任务最优策略通常并非迁移最优策略，即使在局部胜任邻域内也是如此；有效的复用取决于保留并在多个邻近备选方案中进行选择，而不是将它们折叠为一个代表。总之，这些结果围绕可复用且胜任的策略邻域重新构建了持续强化学习，为超越单模型保留、迈向更具可塑性的终身智能体提供了一条路径。

## Abstract
Continual reinforcement learning must balance retention with adaptation, yet many methods still rely on \emph{single-model preservation}, committing to one evolving policy as the main reusable solution across tasks. Even when a previously successful policy is retained, it may no longer provide a reliable starting point for rapid adaptation after interference, reflecting a form of \emph{loss of plasticity} that single-policy preservation cannot address. Inspired by quality-diversity methods, we introduce \textsc{TeLAPA} (Transfer-Enabled Latent-Aligned Policy Archives), a continual RL framework that organizes behaviorally diverse policy neighborhoods into per-task archives and maintains a shared latent space so that archived policies remain comparable and reusable under non-stationary drift. This perspective shifts continual RL from retaining isolated solutions to maintaining \emph{skill-aligned neighborhoods} with competent and behaviorally related policies that support future relearning. In our MiniGrid CL setting, \textsc{TeLAPA} learns more tasks successfully, recovers competence faster on revisited tasks after interference, and retains higher performance across a sequence of tasks. Our analyses show that source-optimal policies are often not transfer-optimal, even within a local competent neighborhood, and that effective reuse depends on retaining and selecting among multiple nearby alternatives rather than collapsing them to one representative. Together, these results reframe continual RL around reusable and competent policy neighborhoods, providing a route beyond single-model preservation toward more plastic lifelong agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Single-Model Optimization: Preserving Plasticity in Continual Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：持续强化学习（Continual RL）面临两大失败模式：**灾难性遗忘**（丢失旧知识）和**可塑性丧失**（学习新任务的能力下降）。
*   **研究动机**：作者指出，现有的持续学习方法（如正则化、回放、架构扩展）大多局限于“单模型范式”，即试图保护或修改一个单一的、高度专业化的参数点。然而，在非平稳环境下，单一的最优解往往非常脆弱，一旦受到干扰，即使性能尚存，也难以作为未来适应的良好起点。
*   **核心假设**：未来的可学习性（Learnability）不是单一参数点的属性，而是围绕胜任解的“可恢复邻域”的属性。

### 2. 方法论：TeLAPA 框架
TeLAPA（Transfer-Enabled Latent-Aligned Policy Archives）将持续强化学习从保留孤立解转向维护**技能对齐的策略邻域**。
*   **核心思想**：为每个任务维护一个包含多个行为多样且性能优异的策略存档（Archive），而非单一模型。
*   **关键技术细节**：
    *   **存档构建（Archive-then-retrieve）**：使用 PPO 训练基础策略，随后通过参数空间变异的 MAP-Elites 算法在基础解周围生成多样化的“精英策略”存档。
    *   **共享潜空间（Shared Latent Space）**：利用轨迹嵌入器（Trajectory Embedder）将策略映射到潜空间，以便衡量行为多样性。
    *   **潜空间对齐与维护**：为了应对非平稳环境下的“表征漂移”，引入了**锚点集（Anchor Sets）**、**回放对齐损失（Distillation Loss）**和**定期重嵌入（Re-embedding）**机制，确保不同时期的策略在潜空间中保持可比性。
    *   **迁移选择**：当面对新任务或重访任务时，从历史存档中检索候选策略，通过简短的“少样本探测”（Few-shot adaptation probe）选择最具恢复潜力的策略作为初始化点。

### 3. 实验设计
*   **场景与数据集**：使用 **MiniGrid** 环境构建了一个“技能阶梯”课程（包含导航、拾取、解锁、处理障碍等任务），并设计了任务重访序列（A→B→C→D→E→A'→B'...）来测试干扰后的恢复能力。
*   **评估指标**：标准化达标时间（TTT，衡量恢复速度）、覆盖率（Coverage）、后向迁移（BWT）和阈值保留率（TR）。
*   **对比方法**：
    *   基础类：Scratch（从头训练）、Finetune（持续微调）。
    *   正则化类：EWC、L2Init。
    *   架构与可塑性增强类：DFF（深层傅里叶特征）、Shrink-and-Perturb（收缩与扰动）。
    *   基准类：Scratch-Reuse（存储每个任务的一个最优解进行复用）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算特征**：由于涉及 MAP-Elites 的照明阶段（每个任务 1000 次迭代，每次评估 50 个回合）以及 PPO 训练，其计算开销显著高于传统的单模型微调方法。

### 5. 实验数量与充分性
*   **实验规模**：主实验针对 10 个任务序列（含重访）进行了 **20 次独立运行**，并提供了 95% 置信区间。
*   **消融与鲁棒性实验**：
    *   测试了**不同任务顺序**（反向课程、随机打乱课程）以验证鲁棒性。
    *   进行了**内在奖励（Intrinsic Reward）**和**跨存档精英注入**的消融研究。
    *   分析了潜空间的几何结构（t-SNE 可视化）。
*   **充分性评价**：实验设计较为充分，通过多种指标和不同课程设置验证了方法的有效性，对比基线涵盖了主流的持续学习技术，评估过程客观。

### 6. 主要结论与发现
*   **邻域优于单点**：TeLAPA 在平均成功率、达标速度（TTT）和任务覆盖率上显著优于所有单模型基线。
*   **源最优 ≠ 迁移最优**：分析表明，在 35.5% 的情况下，源任务中表现最好的策略并不是迁移到新任务时的最佳起点。保留多样化的邻域策略能捕捉到单模型会丢弃的迁移价值。
*   **潜空间稳定性的重要性**：消融实验显示，如果没有在线维护潜空间对齐，存档的复用效果会大幅下降。
*   **阶梯效应**：TeLAPA 形成了跨任务的“垫脚石”（Stepping-stones）结构，长期的迁移性能往往依赖于保留那些具有特定历史路径的策略。

### 7. 优点与亮点
*   **视角创新**：打破了“保护单一权重”的思维定式，提出保护“策略邻域”来维持可塑性。
*   **结合 QD 与 CL**：成功将质量-多样性（QD）算法引入持续强化学习，解决了 QD 在非平稳环境下的表征漂移问题。
*   **深入的几何分析**：不仅展示了性能提升，还通过局部盆地分析解释了为什么多样性对迁移至关重要。

### 8. 不足与局限
*   **计算成本**：维护和照明（Illumination）多个策略存档需要大量的环境交互，可能限制其在昂贵仿真或现实机器人中的应用。
*   **任务边界依赖**：目前框架依赖于明确的任务边界来进行存档维护和初始化选择，在任务平滑过渡的场景下可能需要调整。
*   **环境复杂度**：实验主要集中在离散动作空间的 MiniGrid 简单网格世界，尚未在复杂的高维连续控制任务（如 MuJoCo）或视觉输入任务中验证。

（完）
