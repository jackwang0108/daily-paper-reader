---
title: Multi-Faceted Continual Knowledge Graph Embedding for Semantic-Aware Link Prediction
title_zh: 面向语义感知链接预测的多维度持续知识图谱嵌入
authors: "Jing Qi, Yuxiang Wang, Zhiyuan Yu, Xiaoliang Xu, Yuanshi Zheng, Tianxing Wu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10947v2"
tags: ["query:q3"]
score: 9.0
evidence: 缓解持续知识图谱嵌入中的灾难性遗忘
tldr: 针对持续知识图谱嵌入（CKGE）中实体语义随时间演化且易发生灾难性遗忘的问题，本文提出MF-CKGE框架。该框架通过将新旧知识分离到不同嵌入空间并采用语义解耦技术，有效避免了知识纠缠与冗余。在推理阶段，通过自适应识别查询相关的语义嵌入，显著提升了链接预测的准确性，在多个数据集上超越了现有基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10947v2/fig-001.webp\", \"caption\": \"Table 1: Dataset statistics: |𝐸𝑖 |, |𝑅𝑖 |, and |Δ𝑇𝑖 | are the number of entities, relations, and new facts in each snapshot 𝑆𝑖\", \"page\": 6, \"index\": 1, \"width\": 1048, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2604.10947v2/fig-002.webp\", \"caption\": \"Figure 2: An evolving KGwith three snapshots: each contains multi-faceted knowledge updates about David Beckham\", \"page\": 2, \"index\": 2, \"width\": 472, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10947v2/fig-003.webp\", \"caption\": \"Table 3: Main results on the last four datasets. The best and second best results are highlighted in bold and underlined\", \"page\": 7, \"index\": 3, \"width\": 962, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10947v2/fig-004.webp\", \"caption\": \"Table 2: Main results on the first four datasets. The best and second best results are highlighted in bold and underlined\", \"page\": 7, \"index\": 4, \"width\": 964, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10947v2/fig-005.webp\", \"caption\": \"Figure 10: Importance for each snapshot embedding whether exhibiting distinct local peaks (e.g., 0.42 in HYBRID’s 𝑄4) or a relatively smoother attention distribution (as in FACT), their diagonal entries consistently remain the highest within their respective columns. Because𝑄𝑖 is inherently intended to evaluate the knowledge specific to snapshot 𝑆𝑖 , these results strongly confirm the model’s semantic-aware capability. It adaptively identifies the relevance of each embedding to the given query, leveraging critical historical knowledge while filtering out noise from irrelevant snapshots to preserve prediction accuracy.\", \"page\": 10, \"index\": 5, \"width\": 470, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.10947v2/fig-006.webp\", \"caption\": \"Figure 3: An overview of the proposed MF-CKGE framework\", \"page\": 4, \"index\": 6, \"width\": 994, \"height\": 422}]"
motivation: 现有CKGE方法将实体的新旧知识混杂在同一嵌入空间，无法捕捉实体随时间演化的多面语义特征，导致链接预测精度下降。
method: 提出MF-CKGE框架，在离线学习时分离新旧知识空间并进行语义解耦，在线推理时根据语义重要性自适应选择查询相关的实体嵌入。
result: "在八个数据集上的实验表明，MF-CKGE在MRR和Hits@10指标上分别比最优基准平均提升了1.7%和1.4%。"
conclusion: 该研究证明了区分实体多面语义及分离新旧知识空间对于提升持续知识图谱嵌入性能的重要性。
---

## 摘要
持续知识图谱嵌入（CKGE）旨在持续学习新知识（即实体和关系）的嵌入，同时保留先前获取的知识。大多数现有的 CKGE 方法通过正则化或重放旧知识来缓解灾难性遗忘。它们将实体的旧知识和新知识合并在同一个嵌入空间中，以寻求两者之间的平衡。然而，实体本质上表现出多维度的语义，这些语义随着其关系上下文随时间的变化而动态演变。共享嵌入无法捕捉和区分这些时间语义变化，从而降低了跨快照的终身链接预测准确率。为了解决这一问题，我们提出了一种用于语义感知链接预测的多维度 CKGE 框架（MF-CKGE）。在离线学习阶段，MF-CKGE 将时间上的旧知识和新知识分离到不同的嵌入空间中，以防止知识纠缠，并采用语义解耦来减少语义冗余，从而提高空间效率。在在线推理阶段，MF-CKGE 通过量化语义重要性，自适应地识别与查询语义相关的实体嵌入，减少来自查询无关噪声的干扰。在八个数据集上的实验表明，MF-CKGE 在 MRR 和 Hits@10 指标上较最优基准模型分别实现了 1.7%（最大 2.7%）和 1.4%（最大 3.8%）的平均提升。我们的源代码和数据集可在以下地址获取：https://anonymous.4open.science/r/MF-CKGE-04E5。

## Abstract
Continual Knowledge Graph Embedding (CKGE) aims to continually learn embeddings for new knowledge, i.e., entities and relations, while retaining previously acquired knowledge. Most existing CKGE methods mitigate catastrophic forgetting via regularization or replaying old knowledge. They conflate new and old knowledge of an entity within the same embedding space to seek a balance between them. However, entities inherently exhibit multi-faceted semantics that evolve dynamically as their relational contexts change over time. A shared embedding fails to capture and distinguish these temporal semantic variations, degrading lifelong link prediction accuracy across snapshots. To address this, we propose a Multi-Faceted CKGE framework (MF-CKGE) for semantic-aware link prediction. During offline learning, MF-CKGE separates temporal old and new knowledge into distinct embedding spaces to prevent knowledge entanglement and employs semantic decoupling to reduce semantic redundancy, thereby improving space efficiency. During online inference, MF-CKGE adaptively identifies semantically query-relevant entity embeddings by quantifying their semantic importance, reducing interference from query-irrelevant noise. Experiments on eight datasets show that MF-CKGE achieves an average (maximum) improvement of 1.7% (2.7%) and 1.4% (3.8%) in MRR and Hits@10, respectively, over the best baseline. Our source code and datasets are available at: https://anonymous.4open.science/r/MF-CKGE-04E5.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MF-CKGE** 的新型持续知识图谱嵌入框架，旨在解决知识图谱在动态演化过程中实体语义多面性导致的知识纠缠与遗忘问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：知识图谱（KG）是动态演化的，新实体和关系不断涌现。持续知识图谱嵌入（CKGE）的目标是在学习新知识的同时，防止对旧知识的“灾难性遗忘”。
*   **核心问题**：现有方法通常将实体的新旧知识压缩在同一个共享嵌入空间中。作者发现，实体具有**多面语义（Multi-faceted Semantics）**，例如大卫·贝克汉姆在不同时期或上下文中分别具有“家庭”、“职业足球”和“爱好”等不同维度的语义。
*   **研究动机**：共享空间会导致不同维度的语义发生纠缠，且在进行特定查询（如查询其家庭成员）时，职业相关的语义会变成“噪声”，从而降低链接预测的准确性。

### 2. 方法论：核心思想与关键技术
MF-CKGE 框架由两个核心模块组成：

*   **实体嵌入解耦模块（离线学习）**：
    *   **时间解耦（Temporal Decoupling）**：为每个时间快照分配独立的嵌入空间。新知识在当前空间学习，旧知识空间被冻结，从而彻底避免新旧知识的物理覆盖。
    *   **表示学习**：结合了知识表达损失（TransE等）、跨快照关系对齐损失（保持关系语义稳定性）和邻域语义一致性损失（利用掩码自编码器 MAE 捕捉局部结构）。
    *   **语义解耦（Semantic Decoupling）**：为了节省空间，计算当前嵌入与历史嵌入的余弦相似度。若相似度高于阈值 $\theta$，则丢弃新嵌入并使用指针指向旧嵌入，仅保留具有显著语义变化的“新面孔”。
*   **语义感知链接预测模块（在线推理）**：
    *   **语义重要性计算**：根据查询关系与各快照中实体相关关系的语义相似度，利用 Top-k 过滤和 Softmax 归一化，为不同快照的嵌入分配权重 $\beta$。
    *   **加权聚合预测**：在推理时，将各快照嵌入的预测得分进行加权求和，使与查询语义最相关的嵌入占据主导地位，抑制无关噪声。

### 3. 实验设计
*   **数据集**：使用了 8 个具有不同演化模式的数据集，包括 ENTITY、RELATION、HYBRID、FB-CKGE、GraphEqual、GraphHigher、GraphLower 以及作者构建的大规模数据集 **DB-CKGE**（基于 DBpedia）。
*   **Benchmark 任务**：终身链接预测（Lifelong Link Prediction），评估模型在不断累积的测试集上的表现。
*   **对比方法**：涵盖了 10 种代表性基准，包括：
    *   正则化类：IncDE, LKGE, SI, EWC。
    *   动态架构类：PNN, CWR, FastKGE。
    *   重放类：GEM, EMR, DiCGRL。
    *   以及全量重训（Retrain）作为性能上限参考。

### 4. 资源与算力
*   **硬件环境**：Linux 服务器，2.4 GHz CPU，128 GB 内存，**NVIDIA RTX A6000 GPU**。
*   **训练时长**：论文强调了高效性。例如在 FB-CKGE 数据集上，MF-CKGE 训练仅需 46 秒，比全量重训（899秒）快 19 倍，比最强基准 IncDE 快 1.4 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个数据集上进行了主实验，涵盖了 MRR、Hits@1、Hits@10 等多项指标。
*   **消融实验**：分别验证了“解耦模块”和“重要性机制”的有效性。
*   **敏感性分析**：探讨了语义压缩阈值 $\theta$、Top-k 选择中的 $k$ 值以及不同基础 KGE 模型（TransE, RotatE, ComplEx）的影响。
*   **充分性评价**：实验设计非常充分且客观。通过可视化热图展示了语义重要性的分配逻辑，并通过案例研究（Case Study）直观证明了模型对噪声的抑制能力。

### 6. 主要结论与发现
*   **性能领先**：MF-CKGE 在所有数据集上均优于现有 CKGE 基准，MRR 平均提升 1.7%，Hits@10 平均提升 1.4%。
*   **克服遗忘**：模型在保留旧知识方面表现稳定，甚至能利用新知识增强对旧查询的预测。
*   **空间与效率平衡**：语义解耦技术在减少约 30% 存储空间的情况下，依然能保持极高的预测精度。
*   **语义感知有效性**：实验证实，通过关系相似度来衡量快照嵌入的重要性是可行且高效的。

### 7. 优点
*   **视角新颖**：首次从实体“多面语义”演化的角度切入 CKGE 问题，而非单纯的参数保护。
*   **架构灵活**：解耦机制天然防御了灾难性遗忘，且可以轻松扩展到不同的基础 KGE 模型（如 RotatE）。
*   **高效实用**：训练速度快，且通过指针机制解决了多空间带来的存储压力。

### 8. 不足与局限
*   **推理开销**：虽然训练快，但在推理时需要计算多个快照的得分并进行加权聚合，对于快照数量极多的超长期演化场景，推理延迟可能会增加。
*   **关系稳定性假设**：模型假设关系语义在跨快照时是稳定的，但在某些特殊领域（如社会关系演变），关系的内涵也可能发生漂移，该模型对此讨论较少。
*   **超参数依赖**：语义解耦阈值 $\theta$ 和 Top-k 的 $k$ 值需要根据具体数据集进行微调以达到最优平衡。

（完）
