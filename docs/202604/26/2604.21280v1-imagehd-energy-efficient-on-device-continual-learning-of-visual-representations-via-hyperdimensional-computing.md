---
title: "ImageHD: Energy-Efficient On-Device Continual Learning of Visual Representations via Hyperdimensional Computing"
title_zh: ImageHD：基于超维计算的高能效设备端视觉表示持续学习
authors: "Jebacyril Arockiaraj, Dhruv Parikh, Viktor Prasanna"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21280v1"
tags: ["query:q6"]
score: 9.0
evidence: 基于超维计算的低功耗设备端持续学习
tldr: 针对边缘设备上持续学习面临的高计算和内存开销问题，本文提出了ImageHD，一种基于超维计算（HDC）的FPGA加速器。该系统结合了量化CNN特征提取器与硬件感知的HDC在线更新策略，通过统一样本内存和聚类合并机制实现高效的流式学习。实验表明，ImageHD在保持高精度的同时，显著提升了处理速度和能效，为资源受限环境下的实时边缘AI提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21280v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF REPRESENTATIVE CONTINUAL (VISUAL) LEARNING PIPELINES FROM THE PERSPECTIVE OF on-device deployability.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.21280v1/fig-002.webp\", \"caption\": \"Fig. 5. Compute Flow of our On-Device Continual Learning Accelerator\", \"page\": 7, \"index\": 2, \"width\": 748, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2604.21280v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of ImageHD accelerator.\", \"page\": 4, \"index\": 3, \"width\": 450, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.21280v1/fig-004.webp\", \"caption\": \"Fig. 3. Hyperdimensional Encoding Unit (HEU)\", \"page\": 5, \"index\": 4, \"width\": 520, \"height\": 264}]"
motivation: 现有的持续学习方法依赖反向传播或大量样本，导致边缘设备在处理非平稳数据流时面临巨大的计算、内存和延迟挑战。
method: 提出一种硬件感知的HDC持续学习方法，利用量化CNN前端、统一样本内存管理和硬件高效的聚类合并策略，并在FPGA上实现流式数据流架构。
result: 在CORe50数据集上，ImageHD相比优化后的CPU和GPU基准，分别实现了高达40.4倍和4.84倍的加速，且能效提升分别达到383倍和105.1倍。
conclusion: ImageHD证明了结合超维计算与专用硬件加速器可以在严格的资源限制下实现高效、实时的边缘设备持续学习。
---

## 摘要
设备端持续学习（CL）对于在非平稳数据流上运行的边缘人工智能系统至关重要，但大多数现有方法依赖于反向传播或重样本分类器，导致了巨大的计算、内存和延迟开销。超维计算（HDC）通过快速、非迭代的在线更新提供了一种轻量级的替代方案。结合紧凑型卷积神经网络（CNN）特征提取器，HDC 能够利用强大的视觉表示实现高效的设备端自适应。然而，先前的基于 HDC 的持续学习系统通常依赖于多级存储层次结构和复杂的聚类管理，限制了其在资源受限硬件上的部署能力。我们提出了 ImageHD，一种基于 HDC 的视觉数据设备端持续学习 FPGA 加速器。ImageHD 针对严格延迟和片上内存限制下的流式持续学习，避免了昂贵的迭代优化。在算法层面，我们引入了一种硬件感知的持续学习方法，通过统一的样本存储器和硬件高效的聚类合并策略来限制类别样本，同时结合量化 CNN 前端以在不牺牲准确性的情况下减少部署开销。在系统层面，ImageHD 在 AMD Zynq ZCU104 FPGA 上实现为流式数据流架构，集成了 HDC 编码、相似度搜索和有界聚类管理，使用字打包二进制超向量在紧凑的片上资源预算内进行大规模并行位运算。在 CORe50 数据集上，ImageHD 相比优化的 CPU（GPU）基准测试分别实现了高达 40.4 倍（4.84 倍）的加速和 383 倍（105.1 倍）的能效提升，证明了基于 HDC 的持续学习在实时边缘人工智能中的实用性。

## Abstract
On-device continual learning (CL) is critical for edge AI systems operating on non-stationary data streams, but most existing methods rely on backpropagation or exemplar-heavy classifiers, incurring substantial compute, memory, and latency overheads. Hyperdimensional computing (HDC) offers a lightweight alternative through fast, non-iterative online updates. Combined with a compact convolutional neural network (CNN) feature extractor, HDC enables efficient on-device adaptation with strong visual representations. However, prior HDC-based CL systems often depend on multi-tier memory hierarchies and complex cluster management, limiting deployability on resource-constrained hardware.   We present ImageHD, an FPGA accelerator for on-device continual learning of visual data based on HDC. ImageHD targets streaming CL under strict latency and on-chip memory constraints, avoiding costly iterative optimization. At the algorithmic level, we introduce a hardware-aware CL method that bounds class exemplars through a unified exemplar memory and a hardware-efficient cluster merging strategy, while incorporating a quantized CNN front-end to reduce deployment overhead without sacrificing accuracy. At the system level, ImageHD is implemented as a streaming dataflow architecture on the AMD Zynq ZCU104 FPGA, integrating HDC encoding, similarity search, and bounded cluster management using word-packed binary hypervectors for massively parallel bitwise computation within tight on-chip resource budgets. On CORe50, ImageHD achieves up to 40.4x (4.84x) speedup and 383x (105.1x) energy efficiency over optimized CPU (GPU) baselines, demonstrating the practicality of HDC-enabled continual learning for real-time edge AI.

---

## 论文详细总结（自动生成）

这是一份关于论文《ImageHD: Energy-Efficient On-Device Continual Learning of Visual Representations via Hyperdimensional Computing》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**边缘设备上的实时持续学习（On-Device Continual Learning, CL）**。
*   **研究动机**：传统的持续学习方法（如基于反向传播的深度学习）在边缘端面临三大挑战：计算开销大（需迭代优化）、内存受限（难以存储大量历史样本以防止灾难性遗忘）以及高延迟。
*   **核心问题**：如何在资源极度受限的硬件（如FPGA）上，实现无需云端参与、低功耗、低延迟且能不断适应新视觉数据的在线学习系统？
*   **整体含义**：通过结合“轻量级量化CNN特征提取”与“超维计算（HDC）”，ImageHD 提供了一个端到端的硬件加速方案，证明了非迭代式的类脑计算模型在处理复杂视觉流任务时的优越性。

### 2. 方法论：核心思想与关键技术
ImageHD 的核心思想是将视觉特征映射到高维二进制空间，利用超维向量（Hypervector, HV）的准正交性进行快速分类和聚类。
*   **混合架构**：采用预训练并量化的 **MobileNetV2** 作为前端特征提取器（冻结参数），后端使用 **HDC 编码器**进行持续自适应。
*   **硬件优化算法**：
    *   **量化策略**：使用 INT8 量化 CNN，利用 HDC 对噪声的天然鲁棒性，在不损失精度的情况下降低内存占用。
    *   **统一样本内存**：取消了传统 HDC 方案中的多级存储（短期/长期内存），采用单一的有界聚类内存，简化控制逻辑。
    *   **硬件友好型聚类合并（Merge Clusters）**：将复杂度为 $O(N^3)$ 的谱聚类替换为改进的 **kMeans++** 算法。利用 Top-M 采样和 Hamming 距离计算，将复杂度降至线性 $O(N)$。
*   **FPGA 流式架构**：设计了专门的计算单元（PCU、DCU、HEU、HCU、CMU），支持字打包（Word-packed）位运算，实现了特征提取、HDC 编码、相似度搜索和聚类合并的全流水线化。

### 3. 实验设计
*   **数据集**：使用了三个主流的持续学习基准数据集：**CIFAR-10**、**CIFAR-100** 和 **CORe50**（专门用于物体识别的持续学习数据集）。
*   **对比基准（Baselines）**：
    *   **硬件平台**：高性能 CPU (AMD EPYC 7763)、工作站级 GPU (NVIDIA RTX A4000) 以及边缘 GPU (Jetson AGX Orin)。
    *   **算法对比**：对比了谱聚类（Spectral Clustering）与 kMeans++ 的性能，以及与先前的 FPGA 持续学习加速器（如 AILIS, ILOC）在延迟和能效上的差异。

### 4. 资源与算力
*   **FPGA 平台**：AMD Zynq UltraScale+ **ZCU104**（边缘级 FPGA）。
*   **算力消耗**：
    *   FPGA 运行频率为 **300 MHz**。
    *   资源占用：LUT 62%, BRAM 52%, DSP 7%, URAM 59%。
    *   **未明确说明训练时长**，因为该系统采用的是单次通过（Single-pass）的在线流式学习，不涉及传统意义上的多轮迭代训练时长。
*   **对比平台算力**：CPU (128核), GPU (6144 CUDA核)。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 FP32 到 INT8 的量化消融实验、不同聚类算法的精度/速度对比、以及跨平台的端到端性能评估。
*   **充分性评价**：实验设计较为充分。作者不仅验证了准确率（ACC、Purity、NMI），还深入分析了硬件指标（延迟、功耗、能效）。对比对象既包含了高性能服务器硬件，也包含了同级别的边缘嵌入式平台，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在 CORe50 数据集上，ImageHD 比优化后的 CPU 快 **40.4倍**，比 GPU 快 **4.84倍**。
*   **能效极高**：能效比 CPU 高出 **383倍**，比 GPU 高出 **105.1倍**。
*   **量化无损**：实验证明 HDC 能够容忍 INT8 量化带来的噪声，甚至在某些指标上略有提升，同时模型体积缩小了 3.7 倍。
*   **算法优化有效**：改进的 kMeans++ 聚类在 FPGA 上实现了比 GPU 谱聚类高达 **145.7倍** 的加速，且精度损失微乎其微。

### 7. 优点与亮点
*   **端到端集成**：这是首个在 FPGA 上实现“量化 CNN + 流式 HDC”完整闭环的异步持续学习加速器。
*   **软硬协同设计**：针对硬件特性修改了聚类算法（kMeans++ 替代谱聚类），显著降低了计算复杂度。
*   **高效内存管理**：通过字打包和位运算充分发挥了 FPGA 的并行处理能力，解决了持续学习中的内存爆炸问题。

### 8. 不足与局限
*   **特征提取器局限**：前端 CNN 是预训练且冻结的，这意味着系统只能在现有特征空间内进行聚类，无法像反向传播那样学习全新的底层视觉特征。
*   **任务类型单一**：目前仅验证了图像分类/聚类任务，对于更复杂的视觉任务（如目标检测或分割）的持续学习能力尚待验证。
*   **超参数敏感性**：HDC 的维度（如本实验中的 D=1024 或更高）和聚类合并的频率等超参数可能需要针对不同场景手动调优。

（完）
