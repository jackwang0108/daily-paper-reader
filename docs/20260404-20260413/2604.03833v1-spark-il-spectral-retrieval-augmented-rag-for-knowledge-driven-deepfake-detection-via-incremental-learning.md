---
title: "SPARK-IL: Spectral Retrieval-Augmented RAG for Knowledge-driven Deepfake Detection via Incremental Learning"
title_zh: SPARK-IL：通过增量学习实现知识驱动深度伪造检测的频谱检索增强 RAG 框架
authors: "Hessen Bougueffa Eutamene, Abdellah Zakaria Sellam, Abdelmalik Taleb-Ahmed, Abdenour Hadid"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03833v1"
tags: ["query:cs-cv"]
score: 9.0
evidence: 通过增量学习进行深度伪造检测
tldr: 针对AI生成图像检测中跨模型泛化难的问题，本文提出SPARK-IL框架。该方法利用频域特征的一致性，结合双路径频谱分析（语义与像素路径）和多频段傅里叶分解，通过KAN网络与混合专家模型处理特征。利用检索增强生成（RAG）技术在向量数据库中匹配签名进行预测，并引入增量学习策略以持续扩展检测能力，为知识驱动的深度伪造检测提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03833v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of detection accuracy and model efficiency on the UniversalFakeDetect benchmark. (a) Efficiency comparison in terms of number of parameters. (b) Mean accuracy across 19 generative models.\", \"page\": 3, \"index\": 1, \"width\": 725, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.03833v1/fig-002.webp\", \"caption\": \"Fig. 4: t-SNE visualization of SPARK-IL RAG embedding distributions for real (green) and fake (red) samples across different generative models.\", \"page\": 9, \"index\": 2, \"width\": 725, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2604.03833v1/fig-003.webp\", \"caption\": \"Fig. 2: SPARK-IL architecture. Dual spectral paths process ViT features and RGB pixels through multi-band KAN-FFT blocks. Cross-attention fuses the spectral embeddings, which are used for classification during training and stored in Milvus for retrieval-based inference.\", \"page\": 5, \"index\": 3, \"width\": 616, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.03833v1/fig-004.webp\", \"caption\": \"Fig. 3: Multi-band KAN-FFT block. Input features are transformed via FFT, converted to log-magnitude spectrum, partitioned into four frequency bands, processed through dedicated KAN layers, and fused into a spectral embedding.\", \"page\": 5, \"index\": 4, \"width\": 652, \"height\": 204}]"
motivation: 传统检测器难以应对未见的生成模型，而图像的频域特征相比像素特征在不同生成器间更具一致性。
method: 采用双路径ViT编码器提取语义与像素特征，通过多频段傅里叶分解和KAN-MoE进行融合，并利用Milvus数据库进行检索式推理与增量学习。
result: "在涵盖GAN、换脸及扩散模型等19种生成器的基准测试中，SPARK-IL实现了94.6%的平均准确率。"
conclusion: 结合频谱分析、检索增强和增量学习的策略显著增强了深度伪造检测的泛化性能和知识积累能力。
---

## 摘要
检测 AI 生成的图像仍然是一项重大挑战，因为在特定生成器上训练的检测器往往难以泛化到未见过的模型；然而，尽管像素级伪影在不同模型间各不相同，但频域特征表现出更强的一致性，为跨生成器检测提供了有前景的基础。为此，我们提出了 SPARK-IL，这是一个检索增强框架，它通过利用部分冻结的 ViT-L/14 编码器提取语义表示，并结合原始 RGB 像素嵌入的并行路径，将双路径频谱分析与增量学习相结合。两条路径均进行多波段傅里叶分解，分为四个频段，并由带有混合专家机制（MoE）的 Kolmogorov-Arnold 网络（KAN）分别处理以进行特定波段的变换，随后通过带有残差连接的交叉注意力机制融合生成的频谱嵌入。在推理阶段，该融合嵌入利用余弦相似度从 Milvus 数据库中检索 $k$ 个最近的标记特征，通过多数投票实现预测，同时增量学习策略负责扩展数据库并采用弹性权重巩固（EWC）来保留先前学习的变换。在包含 GAN、换脸和扩散方法等 19 种生成模型的 UniversalFakeDetect 基准测试中，SPARK-IL 达到了 94.6% 的平均准确率，代码将公开发布于 https://github.com/HessenUPHF/SPARK-IL。

## Abstract
Detecting AI-generated images remains a significant challenge because detectors trained on specific generators often fail to generalize to unseen models; however, while pixel-level artifacts vary across models, frequency-domain signatures exhibit greater consistency, providing a promising foundation for cross-generator detection. To address this, we propose SPARK-IL, a retrieval-augmented framework that combines dual-path spectral analysis with incremental learning by utilizing a partially frozen ViT-L/14 encoder for semantic representations alongside a parallel path for raw RGB pixel embeddings. Both paths undergo multi-band Fourier decomposition into four frequency bands, which are individually processed by Kolmogorov-Arnold Networks (KAN) with mixture-of-experts for band-specific transformations before the resulting spectral embeddings are fused via cross-attention with residual connections. During inference, this fused embedding retrieves the $k$ nearest labeled signatures from a Milvus database using cosine similarity to facilitate predictions via majority voting, while an incremental learning strategy expands the database and employs elastic weight consolidation to preserve previously learned transformations. Evaluated on the UniversalFakeDetect benchmark across 19 generative models -- including GANs, face-swapping, and diffusion methods -- SPARK-IL achieves a 94.6\% mean accuracy, with the code to be publicly released at https://github.com/HessenUPHF/SPARK-IL.

---

## 论文详细总结（自动生成）

这是一份关于论文《SPARK-IL: Spectral Retrieval-Augmented RAG for Knowledge-driven Deepfake Detection via Incremental Learning》的结构化深入分析总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的深度伪造（Deepfake）检测器通常针对特定生成模型训练，在面对未见的生成器（如从 GAN 转向扩散模型）时，由于像素级伪影的差异，泛化能力极差。
*   **研究动机**：
    *   **频域一致性**：尽管像素特征多变，但合成图像在频域（频谱）中往往表现出跨模型的一致性偏差。
    *   **静态模型局限**：传统参数化分类器是静态的，无法在不重新训练的情况下吸收新生成器的知识，且容易产生“灾难性遗忘”。
    *   **检索增强的优势**：引入 RAG（检索增强生成）思想，通过比对已知样本库而非固定决策边界来进行推理，能更好地适应开放世界。

### 2. 方法论：SPARK-IL 框架
*   **核心思想**：结合双路径频谱分析、非线性 KAN 网络变换以及基于向量数据库的检索推理，实现可增量学习的检测系统。
*   **关键技术细节**：
    1.  **双路径架构**：
        *   **语义路径**：使用部分冻结的 ViT-L/14 提取高层语义特征。
        *   **像素路径**：直接将原始 RGB 像素投影到嵌入空间，捕捉底层伪影。
    2.  **多频段 KAN-FFT 模块**：
        *   对特征进行一维快速傅里叶变换（FFT），计算对数幅度谱。
        *   将频谱划分为四个频段（低频到高频），每个频段由独立的 **Kolmogorov-Arnold Networks (KAN)** 配合混合专家（MoE）机制处理，以捕捉非线性频率变换。
    3.  **交叉注意力融合**：利用交叉注意力机制对像素级和语义级频谱嵌入进行对齐与融合。
    4.  **检索增强推理（RAG）**：
        *   训练完成后，将融合后的频谱签名存入 **Milvus 向量数据库**。
        *   推理时，通过余弦相似度检索 $K=5$ 个最近邻，采用**多数投票**决定真伪。
    5.  **增量学习策略**：采用经验回放、知识蒸馏和**弹性权重巩固（EWC）**，在引入新生成器数据时保护旧知识不被覆盖。

### 3. 实验设计
*   **数据集与基准**：使用 **UniversalFakeDetect** 这一权威基准，涵盖了 19 种生成模型（包括 ProGAN, CycleGAN, StyleGAN, LDM, GLIDE, DALL·E 等）。
*   **训练协议**：遵循 Wang 等人的标准，仅在 ProGAN 生成的图像上进行初始训练，以测试对其他 18 种未见模型的泛化性。
*   **对比方法**：对比了 14 种主流方法，包括 CNN-Spot, F3Net, UniFD, LGrad, FatFormer, C2P-CLIP 以及最新的 REVEAL 等。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA Quadro RTX 5000 GPU (16 GB VRAM)**。
*   **训练细节**：优化器为 Adam，训练 10 个 epoch，batch size 为 64。
*   **模型规模**：参数量约为 315M，在 224x224 输入下的计算量为 60.33 GFLOPs。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **跨模型测试**：在 19 个子数据集上进行了详尽测试。
    *   **消融实验**：系统地验证了像素 FFT、特征 FFT、KAN 网络以及检索机制各自的增益（共 6 组变体）。
    *   **超参数分析**：研究了检索邻居数量 $K$（从 1 到 20）对准确率的影响。
*   **充分性评价**：实验设计非常充分且客观。通过在单一模型训练、多模型测试的严苛条件下评估，证明了其卓越的泛化性能。

### 6. 主要结论与发现
*   **性能领先**：SPARK-IL 达到了 **94.60%** 的平均准确率，超过了之前最强的 REVEAL (93.90%) 和 UniFD (81.38%)。
*   **检索的威力**：消融实验显示，引入检索机制带来了最大的性能提升（+4.3%），证明非参数化推理在处理分布偏移时的有效性。
*   **频域价值**：双路径频谱分析比单一路径提升了约 3.5%-3.7% 的准确率，证实了像素级和语义级频域特征的互补性。

### 7. 优点与亮点
*   **创新性结合**：首次将 KAN 网络（一种新型神经网络架构）应用于深度伪造检测的频域分析。
*   **知识驱动**：通过 Milvus 数据库实现知识的物理积累，使模型具备“活”的记忆，能够通过简单的数据库更新来应对新威胁。
*   **效率平衡**：在参数量显著低于 FatFormer 等模型的情况下，实现了更高的精度，体现了架构设计的优越性。

### 8. 不足与局限
*   **局部伪影缺失**：全局 FFT 变换对局部区域的微小篡改（如仅修改眼睛或嘴部）可能不够敏感，未来需要引入空间感知或基于 Patch 的检索。
*   **计算开销**：虽然参数量适中，但 60.33 GFLOPs 的计算量在实时检测场景中仍有优化空间。
*   **频段划分**：目前的四频段划分是固定的，对于某些特定生成器，自适应的频率划分可能会更有效。

（完）
