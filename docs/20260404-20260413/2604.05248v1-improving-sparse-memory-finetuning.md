---
title: Improving Sparse Memory Finetuning
title_zh: 改进稀疏记忆微调
authors: "Satyam Goyal, Anirudh Kanchi, Garv Shah, Prakhar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05248v1"
tags: ["query:q3"]
score: 9.0
evidence: 使用稀疏记忆模块解决大语言模型持续学习中的灾难性遗忘
tldr: 针对大语言模型在持续学习中面临的灾难性遗忘问题，本文提出了一种改进的稀疏记忆微调（SMF）方法。研究者开发了开源流水线，将 Qwen-2.5-0.5B 模型改造为包含稀疏记忆模块的架构，并引入基于 KL 散度的槽位选择机制，优先更新包含“惊奇”信息的 Token。实验证明，该方法能在消费级硬件上高效获取新知识，同时显著减少对既有能力的干扰。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05248v1/fig-001.webp\", \"caption\": \"Figure 2: Recovery phase training loss on the instruction dataset (using TF-IDF).\", \"page\": 4, \"index\": 1, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-002.webp\", \"caption\": \"Figure 3: Sparse update mechanism: only retrieved key/value entries receive gradients.\", \"page\": 4, \"index\": 2, \"width\": 582, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-003.webp\", \"caption\": \"Figure 4: Finetuning performance across datasets. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse memory variants.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-004.webp\", \"caption\": \"Figure 5: Finetuning performance when adapting to SimpleQA. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse finetuning variants.\", \"page\": 6, \"index\": 4, \"width\": 826, \"height\": 229}]"
motivation: 传统的全量微调或 LoRA 方法因修改共享的密集表示，容易导致严重的灾难性遗忘，难以在不损害旧知识的情况下吸收新信息。
method: 通过开源流水线为预训练模型植入稀疏记忆模块，并采用基于 KL 散度的理论化机制来选择信息量大的 Token 进行记忆更新。
result: 实验表明，改造后的模型在获取新事实知识的同时，能有效保持模型原有的各项能力，验证了稀疏更新在实际场景中的有效性。
conclusion: 稀疏记忆微调为在消费级硬件上实现高效、低遗忘的持续学习提供了可行方案，证明了局部化更新是解决遗忘问题的关键方向。
---

## 摘要
大语言模型（LLMs）在训练后通常是静态的，但实际应用要求模型能够持续适应新知识，且不降低现有能力。标准的模型更新方法，如全量微调或参数高效微调方法（如 LoRA），面临着一个根本性的权衡：灾难性遗忘。这些方法修改了共享的密集表示，从而导致任务间的干扰。稀疏记忆微调（SMF）提供了一种极具前景的替代方案，它通过将更新定位到显式记忆层中的一小部分参数子集来实现。在本研究中，我们提出了一个开源流水线，用于为现有的预训练模型（Qwen-2.5-0.5B）改造稀疏记忆模块，从而在消费级硬件上实现有效的持续学习。我们通过引入一种基于 Kullback-Leibler (KL) 散度的、具有理论依据的槽位选择机制扩展了前人的工作，该机制优先为相对于背景分布而言具有信息“惊奇性”的标记进行记忆更新。我们的实验表明，改造后的模型能够获取新的事实知识，同时对原有能力的遗忘极小，在实际场景中验证了稀疏更新假设。

## Abstract
Large Language Models (LLMs) are typically static after training, yet real-world applications require continual adaptation to new knowledge without degrading existing capabilities. Standard approaches to updating models, like full finetuning or parameter-efficient methods (e.g., LoRA), face a fundamental trade-off: catastrophic forgetting. They modify shared dense representations, causing interference across tasks. Sparse Memory Finetuning (SMF) offers a promising alternative by localizing updates to a small subset of parameters in explicit memory layers. In this work, we present an open-source pipeline to retrofit existing pretrained models (Qwen-2.5-0.5B) with sparse memory modules, enabling effective continual learning on consumer hardware. We extend prior work by introducing a theoretically grounded slot-selection mechanism based on Kullback-Leibler (KL) divergence, which prioritizes memory updates for informationally "surprising" tokens relative to a background distribution. Our experiments demonstrate that our retrofitted models can acquire new factual knowledge with minimal forgetting of held-out capabilities, validating the sparse update hypothesis in a practical setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Sparse Memory Finetuning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）在部署后需要不断吸收新知识（持续学习），但传统的微调方法（如全量微调或 LoRA）会修改共享的密集表示，导致**灾难性遗忘**——即在学习新任务时严重损害旧知识。
*   **研究背景**：现有的参数高效微调（PEFT）虽然减少了参数量，但更新仍具有全局性影响。稀疏记忆微调（SMF）通过将更新局限在显式记忆层的特定槽位（Slots）来减少干扰，但现有的 SMF 方法依赖于简单的启发式规则（如 TF-IDF），且缺乏针对开源模型的通用改造流程。

### 2. 方法论：核心思想与技术细节
本文提出了一套将标准 Transformer 改造为稀疏记忆架构的流水线，并引入了信息论驱动的更新机制：
*   **三阶段改造流水线**：
    1.  **改造（Retrofitting）**：将预训练模型（如 Qwen-2.5-0.5B）中特定层（第 8, 12, 16 层）的密集前馈网络（FFN）替换为稀疏记忆层（包含 Key-Value 查找机制）。
    2.  **恢复（Healing）**：在通用指令数据集（OpenAssistant）上微调新加入的记忆参数，使随机初始化的记忆模块与预训练的残差流对齐，恢复模型的基础能力。
    3.  **任务特定微调**：在目标任务上进行稀疏更新。
*   **核心技术：KL 散度槽位选择**：
    *   **机制**：在微调时，模型只更新被访问到的记忆槽位。为了进一步减少干扰，作者提出基于 **KL 散度**的选择规则。
    *   **逻辑**：计算当前批次（Batch）的槽位使用分布与背景分布（预先在通用数据上统计）之间的 KL 散度。优先更新那些在当前任务中表现出“惊奇性”（即使用频率显著高于背景分布）的槽位，认为这些槽位承载了任务特有的新知识。
*   **梯度掩码（Gradient Masking）**：通过 Hook 机制，在优化器步进前将未选中槽位的梯度清零，确保只有极少数参数发生变化。

### 3. 实验设计
*   **数据集与场景**：
    *   **目标任务（学习新知识）**：TriviaQA（事实问答）、SimpleQA（文档级问答）、HellaSwag。
    *   **保留任务（测试遗忘）**：GSM8K（数学推理）、NaturalQuestions（通用问答）。
*   **基准（Benchmark）与对比方法**：
    *   **全量密集微调（Full Finetuning）**：作为传统方法的基准。
    *   **SMF (TF-IDF)**：使用传统的词频-逆文档频率启发式规则选择槽位。
    *   **SMF (KL-Divergence)**：本文提出的改进方法。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen-2.5-0.5B-Instruct（约 5 亿参数）。
*   **算力说明**：论文提到该方法旨在使持续学习能在“消费级硬件”上运行。
*   **具体细节**：文中**未明确给出**具体的 GPU 型号、数量或总训练时长，但提到恢复阶段使用了 20,000 条样本，任务微调使用了 1,000 条样本，属于轻量级微调范畴。

### 5. 实验数量与充分性
*   **实验规模**：作者在两个主要场景（TriviaQA 和 SimpleQA 适配）下进行了对比实验，并监控了多个维度的性能指标（F1 分数、Loss）。
*   **充分性评价**：
    *   **优点**：实验涵盖了“可塑性”（学习新任务的能力）和“稳定性”（保留旧知识的能力）的双重评估，对比了密集微调与稀疏微调的差异。
    *   **不足**：实验主要集中在 0.5B 的小模型上，缺乏在更大规模模型（如 7B 或 70B）上的验证；实验任务种类相对有限，主要集中在事实性问答和基础推理。

### 6. 主要结论与发现
*   **稀疏更新的优越性**：相比全量微调，稀疏记忆微调在学习新事实时速度更快，且对 GSM8K 等推理能力的损害显著降低。
*   **KL 机制的有效性**：基于 KL 散度的槽位选择在处理噪声较大的数据（如 SimpleQA）时表现出更好的稳定性，能有效防止模型产生过度漂移。
*   **权衡关系**：实验发现存在“稳定性-可塑性”的权衡，KL 机制在某些强信号任务下可能会因为约束过紧而略微减缓学习速度，但在防止遗忘方面更具优势。

### 7. 优点与亮点
*   **开源贡献**：提供了将现有 Transformer 架构改造为记忆增强架构的通用流水线，具有很强的实用价值。
*   **理论支撑**：将槽位选择从简单的启发式规则提升到信息论高度（KL 散度），为“哪些参数该更新”提供了更科学的解释。
*   **高效性**：证明了局部化更新（Localized Updates）是解决大模型持续学习问题的关键路径。

### 8. 不足与局限
*   **模型规模局限**：仅在 0.5B 模型上验证，尚未证明该方法在超大规模参数下的扩展性（Scaling）。
*   **改造开销**：虽然微调高效，但初始的“改造”和“恢复（Healing）”阶段仍需要一定的通用数据和计算量。
*   **超参数敏感性**：KL 散度中的平滑因子和背景分布的统计质量可能会对最终效果产生较大影响，文中未深入讨论其鲁棒性。

（完）
