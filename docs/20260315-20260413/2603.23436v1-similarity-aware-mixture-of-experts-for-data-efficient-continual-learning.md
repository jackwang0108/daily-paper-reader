---
title: Similarity-Aware Mixture-of-Experts for Data-Efficient Continual Learning
title_zh: 面向数据高效持续学习的相似性感知混合专家模型
authors: "Connor Mclaughlin, Nigel Lee, Lili Su"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23436v1"
tags: ["query:cs-cv"]
score: 9.0
evidence: 用于模型持续适配的持续学习框架
tldr: 针对持续学习中数据稀缺和任务重叠导致的负迁移问题，本文提出一种相似性感知的混合专家（MoE）框架。该方法通过增量全局池化和实例级提示掩码技术，在预训练模型基础上实现了高效的知识迁移与任务解耦。实验证明，该方法在不同数据量和任务相似度下均能显著提升样本效率，有效应对复杂现实场景下的持续学习挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23436v1/fig-001.webp\", \"caption\": \"Figure 4: Impact of training data availability on routing method performance on ImageNet-R.\", \"page\": 6, \"index\": 1, \"width\": 489, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2603.23436v1/fig-002.webp\", \"caption\": \"Figure 5: Comparison of prompt allocation during training (top) and inference time (bottom) on ImageNet-R (10-Tasks).\", \"page\": 8, \"index\": 2, \"width\": 490, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2603.23436v1/fig-003.webp\", \"caption\": \"Table 5: Overall performance of continual learning. We present the final average accuracy (FAA), cumulative average accuracy (CAA) and final forgetting measure (FFM) with ± standard deviation, over three runs of different random seeds and task splits.\", \"page\": 8, \"index\": 3, \"width\": 940, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.23436v1/fig-004.webp\", \"caption\": \"Figure 3: Final Average Accuracy on ImageNet-R across varying training set sizes (expressed as a fraction of the full dataset). While global pooling enables effective transfer in scarce-data regimes, it suffers from interference as data availability increases. Incremental expansion mitigates this by reducing routing errors in early learning stage, simultaneously enabling the refinement of consolidated knowledge and the allocation of fresh capacity for novel knowledge.\", \"page\": 4, \"index\": 4, \"width\": 489, \"height\": 353}]"
motivation: 旨在解决持续学习中因单任务数据稀缺和任务间无序重叠导致的知识迁移困难及负迁移干扰问题。
method: 提出一种基于预训练模型的自适应MoE框架，结合增量全局池化减少关联噪声，并利用实例级提示掩码区分分布内外的样本。
result: 在多种数据规模和任务相似度设置下的实验表明，该方法显著提高了模型的样本效率和适应能力。
conclusion: 该研究通过建立任务间的相似性感知机制，为数据受限且任务关系复杂的持续学习场景提供了一种稳健且高效的解决方案。
---

## 摘要
由于结构化或非结构化的现实世界动态变化，机器学习模型在部署后通常需要适应新数据。持续学习（CL）框架能够实现模型的持续适应，但大多数现有方法要么假设每个任务包含足够的数据样本，要么假设学习任务之间互不重叠。在本文中，我们研究了一种更通用的设定，即每个任务的数据集可能有限，且任务之间可能在没有先验知识的情况下以任意方式重叠。这种通用设定由于两个原因而更具挑战性：一方面，数据稀缺性要求对通用知识进行有效的语境化，并在任务间进行高效的知识迁移；另一方面，非结构化的任务重叠很容易导致负向知识迁移。为了应对上述挑战，我们提出了一种基于预训练模型的自适应混合专家（MoE）框架，该框架能逐步建立任务间的相似性感知。我们的设计包含两个创新的算法组件：增量全局池化和实例级提示掩码。前者通过随时间逐渐引入提示来减轻提示关联噪声；后者将输入的任务样本分解为与当前提示对齐的样本（分布内）和需要新提示的样本（分布外）。综上所述，我们的设计在每个任务数据稀缺的情况下，策略性地利用了潜在的任务重叠，同时积极防止负向相互干扰。在不同数据量和任务间相似度下的实验表明，我们的方法提高了样本效率，并具有广泛的适用性。

## Abstract
Machine learning models often need to adapt to new data after deployment due to structured or unstructured real-world dynamics. The Continual Learning (CL) framework enables continuous model adaptation, but most existing approaches either assume each task contains sufficiently many data samples or that the learning tasks are non-overlapping. In this paper, we address the more general setting where each task may have a limited dataset, and tasks may overlap in an arbitrary manner without a priori knowledge. This general setting is substantially more challenging for two reasons. On the one hand, data scarcity necessitates effective contextualization of general knowledge and efficient knowledge transfer across tasks. On the other hand, unstructured task overlapping can easily result in negative knowledge transfer. To address the above challenges, we propose an adaptive mixture-of-experts (MoE) framework over pre-trained models that progressively establishes similarity awareness among tasks. Our design contains two innovative algorithmic components: incremental global pooling and instance-wise prompt masking. The former mitigates prompt association noise through gradual prompt introduction over time. The latter decomposes incoming task samples into those aligning with current prompts (in-distribution) and those requiring new prompts (out-of-distribution). Together, our design strategically leverages potential task overlaps while actively preventing negative mutual interference in the presence of per-task data scarcity. Experiments across varying data volumes and inter-task similarity show that our method enhances sample efficiency and is broadly applicable.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**相似性感知混合专家模型（Similarity-Aware Mixture-of-Experts）**的方法，旨在解决持续学习（Continual Learning, CL）在现实应用中面临的两个核心挑战：**单任务数据稀缺**和**任务间的无序重叠**。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的持续学习研究通常假设每个任务有充足的训练样本，且任务边界清晰（不重叠）。然而，在现实场景（如欺诈检测或演进中的图像分类）中，新任务往往只有少量样本（数据稀缺），且不同任务间可能存在相似或重复的概念（任务重叠）。
*   **数据稀缺**：导致模型难以从头学习任务特征，必须依赖预训练知识的迁移。
*   **任务重叠**：虽然提供了知识共享的机会，但若处理不当，会导致“负迁移”（Negative Transfer），即旧知识干扰新任务的学习，或新学习破坏旧知识。
*   **研究目标**：在不存储旧数据（Rehearsal-free）的前提下，通过自适应的提示（Prompt）路由机制，实现高效的知识复用并抑制干扰。

### 2. 方法论：核心思想与关键技术
论文提出了一种基于预训练视觉 Transformer（ViT）的自适应混合专家框架，核心包含两个创新组件：

*   **增量全局池化 (Incremental Global Pooling)**：
    *   不同于固定大小的全局池化或完全隔离的任务特定提示，该方法维护一个随任务增加而动态增长的提示池。
    *   新任务开始时，引入新的提示并初始化为与现有提示正交，以减少初始干扰。
    *   这种机制在早期阶段限制了搜索空间，降低了路由噪声，有助于在数据稀缺时精炼已有知识。

*   **实例级提示掩码 (Instance-wise Prompt Masking)**：
    *   **核心思想**：将持续学习视为一种“持续的离群检测（OOD Detection）”过程。
    *   **技术细节**：利用**相对马氏距离 (Relative Mahalanobis Distance, RMD)** 来评估输入样本 $x$ 与已知任务分布的相似度。
    *   **路由逻辑**：
        *   如果样本被判定为“分布内（ID）”，则掩码掉新提示，强制其路由至旧提示池以复用知识。
        *   如果样本被判定为“分布外（OOD）”，则路由至新提示以学习新概念。
    *   **公式应用**：通过在线更新类均值和全局协方差矩阵，计算样本到各类的 RMD，并根据分位数设定动态阈值 $\tau_t$。

### 3. 实验设计
*   **数据集与场景**：
    *   **标准 Benchmark**：Split CIFAR-100, Split ImageNet-R。
    *   **领域偏移与长序列**：5-Datasets (CIFAR-10, MNIST等混合), DomainNet (不同视觉风格)。
    *   **混合任务流**：M-EMNIST, M-CifarCeleb（包含相似和不相似任务的混合序列）。
*   **对比方法**：对比了 5 种最先进的提示学习方法：L2P, DualPrompt, S-Prompt++, CODA-Prompt, 以及 HiDe-Prompt。
*   **评价指标**：最终平均准确率 (FAA)、累积平均准确率 (CAA) 和最终遗忘度 (FFM)。

### 4. 资源与算力
*   **模型骨干**：使用在 ImageNet-21k 上预训练的 ViT-B/16。
*   **超参数**：Batch size 为 128，使用 Adam 优化器，提示长度 $L_p=5$，OOD 分位数 $q=0.95$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但由于其采用冻结主干网络仅训练少量 Prompt 的策略，通常这类方法的算力需求远低于全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流数据集，并在 5、10、15 个任务的不同尺度下进行了测试。
*   **消融实验**：详细分析了数据比例（从 1% 到 100%）对性能的影响，并可视化了训练与推理阶段的提示使用率（Usage Gap）。
*   **客观性**：实验采用了三次随机种子取平均值的方式，并提供了标准差。通过引入“混合任务流”和“数据稀缺度分析”，实验设计比常规 CL 论文更贴近实际应用，具有较高的说服力。

### 6. 主要结论与发现
*   **数据稀缺时优势巨大**：在仅有 1% 或 5% 训练数据的情况下，该方法显著优于全局池化和任务特定方法，证明了相似性感知路由在低数据制度下的有效性。
*   **解决路由失调**：发现传统方法在训练和推理时的 Prompt 选择存在巨大差异（Usage Gap），而本方法通过 OOD 掩码缩小了这一差距。
*   **任务分离能力强**：通过 AUC 指标验证，RMD 机制比传统的“可学习键值（Learnable Keys）”能更准确地识别任务切换。

### 7. 优点（亮点）
*   **跨学科结合**：创新性地将 OOD 检测技术（RMD）引入持续学习的提示路由中，解决了 Prompt 选取的盲目性。
*   **灵活性**：该路由策略是“架构无关”的，可以插入到现有的各种 Prompt-based CL 方法中。
*   **高效复用**：在处理混合相似任务时，能够精准复用旧 Prompt，减少了参数冗余。

### 8. 不足与局限
*   **依赖预训练质量**：由于 RMD 计算依赖于冻结主干提取的特征，如果预训练模型在某些特定领域（如医学影像）表现不佳，其 OOD 检测的准确性可能会受限。
*   **计算开销**：虽然是增量更新，但维护类均值和协方差矩阵（尤其是高维特征下）仍会带来额外的内存和计算负担。
*   **任务类型限制**：目前实验主要集中在分类任务，尚未在回归、检测或生成式任务中验证其通用性。

（完）
