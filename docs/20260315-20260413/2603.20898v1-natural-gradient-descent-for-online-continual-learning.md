---
title: Natural Gradient Descent for Online Continual Learning
title_zh: 面向在线持续学习的自然梯度下降
authors: "Joe Khawand, David Colliaux"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20898v1"
tags: ["query:cs-cv"]
score: 10.0
evidence: 用于图像分类在线持续学习的自然梯度下降法
tldr: 在线持续学习（OCL）面临非独立同分布数据流带来的灾难性遗忘和收敛缓慢挑战。本文提出一种基于自然梯度下降（NGD）的新型训练方法，通过克罗内克因子近似曲率（KFAC）来高效近似费雪信息矩阵。该方法能显著提升现有OCL算法在Split CIFAR-100等主流数据集上的性能，为解决在线设置下的快速收敛问题提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20898v1/fig-001.webp\", \"caption\": \"Figure 1: Average End Accuracy on Split-CIFAR-100 [20] for 3 buffer sizes (1k-5k-10k) using ER [33] in combination with OCL tricks [25].\", \"page\": 2, \"index\": 1, \"width\": 829, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2603.20898v1/fig-002.webp\", \"caption\": \"Figure 2: Average End Accuracy by trick, averaged on all datasets in the OCI setting with regards to the damping parameter ϵ.\", \"page\": 10, \"index\": 2, \"width\": 826, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2603.20898v1/fig-003.webp\", \"caption\": \"Table 3: Average End Accuracy for NonStationary-MiniImageNet (Noise, Occlusion, Blur) and CORe50-NI in the ODI setting.\", \"page\": 8, \"index\": 3, \"width\": 827, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2603.20898v1/fig-004.webp\", \"caption\": \"Table 2: End Average Accuracy of methods and tricks with or without NGD-KFAC for the OCI setting on Split CIFAR-100.\", \"page\": 8, \"index\": 4, \"width\": 832, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2603.20898v1/fig-005.webp\", \"caption\": \"Table 4: End Average Accuracy of methods and tricks with or without NGD-KFAC for the OCI setting on Split MiniImageNet.\", \"page\": 9, \"index\": 5, \"width\": 832, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.20898v1/fig-006.webp\", \"caption\": \"Table 5: End Average Accuracy of methods and tricks with or without NGD-KFAC for the OCI setting on CORe50-NC.\", \"page\": 9, \"index\": 6, \"width\": 832, \"height\": 250}]"
motivation: 旨在解决在线持续学习中模型因数据非独立同分布而难以快速收敛并克服灾难性遗忘的问题。
method: 引入自然梯度下降优化器，并利用克罗内克因子近似曲率（KFAC）对费雪信息矩阵进行近似计算。
result: 在Split CIFAR-100、CORE50和Split miniImageNet等多个基准数据集上，该方法显著增强了现有OCL算法的性能。
conclusion: 自然梯度下降结合KFAC近似是一种提升在线持续学习模型性能和收敛效率的通用且有效的优化策略。
---

## 摘要
用于图像分类的在线持续学习 (OCL) 是持续学习中一个具有挑战性的子集，其重点是在不假设数据独立同分布 (i.i.d.) 的情况下对流式图像进行分类。在这种背景下，主要的挑战是防止灾难性遗忘，即模型在学习新任务时，在先前任务上的性能会发生恶化。尽管已经提出了各种策略来解决这一问题，但在在线设置中实现快速收敛仍然是一个重大挑战。在这项工作中，我们介绍了一种训练 OCL 模型的新方法，该方法利用自然梯度下降优化器，通过 Kronecker 因子分解近似曲率 (KFAC) 结合了 Fisher 信息矩阵 (FIM) 的近似。该方法在 Split CIFAR-100、CORE50 和 Split miniImageNet 等数据集上，在所有 OCL 方法中都表现出显著的性能提升，特别是与现有的 OCL 技巧结合使用时。

## Abstract
Online Continual Learning (OCL) for image classification represents a challenging subset of Continual Learning, focusing on classifying images from a stream without assuming data independence and identical distribution (i.i.d). The primary challenge in this context is to prevent catastrophic forgetting, where the model's performance on previous tasks deteriorates as it learns new ones. Although various strategies have been proposed to address this issue, achieving rapid convergence remains a significant challenge in the online setting. In this work, we introduce a novel approach to training OCL models that utilizes the Natural Gradient Descent optimizer, incorporating an approximation of the Fisher Information Matrix (FIM) through Kronecker Factored Approximate Curvature (KFAC). This method demonstrates substantial improvements in performance across all OCL methods, particularly when combined with existing OCL tricks, on datasets such as Split CIFAR-100, CORE50, and Split miniImageNet.

---

## 论文详细总结（自动生成）

这是一份关于论文《Natural Gradient Descent for Online Continual Learning》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在线持续学习（Online Continual Learning, OCL）要求模型从非独立同分布（non-i.i.d.）的数据流中学习，且每个样本仅能观察一次（one-pass）。
*   **核心问题**：OCL 面临两大挑战：一是**灾难性遗忘**（Catastrophic Forgetting），即学习新任务时遗忘旧知识；二是**收敛速度慢**，在单次遍历和极小批次（Batch Size）的限制下，传统的一阶优化器（如 SGD）难以快速达到理想性能。
*   **研究动机**：探索二阶优化方法在 OCL 中的潜力，利用概率分布空间的几何特性加速模型收敛并提升泛化能力。

### 2. 论文提出的方法论
*   **核心思想**：引入**自然梯度下降（Natural Gradient Descent, NGD）**优化器替代传统的 SGD。NGD 通过费雪信息矩阵（Fisher Information Matrix, FIM）对梯度进行调整，使其在参数更新时遵循概率分布的最速下降方向，而非欧几里得空间的最速下降方向。
*   **关键技术细节**：
    *   **KFAC 近似**：由于 FIM 的维度巨大（参数量的平方），直接计算和求逆不可行。论文采用 **Kronecker 因子近似曲率（KFAC）**，将 FIM 近似为两个小矩阵的克罗内克积，大幅降低了计算和存储开销。
    *   **数值稳定性（阻尼技术）**：OCL 处于持续的“探索阶段”，二阶近似容易失效。论文采用了 **Tikhonov 正则化（Damping）**，通过在 FIM 近似中加入阻尼参数 $\lambda$ 来防止更新步长过大导致的崩溃。
    *   **更新规则**：$\theta_{t+1} = \theta_{t} - \alpha \cdot F^{-1}(\theta_t) \cdot \nabla L(\theta_t)$，其中 $F$ 是经过 KFAC 近似和阻尼处理后的 FIM。

### 3. 实验设计
*   **数据集与场景**：
    *   **在线类增量（OCI）**：Split CIFAR-100, Split MiniImageNet, CORe50-NC。
    *   **在线域增量（ODI）**：NonStationary-MiniImageNet（包含噪声、模糊、遮挡三种非平稳场景）, CORe50-NI。
*   **Benchmark 与对比方法**：
    *   **基础方法**：ER（经验回放）、A-GEM、MIR（最大干扰检索）、GSS（梯度选择）。
    *   **OCL 技巧（Tricks）**：Labels Trick、Nearest Class Mean (NCM) 分类器、Separated Softmax (SS)、Review Trick (RV)。
    *   **基准线**：Finetune（朴素训练，下限）、Offline（离线多轮训练，上限）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：使用了 ResNet18 作为骨干网络，学习率固定为 0.1，Batch Size 为 10（模拟真实的在线低延迟场景）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个主流 OCL 基准数据集。
    *   针对 3 种不同的内存缓冲区大小（1k, 5k, 10k）进行了测试。
    *   每组实验均独立运行 **10 次**并提供均值和标准差，确保了结果的统计显著性。
*   **充分性评价**：实验设计较为充分，不仅对比了基础算法，还深入探讨了 NGD-KFAC 与各种现有“优化技巧”的协同效应，并进行了针对阻尼参数 $\epsilon$ 的消融实验。

### 6. 论文的主要结论与发现
*   **性能提升**：NGD-KFAC 在几乎所有数据集和方法组合上都带来了显著的准确率提升。在 OCI 模式下，平均准确率提升了约 **2.62%**。
*   **遗忘权衡**：虽然 NGD-KFAC 提升了最终准确率，但也略微增加了遗忘率（Forgetting）。作者认为这是因为 NGD 加快了新任务的收敛，导致参数对当前任务的偏置更强，但整体性能增益覆盖了遗忘带来的损失。
*   **最佳组合**：NGD-KFAC 与 **NCM（最近类均值）** 技巧结合时效果最为突出，既能显著提升准确率，又能有效抑制遗忘。
*   **正则化敏感性**：实验发现较高的阻尼系数（如 1.0）在 OCL 场景下表现最好，这反映了在线学习中曲率估计的高不确定性。

### 7. 优点
*   **创新性**：首次将 NGD-KFAC 这种高效的二阶优化方法系统性地应用于在线持续学习领域。
*   **通用性**：该方法作为一个优化器插件，可以无缝集成到现有的各种 OCL 框架（如 ER, MIR 等）中，具有很强的实用价值。
*   **实验严谨**：通过 10 次重复实验和多维度的消融研究，证明了二阶优化在处理非平稳数据流时的鲁棒性。

### 8. 不足与局限
*   **计算开销未量化**：虽然 KFAC 降低了二阶导的计算量，但相比 SGD 仍有额外的矩阵运算开销。论文未给出训练时间或推理延迟的对比数据。
*   **遗忘率上升**：NGD 带来的快速收敛在某种程度上加剧了对旧任务的干扰，如何平衡“快速学习”与“长期记忆”仍需进一步探索。
*   **超参数依赖**：实验显示性能对阻尼参数 $\epsilon$ 较为敏感，在不同任务中可能需要精细调优。

（完）
