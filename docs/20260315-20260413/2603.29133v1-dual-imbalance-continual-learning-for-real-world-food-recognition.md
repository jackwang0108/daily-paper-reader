---
title: Dual-Imbalance Continual Learning for Real-World Food Recognition
title_zh: 面向真实场景食物识别的双重不平衡持续学习
authors: "Xiaoyan Zhang, Jiangpeng He"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29133v1"
tags: ["query:cs-cv"]
score: 10.0
evidence: 持续食物识别，新类别随时间顺序引入
tldr: 针对真实场景下食物识别面临的类别样本长尾分布和增量步骤类别数不均的“双重不平衡”挑战，本文提出了DIME框架。该框架利用轻量化适配器进行参数高效微调，并通过类别计数引导的光谱合并策略及秩阈值调制机制，实现了知识的稳定整合与高效推理，在多个基准数据集上显著提升了性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29133v1/fig-001.webp\", \"caption\": \"Table 1. Last task accuracy (AT ), average accuracy (Ā), and weighted average accuracy (wĀ) on VFN186-LT, VFN186-Insulin, VFN186T2D, and Food101-LT with ρ = 0.01. Results are averaged over 5 runs. The best results are in bold and the second-best results are underlined.\", \"page\": 7, \"index\": 1, \"width\": 979, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.29133v1/fig-002.webp\", \"caption\": \"Figure 3. Performance comparison under different step-imbalance ratios ρ on VFN186-LT and Food101-LT with T = 10 incremental steps. Our method consistently achieves higher and more stable performance across tasks under all imbalance settings.\", \"page\": 7, \"index\": 2, \"width\": 1036, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2603.29133v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of DIME. (a) Sequential food datasets are learned with lightweight adapters on a frozen backbone using Balanced Softmax. After each step, the new adapter is merged with the accumulated base adapter to produce a unified model. (b) The merging module performs spectral alignment via SVD, class-count guided weighting, and rank-wise threshold modulation.\", \"page\": 4, \"index\": 3, \"width\": 980, \"height\": 361}]"
motivation: 现有持续学习方法忽略了真实场景中食物类别样本分布不均以及各增量阶段新类别数量差异巨大的“双重不平衡”问题。
method: 提出DIME框架，采用参数高效微调学习各任务适配器，并结合类别计数引导的光谱合并与秩阈值调制技术进行模型整合。
result: "在真实的增量长尾食物识别基准测试中，该方法比最强的持续学习基准模型性能提升了3%以上。"
conclusion: DIME通过自适应合并策略有效解决了双重不平衡问题，为现实世界中动态变化的食物识别任务提供了一种高效且稳健的解决方案。
---

## 摘要
在现实世界的饮食记录场景中，视觉食物识别天然地表现出严重的数据不平衡，即少数食物类别频繁出现，而许多其他类别极少出现，从而导致长尾类别分布。在实践中，食物识别系统通常运行在持续学习设置下，新类别随时间推移顺序引入。然而，现有研究通常假设每个增量步骤引入的新食物类别数量相似，这在现实世界中很少发生，因为不同步骤中新观察到的类别数量可能存在显著差异，导致学习动态高度不均匀。因此，持续食物识别表现出双重不平衡：每个食物类别内的样本不平衡，以及每个增量学习步骤中待学习的新食物类别数量不平衡。在这项工作中，我们引入了 DIME，一种用于持续食物识别的双重不平衡感知适配器合并（Adapter Merging）框架。DIME 利用参数高效微调为每个任务学习轻量级适配器，并通过一种类别计数引导的光谱合并策略逐步整合它们。此外，一种按秩阈值调制机制通过保留主导知识并允许自适应更新，进一步稳定了合并过程。最终模型在推理时仅维持单个合并后的适配器，从而实现高效部署，无需累积特定于任务的模块。在现实长尾食物基准及步骤不平衡设置下的实验表明，所提方法比现有的最强持续学习基准模型性能提升了 3% 以上。代码可在 https://github.com/xiaoyanzhang1/DIME 获取。

## Abstract
Visual food recognition in real-world dietary logging scenarios naturally exhibits severe data imbalance, where a small number of food categories appear frequently while many others occur rarely, resulting in long-tailed class distributions. In practice, food recognition systems often operate in a continual learning setting, where new categories are introduced sequentially over time. However, existing studies typically assume that each incremental step introduces a similar number of new food classes, which rarely happens in real world where the number of newly observed categories can vary significantly across steps, leading to highly uneven learning dynamics. As a result, continual food recognition exhibits a dual imbalance: imbalanced samples within each food class and imbalanced numbers of new food classes to learn at each incremental learning step. In this work, we introduce DIME, a Dual-Imbalance-aware Adapter Merging framework for continual food recognition. DIME learns lightweight adapters for each task using parameter-efficient fine-tuning and progressively integrates them through a class-count guided spectral merging strategy. A rank-wise threshold modulation mechanism further stabilizes the merging process by preserving dominant knowledge while allowing adaptive updates. The resulting model maintains a single merged adapter for inference, enabling efficient deployment without accumulating task-specific modules. Experiments on realistic long-tailed food benchmarks under our step-imbalanced setup show that the proposed method consistently improves by more than 3% over the strongest existing continual learning baselines. Code is available at https://github.com/xiaoyanzhang1/DIME.

---

## 论文详细总结（自动生成）

以下是对论文《Dual-Imbalance Continual Learning for Real-World Food Recognition》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**真实场景下食物识别系统**在持续学习（Continual Learning）中面临的独特挑战。作者指出，现有的持续学习研究通常假设每个增量步骤引入的类别数量是均衡的，但在现实饮食记录中，数据呈现出**“双重不平衡”（Dual-Imbalance）**特征：
*   **类别不平衡（Class Imbalance）：** 食物消费遵循长尾分布，少数常见食物占据大部分样本，而大量罕见食物样本极少。
*   **步骤不平衡（Step Imbalance）：** 随着时间推移，每个增量阶段新出现的食物类别数量差异巨大（例如，有时一次引入几十种新菜，有时只引入几种）。
这种双重不平衡会导致学习动态高度不稳定，小任务或尾部类别的噪声梯度容易破坏模型已有的知识表示。

### 2. 方法论：DIME 框架
作者提出了 **DIME（Dual-Imbalance-aware Adapter Merging）** 框架，旨在通过参数高效微调（PEFT）和智能模型合并来解决上述问题。其核心流程如下：
*   **基础架构：** 采用冻结的预训练 Vision Transformer (ViT) 作为骨干网络，仅在每个 Transformer 块中插入轻量级的 **MLP 适配器（Adapter）**。
*   **内任务平衡优化：** 在训练当前任务时，使用 **Balanced Softmax Loss**。该损失函数通过引入类别频率先验来调整 Logits，从而缓解长尾分布带来的预测偏差。
*   **光谱合并（Spectral Merging）：** 任务完成后，不直接平均参数，而是将新旧适配器矩阵拼接后进行 **奇异值分解（SVD）**。这能将知识对齐到共享的正交基空间中，减少不同任务间的干扰。
*   **类别计数权重（Class-Count Weighting）：** 根据历史类别总数与当前新类别数量的比例，动态计算合并权重，确保大任务提供更稳定的更新，小任务提供自适应补充。
*   **秩阈值调制（Rank-wise Threshold Modulation）：** 这是核心创新点。根据奇异值大小对更新方向进行分段处理：
    *   **头部方向（大奇异值）：** 代表主导视觉模式，给予较小的更新系数以保持稳定性。
    *   **尾部方向（小奇异值）：** 代表细微特征，给予较大的更新系数以允许模型吸收新知识。
*   **推理效率：** 最终模型将所有知识整合进**单个适配器**，推理时无需存储多个任务模块或进行复杂的路由选择。

### 3. 实验设计
*   **数据集：** 使用了四个具有挑战性的长尾食物基准：**VFN186-LT**、**VFN186-Insulin**（胰岛素用户特定）、**VFN186-T2D**（2型糖尿病用户特定）以及经典的 **Food101-LT**。
*   **Benchmark 设置：** 引入了步骤不平衡因子 $\rho$，模拟从轻微到极端的步骤不平衡场景（$\rho$ 越小，步骤间类别数差异越大）。
*   **对比方法：** 涵盖了 9 种先进的持续学习方法，包括 SLCA、InfLoRA、EASE、SEMA、CL-LoRA、ACMap、MOS 和 TUNA 等。
*   **评价指标：** 除了最终准确率 ($A_T$) 和平均准确率 ($\bar{A}$)，还专门引入了**加权平均准确率 ($w\bar{A}$)**，以更公平地衡量步骤不平衡下的整体表现。

### 4. 资源与算力
*   **硬件：** 实验在一台 **NVIDIA L40S GPU** 上完成。
*   **软件：** 基于 PyTorch 框架。
*   **训练细节：** 每个任务训练 20 个 Epoch，使用 SGD 优化器，学习率为 0.07，Batch Size 为 16。适配器的隐藏层维度设为 64。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **跨数据集验证：** 在 4 个不同的食物数据集上验证了通用性。
*   **不平衡强度测试：** 测试了 $\rho \in \{0.1, 0.05, 0.01, 0.001\}$ 四种不同程度的步骤不平衡，证明了 DIME 在极端不平衡下的稳健性。
*   **消融实验：** 逐步验证了光谱合并 (SM)、类别计数权重 (CCW)、秩阈值调制 (RTM) 和 Balanced Softmax (BSM) 的有效性。
*   **效率分析：** 对比了推理时间、FLOPs 和参数量，证明了其在保持高性能的同时具有极高的部署效率。
*   **敏感性分析：** 探讨了秩阈值比例 $r_h$ 和调制系数 $\gamma$ 对结果的影响。
实验设计客观、公平，对比基准涵盖了最新的主流方法，数据量充足。

### 6. 主要结论与发现
*   **性能领先：** DIME 在所有数据集和指标上均显著优于现有方法，在 $A_T$ 指标上通常比最强基准高出 **3% 以上**。
*   **稳定性：** 在步骤不平衡极其严重时（如 $\rho=0.001$），DIME 的性能曲线比其他方法更平滑，证明其能有效抑制小任务带来的噪声干扰。
*   **任务规模适应性：** 任务分析显示，DIME 在保持大任务知识的同时，对小任务的识别能力也优于大多数对比方法。

### 7. 优点
*   **问题定义精准：** 首次明确提出了持续学习中的“双重不平衡”问题，非常贴合食物识别的实际应用场景。
*   **数学逻辑严密：** 利用 SVD 进行光谱对齐和秩调制，相比简单的参数平均或正则化，具有更强的理论支撑和解释性。
*   **部署友好：** 最终合并为单一适配器，解决了持续学习中常见的“模型爆炸”问题，适合移动端部署。

### 8. 不足与局限
*   **超参数依赖：** 秩阈值调制中的 $r_h$ 和 $\gamma$ 等参数可能需要针对不同领域进行微调，虽然文中给出了敏感性分析，但尚未实现完全的自动化参数搜索。
*   **任务类型局限：** 目前仅验证了图像分类任务，尚未扩展到更复杂的食物理解任务，如成分识别（多标签）或食物分量估计（回归）。
*   **预训练模型依赖：** 效果高度依赖于高质量的预训练骨干网络（如 ImageNet-21K 预训练的 ViT），在完全无监督或跨域极大的场景下表现未知。

（完）
