---
title: Abstraction as a Memory-Efficient Inductive Bias for Continual Learning
title_zh: 抽象作为持续学习中一种内存高效的归纳偏置
authors: "Elnaz Rahmati, Nona Ghazizadeh, Zhivar Sourati, Nina Rouhani, Morteza Dehghani"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17198v1"
tags: ["query:cs-cv"]
score: 9.0
evidence: 使用抽象增强训练在严格的在线数据流中稳定学习
tldr: 针对在线持续学习中灾难性遗忘和泛化能力差的问题，本文提出了一种名为“抽象增强训练（AAT）”的方法。该方法通过在损失函数层面引入归纳偏置，促使模型同时学习具体实例及其背后的抽象关系结构。AAT无需重放缓冲区，在关系数据集和叙事数据集上的表现达到或超过了传统的经验重放基线，为实现内存高效的持续学习提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.17198v1/fig-001.webp\", \"caption\": \"Figure 4: Distractor generation prompt used for NAB.\", \"page\": 15, \"index\": 1, \"width\": 950, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2603.17198v1/fig-002.webp\", \"caption\": \"Table 1: Step-averaged online accuracy, forgetting, and cumulative accuracy on the RCB for Qwen and SmolLM.\", \"page\": 7, \"index\": 2, \"width\": 800, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.17198v1/fig-003.webp\", \"caption\": \"Table 5: Dataset statistics for RCB.\", \"page\": 13, \"index\": 3, \"width\": 787, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2603.17198v1/fig-004.webp\", \"caption\": \"Figure 5: Narrative expansion prompt used for NAB.\", \"page\": 16, \"index\": 4, \"width\": 950, \"height\": 954}]"
motivation: 旨在解决在线持续学习中新旧知识干扰导致的灾难性遗忘问题，并寻求一种无需重放缓冲区的内存高效方案。
method: 提出AAT方法，通过在训练目标中加入对抽象表示的联合优化，引导模型捕捉跨实例的共享潜在关系结构。
result: 在实体掩码关系任务和谚语叙事任务中，AAT在零额外内存占用的情况下，性能达到或优于强经验重放基线。
conclusion: 结构化抽象可以作为一种强大的、无需内存的归纳偏置，有效替代经验重放来稳定持续学习过程并提升泛化能力。
---

## 摘要
现实世界是非平稳且无限复杂的，这要求智能体能够持续学习，而无需承担从头开始重新训练的高昂成本。虽然在线持续学习为这一场景提供了框架，但学习新信息往往会干扰先前获得的知识，导致遗忘和泛化能力下降。为解决这一问题，我们提出了抽象增强训练（Abstraction-Augmented Training, AAT），这是一种损失函数层面的改进，旨在鼓励模型捕捉样本间共享的潜在关系结构。通过对具体实例及其抽象表示进行联合优化，AAT 引入了一种内存高效的归纳偏置，从而稳定了严格在线数据流中的学习过程，且无需重放缓冲区（replay buffer）。为了捕捉抽象的多面性，我们在两个基准测试上引入并评估了 AAT：一个是受控的关系型数据集，其中抽象通过实体掩码实现；另一个是叙事型数据集，其中抽象通过共享谚语来表达。我们的结果表明，尽管 AAT 不需要额外的内存，且仅对训练目标进行了极小的改动，但其性能仍可与强经验重放（ER）基线相媲美甚至更优。这项工作强调了结构化抽象是经验重放的一种强大且无需内存的替代方案。

## Abstract
The real world is non-stationary and infinitely complex, requiring intelligent agents to learn continually without the prohibitive cost of retraining from scratch. While online continual learning offers a framework for this setting, learning new information often interferes with previously acquired knowledge, causes forgetting and degraded generalization. To address this, we propose Abstraction-Augmented Training (AAT), a loss-level modification encouraging models to capture the latent relational structure shared across examples. By jointly optimizing over concrete instances and their abstract representations, AAT introduces a memory-efficient inductive bias that stabilizes learning in strictly online data streams, eliminating the need for a replay buffer. To capture the multi-faceted nature of abstraction, we introduce and evaluate AAT on two benchmarks: a controlled relational dataset where abstraction is realized through entity masking, and a narrative dataset where abstraction is expressed through shared proverbs. Our results show that AAT achieves performance comparable to or exceeding strong experience replay (ER) baselines, despite requiring zero additional memory and only minimal changes to the training objective. This work highlights structural abstraction as a powerful, memory-free alternative to ER.

---

## 论文详细总结（自动生成）

### 论文总结：抽象作为持续学习中一种内存高效的归纳偏置

#### 1. 核心问题与研究背景
论文探讨了**在线持续学习（Online Continual Learning, OCL）**中的两大核心挑战：**灾难性遗忘**（旧知识被覆盖）和**塑性损失**（模型适应新任务的能力下降）。
*   **研究动机**：现有的经验重放（ER）方法虽然有效，但依赖于存储旧数据的缓冲区，这在严格的在线流式场景下会带来巨大的内存开销。
*   **核心思想**：受人类认知科学启发（人类倾向于形成抽象图式而非孤立存储实例），作者提出通过“抽象”来捕捉跨样本的共享关系结构，以此作为一种无需额外内存的归纳偏置，减少参数更新时的梯度干扰。

#### 2. 方法论：抽象增强训练 (AAT)
AAT 是一种轻量级的损失函数级改进，其核心流程如下：
*   **双重目标优化**：当模型遇到一个具体实例（如“张三是李四的父亲”）时，同时生成其对应的符号化抽象表示（如“ENTITY1 是 ENTITY2 的父亲”）。
*   **联合损失函数**：
    $$L(\theta) = \alpha \cdot \text{NLL}(\hat{y}_{abs}, y_{abs}) + (1-\alpha) \cdot \text{NLL}(\hat{y}_{inst}, y_{inst})$$
    其中 $\alpha$ 控制抽象损失的权重。通过这种方式，模型被引导去学习不依赖于具体实体的底层关系逻辑。
*   **局部重放 (Local Replay)**：在不存储历史数据的前提下，对当前批次（Batch）进行 $n$ 次局部迭代（通常 $n \in [1, 5]$）。第一次迭代使用联合损失，后续迭代仅针对具体实例，以强化事实记忆。
*   **理论支撑**：通过梯度分析证明，抽象能抑制高方差的实体特定梯度，放大低方差的结构化梯度，从而平滑损失景观（Loss Landscape）。

#### 3. 实验设计
论文引入了两个全新的基准测试来评估模型的事实保留与结构泛化能力：
*   **数据集/场景**：
    1.  **Relational Cycle Benchmark (RCB)**：基于知识图谱构建，包含 8 个领域、51 种关系类型。通过实体掩码实现抽象，测试模型推断未观察到的关系边（Unknown Edges）的能力。
    2.  **Narrative Abstraction Benchmark (NAB)**：基于故事和谚语。抽象表现为共享的叙事主旨（如“欲速则不达”），要求模型在不同领域的故事中识别相同的抽象逻辑。
*   **对比方法**：
    *   **标准 SFT**（无抽象，$\alpha=0$）。
    *   **经验重放 (ER)**：设置了缓冲区大小为 50 和 100 的对比组。
*   **评估指标**：在线准确率（Online Acc）、遗忘率（Forgetting）、累积准确率（Cumulative Acc）。

#### 4. 资源与算力
*   **硬件**：使用了 **3 台 NVIDIA RTX A6000 GPU**。
*   **模型**：主要使用 **Qwen2.5-1.5B** 和 **SmolLM-1.7B** 作为骨干模型，附录中还测试了 Qwen2.5-3B 和 7B。
*   **训练时长**：在最大的 NAB 数据集上，每 10 步评估一次的情况下，训练耗时约 **1 小时 20 分钟**。

#### 5. 实验数量与充分性评价
实验设计较为全面且客观：
*   **多模型验证**：在 Qwen 和 SmolLM 两个不同家族的模型上验证了方法的通用性。
*   **消融实验**：详细探讨了抽象权重 $\alpha$、局部重放次数 $n$、以及不同抽象级别（随机抽象、类别抽象、掩码抽象）对结果的影响。
*   **鲁棒性测试**：通过 3 个不同的随机种子测试了数据顺序对在线学习的影响，并报告了均值和标准差。
*   **充分性**：实验不仅关注最终性能，还通过损失景观插值分析了优化动力学，证明了 AAT 对训练稳定性的贡献。

#### 6. 主要结论与发现
*   **性能优异**：在零额外内存占用的情况下，AAT 的表现达到或超过了拥有 100 个样本缓冲区的 ER 基线。
*   **泛化能力强**：AAT 在处理“未知边”（需要逻辑推理的任务）时表现尤为突出，提升幅度达 6.3%。
*   **稳定性**：抽象机制显著降低了损失表面的方差（降低约 17%），使梯度更新更加一致，减少了顺序学习中的干扰。
*   **模型无关性**：无论模型规模大小，引入抽象偏置均能带来一致的性能增益。

#### 7. 优点与亮点
*   **内存效率极高**：完全摒弃了重放缓冲区，解决了 OCL 在资源受限环境下的痛点。
*   **认知启发式设计**：将人类的类比推理和结构映射理论成功转化为深度学习的优化目标。
*   **高质量 Benchmark**：新提出的 RCB 和 NAB 数据集能够有效区分“死记硬背”与“结构化泛化”，对后续研究有参考价值。

#### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1.5B 到 7B 的模型上，尚未在超大规模参数（如 70B+）模型上验证其有效性。
*   **抽象获取的依赖性**：AAT 依赖于能够预先定义或生成的抽象表示（如实体掩码或谚语标签）。在完全无监督或缺乏结构化信息的原始文本流中，如何自动提取这些抽象仍是挑战。
*   **超参数敏感性**：虽然论文给出了推荐值，但 $\alpha$ 和 $n$ 的最优选择可能随任务复杂度动态变化，目前尚缺乏自适应调整机制。

（完）
