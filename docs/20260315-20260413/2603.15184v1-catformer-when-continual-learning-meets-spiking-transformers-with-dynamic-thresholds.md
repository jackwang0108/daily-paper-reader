---
title: "CATFormer: When Continual Learning Meets Spiking Transformers With Dynamic Thresholds"
title_zh: CATFormer：当持续学习遇上具有动态阈值的脉冲 Transformer
authors: "Vaishnavi Nagabhushana, Kartikay Agrawal, Ayon Borthakur"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15184v1"
tags: ["query:cs-cv"]
score: 10.0
evidence: 解决脉冲神经网络在类增量学习中的灾难性遗忘问题
tldr: 针对脉冲神经网络（SNN）在类增量学习中面临的灾难性遗忘问题，本文提出CATFormer框架。该研究受大脑神经调节机制启发，认为防止遗忘的关键在于调节神经元兴奋性而非仅靠突触塑性。通过引入动态阈值LIF神经元（DTLIF）和门控动态头选择机制（G-DHS），CATFormer在无需重演旧数据的情况下，在多个静态和神经形态数据集上显著提升了知识保留能力，为低能耗增量学习提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.15184v1/fig-001.webp\", \"caption\": \"Figure 3: Reverse Forgetting vs Catastrophic Forgetting Trend comparison (No. of Task vs Accuracy( in %)) of CATFormer against DSD-SNN [5] and same SpikFormer on [20, 21, 22, 23, 40]. The dotted line represents the average accuracy across tasks as the number of tasks increases.\", \"page\": 6, \"index\": 1, \"width\": 933, \"height\": 562}, {\"url\": \"assets/figures/arxiv/2603.15184v1/fig-002.webp\", \"caption\": \"Figure 1: Test performance variation with respect to the progress in the number of trained tasks (for a maximum of 50 tasks). CATFormer (ours) maintains consistent performance with other existing CIL methods when implemented on a Spiking Transformer. All methods are evaluated on CIFAR 100.\", \"page\": 2, \"index\": 2, \"width\": 830, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2603.15184v1/fig-003.webp\", \"caption\": \"Table 1: Comparison of standard CIL accuracy (%) on Split CIFAR-100 across different task granularities. Reported average test accuracy after all tasks. DSD-SNN results (25/50 tasks)[5]; other CIL baselines [20, 21, 22, 23, 40] are evaluated on SpikFormer [7]. Task 0 and Task k describe the total number of parameter updates during the 0th and kth training tasks, respectively.\", \"page\": 7, \"index\": 3, \"width\": 975, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2603.15184v1/fig-004.webp\", \"caption\": \"Table 2: Task-wise accuracy (%) of CATFormer with respect to the state-of-the-art SNNs on static and neuromorphic datasets. Methods marked with * denote online continual learning approaches.\", \"page\": 8, \"index\": 4, \"width\": 494, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.15184v1/fig-005.webp\", \"caption\": \"Figure 2: The diagram depicts the full architecture and workflow of CATFormer.\", \"page\": 4, \"index\": 5, \"width\": 790, \"height\": 560}]"
motivation: 现有的脉冲神经网络在处理类增量学习任务时，随着任务数量增加会出现严重的性能下降和灾难性遗忘问题。
method: 提出了结合动态阈值LIF神经元（DTLIF）与门控动态头选择机制（G-DHS）的上下文自适应阈值Transformer框架。
result: 在CIFAR-10/100、Tiny-ImageNet及神经形态数据集上的实验证明，CATFormer在无重演条件下性能优于现有的类增量学习算法。
conclusion: 通过调节神经元兴奋性实现知识保留的CATFormer，是构建高能效、真类增量学习架构的理想选择。
---

## 摘要
尽管深度神经网络在受控环境中表现极佳，但在现实场景中却往往失效。在现实场景中，数据并非一次性提供，模型必须适应可能与初始分布不一致的新数据分布。在基于新数据的后续更新过程中，先前获得的知识会丢失，这种现象通常被称为灾难性遗忘。相比之下，大脑无论遇到多少任务，都能在不发生此类灾难性遗忘的情况下进行学习。现有的用于类增量学习（CIL）的脉冲神经网络（SNN）随着任务的积累，性能会出现大幅下降。在此，我们介绍了 CATFormer（上下文自适应阈值 Transformer），这是一个克服了上述局限性的可扩展框架。我们观察到，防止 SNN 遗忘的关键不仅在于突触可塑性，还在于调节神经元的兴奋性。CATFormer 的核心是动态阈值漏电积分发放（DTLIF）神经元模型，该模型利用上下文自适应阈值作为知识保留的主要机制。这与门控动态头选择（G-DHS）机制相结合，用于任务无关的推理。在静态（CIFAR-10/100/Tiny-ImageNet）和神经形态（CIFAR10-DVS/SHD）数据集上的广泛评估表明，CATFormer 在各种任务划分下均优于现有的无重演（rehearsal-free）CIL 算法，使其成为高效能、真·类增量学习的理想架构。

## Abstract
Although deep neural networks perform extremely well in controlled environments, they fail in real-world scenarios where data isn't available all at once, and the model must adapt to a new data distribution that may or may not follow the initial distribution. Previously acquired knowledge is lost during subsequent updates based on new data. a phenomenon commonly known as catastrophic forgetting. In contrast, the brain can learn without such catastrophic forgetting, irrespective of the number of tasks it encounters. Existing spiking neural networks (SNNs) for class-incremental learning (CIL) suffer a sharp performance drop as tasks accumulate. We here introduce CATFormer (Context Adaptive Threshold Transformer), a scalable framework that overcomes this limitation. We observe that the key to preventing forgetting in SNNs lies not only in synaptic plasticity but also in modulating neuronal excitability. At the core of CATFormer is the Dynamic Threshold Leaky Integrate-and-Fire (DTLIF) neuron model, which leverages context-adaptive thresholds as the primary mechanism for knowledge retention. This is paired with a Gated Dynamic Head Selection (G-DHS) mechanism for task-agnostic inference. Extensive evaluation on both static (CIFAR-10/100/Tiny-ImageNet) and neuromorphic (CIFAR10-DVS/SHD) datasets reveals that CATFormer outperforms existing rehearsal-free CIL algorithms across various task splits, establishing it as an ideal architecture for energy-efficient, true-class incremental learning.

---

## 论文详细总结（自动生成）

### CATFormer：当持续学习遇上具有动态阈值的脉冲 Transformer

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**脉冲神经网络（SNN）在类增量学习（CIL）中的灾难性遗忘问题**。
*   **背景**：在现实世界的具身智能（如机器人、边缘设备）中，数据是流式到达的。标准深度学习模型在学习新任务时会迅速遗忘旧知识。
*   **动机**：现有的 SNN 增量学习方法大多依赖于“数据重演”（Rehearsal），即存储并回放旧数据，这在存储受限、隐私敏感或带宽有限的硬件上不可行。
*   **核心思想**：受大脑神经调节机制启发，作者认为防止遗忘的关键不仅在于突触塑性（权重更新），更在于**调节神经元的兴奋性（阈值调制）**。

#### 2. 方法论：核心思想与技术细节
作者提出了 **CATFormer (Context Adaptive Threshold Transformer)** 框架，其核心组件包括：
*   **DTLIF 神经元模型**：在传统的漏电积分发放（LIF）神经元基础上，引入了**可学习的任务特定动态阈值 $\phi^{(k)}$**。通过梯度下降调整每个通道的阈值，以适应不同任务的脉冲动力学。
*   **两阶段训练协议**：
    1.  **初始阶段（Task 0）**：联合训练 Backbone 权重、初始阈值和分类头。
    2.  **增量阶段（Task $k>0$）**：**冻结 Backbone 权重**，仅针对新任务优化该任务特有的动态阈值 $\phi^{(k)}$ 和分类头 $W_k$。这种做法确保了旧任务的特征提取能力不被破坏。
*   **G-DHS（门控动态头选择）机制**：为了实现任务无关的推理，设计了一个轻量级的门控 MLP。
    *   **推理流程**：输入样本首先通过基础阈值配置下的 Backbone 提取特征，由门控 MLP 预测任务 ID；随后，模型切换到对应任务的动态阈值配置，通过特定任务头输出最终分类结果。

#### 3. 实验设计
*   **数据集**：
    *   **静态图像**：CIFAR-10、CIFAR-100（划分为 10/20/25/50 个任务）、Tiny-ImageNet（100 个任务）。
    *   **神经形态（事件相机数据）**：CIFAR10-DVS、SHD（语音脉冲数据集）。
*   **Benchmark 与对比方法**：
    *   **正则化方法**：EWC, SI, MAS。
    *   **重演方法**：iCaRL, DER++, RDER。
    *   **SNN 专用方法**：DSD-SNN, ALADE-SNN。
    *   **基准架构**：以 SpikFormer 为 Backbone。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到了参数量信息。基础 SpikFormer 约为 9.32M，CATFormer 初始约为 10.5M（含 1.2M 路由机制）。每个新任务仅需额外存储约 64.2 KB 的阈值参数（FP32 格式），具有极高的存储效率。

#### 5. 实验数量与充分性
*   **实验规模**：作者进行了跨越多个维度的实验，包括不同任务粒度（从 2 任务到 100 任务）的对比。
*   **消融实验**：针对固定阈值、Identity Mixer（验证 Transformer 结构必要性）、冻结 FFN 等变体进行了 5 组核心消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到复杂的静态和动态数据集。特别是在 CIFAR-100 上进行了长达 50 个任务的测试，在 Tiny-ImageNet 上进行了 100 个任务的测试，这在同类研究中属于非常严苛且客观的评估。

#### 6. 主要结论与发现
*   **反向遗忘（Reverse Forgetting）现象**：这是论文最显著的发现。在 CATFormer 中，随着任务数量增加（每个任务包含的类减少），平均准确率反而呈现上升趋势（例如 CIFAR-100 从 10 任务的 68.33% 上升到 50 任务的 75.66%）。
*   **超越 SOTA**：在无重演（Rehearsal-free）条件下，CATFormer 的性能显著优于现有的正则化和架构扩展方法，甚至在某些指标上接近或超过了需要存储旧数据的重演方法。
*   **神经形态兼容性**：在 DVS 和音频脉冲数据集上表现稳健，证明了动态阈值机制与脉冲时空特性的天然契合。

#### 7. 优点（亮点）
*   **生物启发性强**：通过模拟神经调节改变阈值而非仅仅修改突触，提供了一种更符合神经科学逻辑的持续学习方案。
*   **极低存储开销**：无需存储旧样本，且每个新任务增加的参数量极小，非常适合资源受限的边缘计算。
*   **长序列扩展性**：打破了“任务越多性能越差”的常规，展示了在超长任务序列（100 任务）下的稳定性。

#### 8. 不足与局限
*   **推理延迟**：由于采用门控机制，推理过程需要两次前向传递（第一次确定任务 ID，第二次执行特定任务分类），这在实时性要求极高的场景下可能会增加延迟。
*   **Backbone 依赖**：增量阶段完全冻结 Backbone，这意味着模型极其依赖 Task 0 阶段学到的特征表示能力。如果初始任务的数据多样性不足，后续任务的泛化能力可能受限。
*   **算力细节缺失**：缺乏具体的训练环境和能耗对比数据，难以直观评估其在真实神经形态硬件（如 Loihi 2）上的加速效果。

（完）
