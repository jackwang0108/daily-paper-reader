---
title: "LACE: Loss-Adaptive Capacity Expansion for Continual Learning"
title_zh: LACE：面向持续学习的损失自适应容量扩张
authors: Shivnath Tathe
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28611v1"
tags: ["query:cs-cv"]
score: 9.0
evidence: 在训练期间扩展表示能力的在线机制
tldr: 针对持续学习中模型容量固定、需预先设定宽度的难题，本文提出LACE（损失自适应容量扩展）机制。该方法通过监控训练过程中的损失信号，在检测到当前容量不足以处理新数据时，自动在投影层增加新维度。LACE无需标签或重放缓存，能精准识别领域边界，在保持高性能的同时显著降低初始资源消耗，非常适合资源受限的设备端持续学习场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28611v1/fig-001.webp\", \"caption\": \"Fig. 12. From-scratch training on 10 sequential domains: loss curve and active dimensions. Loss spikes at each domain boundary trigger expansions.\", \"page\": 6, \"index\": 1, \"width\": 529, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.28611v1/fig-002.webp\", \"caption\": \"Fig. 11. GPT-2 activation space by layer, colored by true domain. Each subplot shows PCA projection of layer activations with cluster purity (p) and number of clusters (k).\", \"page\": 6, \"index\": 2, \"width\": 529, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2603.28611v1/fig-003.webp\", \"caption\": \"Fig. 10. Cluster center similarity matrix for toy K-Means experiment.\", \"page\": 6, \"index\": 3, \"width\": 450, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2603.28611v1/fig-004.webp\", \"caption\": \"Fig. 8. GPT-2 layer-wise domain separability. Purity drops in middle layers (3–7) where cross-token attention mixes representations, then recovers in deep layers (8–12).\", \"page\": 5, \"index\": 4, \"width\": 529, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.28611v1/fig-005.webp\", \"caption\": \"Fig. 7. Real-world experiment: Wikipedia → Code → Chat. LACE fires precisely at both domain boundaries (orange/purple vertical lines), expands from d = 32 to d = 34, and outperforms Fixed-Small while approaching Fixed-Large accuracy.\", \"page\": 5, \"index\": 5, \"width\": 529, \"height\": 364}]"
motivation: 传统的持续学习模型需要预先设定固定容量，难以适应未知数量的新概念和动态变化的数据流。
method: 通过实时监控损失信号的偏差，当损失超过阈值时自动扩展投影层维度，并与现有参数共同训练。
result: "LACE实现了100%的领域边界识别精度，并能以较小的初始参数量达到与大型固定模型相当的准确率。"
conclusion: LACE证明了通过损失信号自发驱动模型扩展的可行性，为无需重放缓存的在线持续学习提供了一种高效方案。
---

## 摘要
固定表示容量是持续学习中的一个基本约束：从业者必须在训练前预估合适的模型宽度，而无法预知数据中包含多少不同的概念。我们提出了 LACE（Loss-Adaptive Capacity Expansion，损失自适应容量扩张），这是一种简单的在线机制，通过监测自身的损失信号在训练过程中扩展模型的表示容量。当持续的损失偏差超过阈值时（表明当前容量不足以处理新遇到的数据），LACE 会向投影层添加新维度，并将其与现有参数共同训练。在合成数据和真实数据的实验中，LACE 仅在领域边界触发扩张（100% 的边界精确度，零误报），在仅以初始维度的一小部分开始的情况下，达到了与大型固定容量模型相当的准确率，且生成的适配器维度对性能具有集体关键性（移除所有适配器后准确率下降 3%）。我们进一步通过逐层聚类展示了 GPT-2 激活值中的无监督领域分离，揭示了跨层的 U 型可分离性曲线，这为深度网络中的自适应容量分配提供了依据。LACE 不需要标签、重放缓冲区或外部控制器，使其适用于资源受限下的端侧持续学习。

## Abstract
Fixed representational capacity is a fundamental constraint in continual learning: practitioners must guess an appropriate model width before training, without knowing how many distinct concepts the data contains. We propose LACE (Loss-Adaptive Capacity Expansion), a simple online mechanism that expands a model's representational capacity during training by monitoring its own loss signal. When sustained loss deviation exceeds a threshold - indicating that the current capacity is insufficient for newly encountered data - LACE adds new dimensions to the projection layer and trains them jointly with existing parameters. Across synthetic and real-data experiments, LACE triggers expansions exclusively at domain boundaries (100% boundary precision, zero false positives), matches the accuracy of a large fixed-capacity model while starting from a fraction of its dimensions, and produces adapter dimensions that are collectively critical to performance (3% accuracy drop when all adapters removed). We further demonstrate unsupervised domain separation in GPT-2 activations via layer-wise clustering, showing a U-shaped separability curve across layers that motivates adaptive capacity allocation in deep networks. LACE requires no labels, no replay buffers, and no external controllers, making it suitable for on-device continual learning under resource constraints.

---

## 论文详细总结（自动生成）

这是一份关于论文《LACE: Loss-Adaptive Capacity Expansion for Continual Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在持续学习（Continual Learning）中，神经网络通常面临**固定容量**的限制。开发者必须在训练前预设模型宽度（维度），但由于无法预知未来数据流中包含多少新概念或领域，往往会导致两种困境：预设太小则后期容量不足，预设太大则早期资源浪费。
该研究的核心动机是：**能否让模型根据自身需求，在训练过程中自动、在线地扩展容量？** 论文提出了一种无需外部干预、仅依赖损失信号驱动的自适应扩展机制。

### 2. 方法论：核心思想与关键技术
LACE 的核心思想是利用**训练损失（Loss）作为容量不足的信号**。
*   **损失尖峰检测（Loss-Based Novelty Detection）**：
    *   维护一个损失值的指数移动平均（EMA）作为基准。
    *   当当前损失与 EMA 的比值超过设定阈值 $\tau$，且这种偏差在确认窗口 $K$ 内持续存在时，判定为遇到了现有容量无法处理的新分布（领域边界）。
    *   设置冷却期 $C$ 以防止在单次领域切换中过度扩张。
*   **容量扩张（Capacity Expansion）**：
    *   一旦触发扩张，直接向投影矩阵 $W$ 添加新的维度（列）。
    *   新维度使用小的随机权重初始化，并与旧参数共同参与后续的梯度下降训练。
*   **算法流程**：模型在接收流式数据时，同步计算损失、更新参数并监控损失波动。若满足触发条件且未达到最大预设容量 $d_{max}$，则执行维度扩展。

### 3. 实验设计与 Benchmark
*   **数据集/场景**：
    *   **合成数据**：包含 10 到 50 个不同领域的字符级序列（如科学、新闻、代码、诗歌等），用于压力测试。
    *   **真实数据**：使用 Wikipedia（百科）、Python Code（代码）和 Persona-Chat（对话）三个异构领域进行顺序训练。
*   **对比基准（Baselines）**：
    *   **Fixed-Small**：固定使用较小的初始维度。
    *   **Fixed-Large**：固定使用预设的最大维度（上限）。
    *   **Dynamic (LACE)**：从 Small 开始，根据需要动态增长至 Large 的规模。
*   **分析实验**：包括消融实验（验证新增维度的必要性）、确认窗口 $K$ 的敏感度分析，以及对 GPT-2 激活值的无监督聚类分析。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   但从实验描述看，模型规模较小（如 GPT-2 嵌入层作为输入，投影层维度在 32-128 之间），且主要针对字符级任务，推测其算力需求较低，符合其“端侧学习（on-device learning）”的定位。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 10 领域到 50 领域的扩展性测试，以及真实世界数据集的验证。
*   **消融与鲁棒性**：进行了维度消融（证明新增维度不是冗余的）和超参数（确认窗口）的对比。
*   **充分性评价**：实验设计较为客观，通过 100% 的边界检测精度证明了机制的有效性。然而，实验主要集中在分类和简单的序列任务上，对于更复杂的生成任务或大规模视觉任务的覆盖尚显不足。

### 6. 主要结论与发现
*   **精准触发**：LACE 在所有实验中均实现了 100% 的领域边界识别精度，无误报。
*   **性能优异**：LACE 能以远低于 Fixed-Large 的平均参数量，达到与其接近的准确率（在真实数据实验中达到 96.9% 的性能，但平均维度减少了 74%）。
*   **分布式表示**：消融实验显示，移除所有新增维度会导致性能显著下降（3%），证明这些维度参与了“叠加态（Superposition）”式的特征存储，而非闲置。
*   **U型可分离性**：对 GPT-2 的分析发现，中间层领域分离度最低，最需要自适应容量。

### 7. 优点与亮点
*   **极简且高效**：无需标签、无需重放缓冲区（Replay Buffer）、无需复杂的神经架构搜索（NAS），仅靠损失信号驱动。
*   **在线性**：完全支持流式数据，适合资源受限的移动端或嵌入式设备。
*   **理论结合**：通过无监督聚类分析深度模型各层的领域分离度，为“在哪里扩展容量”提供了科学依据。

### 8. 不足与局限
*   **任务类型局限**：目前主要在分类和字符级任务上验证，尚未在生成式大模型或复杂视觉任务（如 Split-CIFAR/ImageNet）上广泛测试。
*   **遗忘问题**：论文承认在分类任务中未观察到严重遗忘，是因为输出头保留了所有类别，但在生成任务中，新旧知识的冲突（灾难性遗忘）可能依然存在，LACE 仅解决了“何时加容量”，未完全解决“如何保旧知识”。
*   **超参数敏感性**：损失尖峰检测依赖于阈值 $\tau$ 和窗口 $K$，在噪声极大的真实数据中，这些超参数可能需要精细调优。

（完）
